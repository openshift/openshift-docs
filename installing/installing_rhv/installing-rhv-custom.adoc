[id="installing-rhv-installer-custom"]
= Installing a cluster on Red Hat Virtualization with customizations
include::modules/common-attributes.adoc[]
:context: installing-rhv-installer-custom

toc::[]

In {product-title} version {product-version}, you can install a customized cluster on {rh-virtualization-first}. Here’s an overview of the steps:

. Creating an installation configuration file, `install-config.yaml`, by answering the interactive prompts.
. Inspecting and modifying parameters in `install-config.yaml`.
. Creating a working copy of `install-config.yaml`.
. Running the installer with a copy of `install-config.yaml` (no prompts).

.Prerequisites

You have completed the steps in xref:../../installing-rhv-preparing-to-install.adoc[], including:
* Preparing your {rh-virtualization} and network environments
* Setting up the installer software, SSH keys, and CA certificates.

.Procedure

include::modules/installing-rhv-create-install-config-file.adoc[leveloffset=+1]
include::modules/installing-rhv-responding-to-installer-prompts.adoc[leveloffset=+1]
include::modules/installing-rhv-inspect-and-modify-params-in-install-config.adoc[leveloffset=+1]
include::modules/installing-rhv-copying-install-config-and-running-installer.adoc[leveloffset=+1]

.Next Steps

.Troubleshooting
