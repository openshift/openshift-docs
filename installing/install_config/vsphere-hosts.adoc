[id="vsphere-hosts"]
= Configuring a cluster to use multiple vSphere hosts
include::modules/common-attributes.adoc[]
:context: vsphere-hosts

toc::[]

If you use a firewall, you must configure it so that {product-title} can access
Red Hat Insights. This access is required for {product-title} to access the
Telemetry service, which is required to receive updates.

include::modules/vsphere-multiple-hosts.adoc[leveloffset=+1]
