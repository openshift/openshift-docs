:_mod-docs-content-type: MAP
:context: when-my-pod-runs-a-workload-i-want-it-to

// TODO: Add module includes for this job
// Job name: When my pod runs a workload, I want it to automatically scale the pod so that the workload runs without incident due to a lack of resources.
