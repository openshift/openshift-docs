:_mod-docs-content-type: ASSEMBLY
[id="add-a-node-without-an-external-registry"]
= Add a node without an external registry
include::_attributes/common-attributes.adoc[]
:context: add-a-node-without-an-external-registry
:chunk-to-content:

[role="_abstract"]
When you need to add a node to a cluster in a disconnected environment, you can add the node without a complex process, so that you can quickly expand a cluster without internet access.

// GAP: The coverage map found no dedicated module for adding a node to an
// existing cluster installed without an external registry (OVE). The related
// module modules/installing-ove-hosts.adoc covers adding hosts during initial
// install only. Post-install node-expansion content must be authored.
// See JTBD-Disconnected-coverage-map.csv.
