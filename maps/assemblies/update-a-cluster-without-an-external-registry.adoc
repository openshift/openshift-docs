:_mod-docs-content-type: ASSEMBLY
[id="update-a-cluster-without-an-external-registry"]
= Update a cluster without an external registry
include::_attributes/common-attributes.adoc[]
:context: update-a-cluster-without-an-external-registry
:chunk-to-content:

[role="_abstract"]
When you need to update a cluster in a disconnected environment, you can update the cluster without having to set up a mirror registry and mirror images to the registry, so that you can quickly update a cluster without internet access.

// GAP: The coverage map found no existing module that covers updating a
// cluster installed without an external registry (OVE). Content must be
// authored before this job can be completed. See JTBD-Disconnected-coverage-map.csv.
