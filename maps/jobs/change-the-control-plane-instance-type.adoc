:_mod-docs-content-type: MAP
include::_attributes/common-attributes.adoc[]

[id="change-the-control-plane-instance-type_{context}"]
= Change the control plane instance type
:context: change-the-control-plane-instance-type

[role="_abstract"]
When you need to use a different instance type to back your control plane machines, you want to change the control plane instance type so that the control plane uses the correct instance type.

include::modules/cpms-changing-openstack-flavor-type.adoc[leveloffset=+1,navtitle="Change the {rh-openstack} compute flavor by using a control plane machine set",chunk="to-content"]

include::modules/cpms-changing-aws-instance-type.adoc[leveloffset=+1,navtitle="Change the Amazon Web Services instance type by using a control plane machine set"]

