:_mod-docs-content-type: ASSEMBLY
include::_attributes/common-attributes.adoc[]

[id="build-modules-against-the-linux-kernel_{context}"]
= Build modules against the Linux kernel.
:context: build-modules-against-the-linux-kernel
:job-gap:

[role="_abstract"]
When you want to build modules against the kernel package on nodes in a specific pool, you can use a machine config to add the RHEL kernel-devel package to all the nodes.

// job-gap: No mapped source content exists for this job.
// job-gap: mapping_status: unmapped