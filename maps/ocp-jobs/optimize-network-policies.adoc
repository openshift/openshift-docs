:_mod-docs-content-type: MAP
:context: optimize-network-policies

// TODO: This job is partially mapped. Only one module
// (nw-networkpolicy-optimize-ovn) actually addresses flow-count reduction,
// and it is guidance-only. Missing: (1) a procedure to measure OVS flow
// count / network policy cost (no oc/ovn-nbctl or metrics-based
// verification step), (2) documented scale limits for network policies and
// rules per cluster, (3) a worked before/after consolidation procedure for
// an existing set of policies, (4) an unresolved inline TODO in the module
// (OSDOCS-11830, 'Why? Because each policy has to spin up its own
// resources?').

// Decomposed from networking/network_security/network_policy/about-network-policy.adoc
include::modules/nw-networkpolicy-optimize-ovn.adoc[leveloffset=+0]

include::modules/nw-networkpolicy-about.adoc[leveloffset=+1]

// Decomposed from networking/network_security/network_policy/creating-network-policy.adoc
include::modules/nw-networkpolicy-object.adoc[leveloffset=+1]

// Decomposed from networking/network_security/network_policy/editing-network-policy.adoc
include::modules/nw-networkpolicy-edit.adoc[leveloffset=+1]
