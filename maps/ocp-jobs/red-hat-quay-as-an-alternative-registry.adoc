:_mod-docs-content-type: MAP
:context: red-hat-quay-as-an-alternative-registry

// TODO: This job is partially mapped. One short overview module covers
// Quay as an option. Nothing compares Quay against the integrated registry
// on the axes a planner needs -- storage backends, scale limits, and when
// replacing the integrated registry is warranted. Expand before this job
// can answer the question it poses.

include::modules/red-hat-quay-as-an-alternative-registry-con.adoc[leveloffset=+0,chunk="to-content"]

// Decomposed from registry/index.adoc
include::modules/registry-quay-overview.adoc[leveloffset=+1,toc="no"]
