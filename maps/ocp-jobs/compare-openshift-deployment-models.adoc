:_mod-docs-content-type: MAP
:context: compare-openshift-deployment-models

// TODO: This job is partially mapped. Partially mapped.
// installing-preparing-cluster-for-users-reference was dropped because it
// is a navigation-only module that must only ever be included from one
// location and its xrefs are relative to installing/overview/. Only the
// hosted-control-planes overview remains, so this job is now identical in
// content to the Compare architectural deployment models sub-job under
// Understand platform architecture. No module compares the deployment
// models side by side. Fold these two jobs together and write a single
// comparison module.

// Decomposed from hosted_control_planes/index.adoc
// TODO: Covers the hosted model only.
include::modules/hosted-control-planes-overview.adoc[leveloffset=+0,chunk="to-content"]
