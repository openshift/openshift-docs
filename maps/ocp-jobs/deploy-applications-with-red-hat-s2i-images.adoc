:_mod-docs-content-type: MAP
:context: deploy-applications-with-red-hat-s2i-images

// TODO: This job is partially mapped. The "use supported S2I images" half of
// the job statement is covered by the three sub-jobs included below, but the
// "locate supported Red Hat S2I and middleware images" half has no module
// behind it. The content that answers it -- registry.redhat.io, the
// openshift4 repository in the Quay registry, the -openshift xPaaS suffix,
// the Red Hat Ecosystem Catalog, and the image/cluster version-compatibility
// warning -- is written inline in the body of
// openshift_images/using_images/using-images-overview.adoc, which is an
// ASSEMBLY with no include::modules lines. Extract that into a CONCEPT module
// and add the "Find supported Red Hat container images" sub-job here once it
// exists.

include::modules/deploy-applications-with-red-hat-s2i-images-con.adoc[leveloffset=+0,chunk="to-content"]

// Sub-jobs. These are included last: each sets its own :context:, which would
// otherwise apply to any module included after it.
include::use-s2i-builder-images.adoc[leveloffset=+1]

include::select-builders-in-developer-console.adoc[leveloffset=+1]

include::customize-s2i-builder-behavior.adoc[leveloffset=+1]
