:_mod-docs-content-type: MAP
:context: add-node-without-registry

// TODO: This job is a documented gap. The coverage map analysis found no existing
// modules that cover adding a node to a cluster in a disconnected environment
// without a complex process and without requiring an external registry.
//
// This workflow would allow users to quickly expand a cluster without internet
// access, potentially using installation media similar to the OVE method.
//
// Required content:
// - Concept module explaining node addition without an external registry
// - Prerequisites (what must be in place before adding nodes)
// - Procedure for preparing node installation media
// - Procedure for booting and joining new nodes to the cluster
// - Post-addition verification steps
//
// Related content to investigate:
// - modules/installing-ove-hosts.adoc (booting hosts during OVE installation)
// - Check if OVE clusters support Day 2 node addition without registry
// - Standard machine/node addition procedures in machine_management/

