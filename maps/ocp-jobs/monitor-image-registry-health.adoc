:_mod-docs-content-type: MAP
:context: monitor-image-registry-health

include::modules/monitor-image-registry-health-con.adoc[leveloffset=+0]

// Decomposed from registry/accessing-the-registry.adoc
// TODO: This module only partially covers its job step: prerequisite: how
// to reach the registry before inspecting it
include::modules/registry-accessing-directly.adoc[leveloffset=+1]

// Sub-jobs. These are included last: each sets its own :context:, which
// would otherwise apply to any module included after it.
include::check-image-registry-pod-status.adoc[leveloffset=+1]

include::view-image-registry-logs.adoc[leveloffset=+1]

include::access-registry-metrics.adoc[leveloffset=+1]
