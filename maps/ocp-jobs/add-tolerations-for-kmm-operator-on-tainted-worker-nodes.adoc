:_mod-docs-content-type: MAP
:context: add-tolerations-for-kmm-operator-on-tainted-worker-nodes

// TODO: This job is partially mapped. Existing tolerations content targets
// the kernel-module worker pods; nothing covers adding tolerations to the
// KMM Operator itself through its OLM Subscription so it can schedule on
// tainted worker nodes.

// Decomposed from hardware_enablement/kmm-kernel-module-management.adoc
// TODO: covers tolerations on Module/worker pods, not on the KMM Operator
// Subscription
include::modules/kmm-using-tolerations-for-kernel-module-scheduling.adoc[leveloffset=+0]

// TODO: same - module pods rather than the Operator deployment
include::modules/kmm-applying-tolerations-to-kernel-module-pods.adoc[leveloffset=+1]
