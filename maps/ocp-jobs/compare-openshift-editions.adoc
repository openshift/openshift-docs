:_mod-docs-content-type: MAP
:context: compare-openshift-editions

// TODO: This job is partially mapped. Partially mapped at the parent level:
// welcome/openshift-editions.adoc is an ASSEMBLY whose entire edition
// comparison is written inline with no include::modules lines, so the
// parent has no module to include. Its only sub-job (Evaluate OpenShift
// Kubernetes Engine) is fully covered. Extract the editions comparison into
// a CONCEPT module.

include::evaluate-openshift-kubernetes-engine.adoc[leveloffset=+0,chunk="to-content"]
