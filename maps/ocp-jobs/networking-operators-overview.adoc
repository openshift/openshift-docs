:_mod-docs-content-type: MAP
:context: networking-operators-overview

// TODO: This job is partially mapped. Partially mapped. No module surveys
// the networking Operator set as a whole; each Operator has its own
// assembly and only the Cluster Network Operator and DNS Operator have a
// conceptual overview module. The Ingress, MetalLB, SR-IOV, Ingress Node
// Firewall, k8s-NMState, AWS Load Balancer, eBPF Manager, DPU, and External
// DNS Operators have no equivalent overview module to map.

include::modules/networking-operators-overview-con.adoc[leveloffset=+0,chunk="to-content"]

// Decomposed from networking/networking_operators/cluster-network-operator.adoc
// TODO: Cluster Network Operator only.
include::modules/nw-cluster-network-operator.adoc[leveloffset=+1,toc="no"]

// Decomposed from networking/networking_operators/dns-operator.adoc
// TODO: DNS Operator only.
include::modules/nw-dns-operator.adoc[leveloffset=+1,toc="no"]
