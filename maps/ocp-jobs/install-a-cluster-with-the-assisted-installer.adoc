:_mod-docs-content-type: MAP
:context: install-a-cluster-with-the-assisted-installer

// TODO: This job is a gap. Its source assembly is a pointer page
// that carries an abstract and links out but includes no modules,
// so this map holds only its sub-jobs. Write the shared content
// for this job and add it above the sub-job includes.

include::modules/install-a-cluster-with-the-assisted-installer-con.adoc[leveloffset=+0,chunk="to-content"]

// Sub-jobs. These come last: a job map sets its own :context:, which leaks
// past the include, so any module of this job placed after them would
// render under the wrong context.
include::install-an-oracle-cloud-cluster-with-the-assisted-installer.adoc[leveloffset=+1]
