:_mod-docs-content-type: MAP
:context: evaluate-high-availability-capabilities

// TODO: This job is partially mapped. Partially mapped. There is no
// dedicated high availability architecture module. The available content is
// scattered across control-plane.adoc and is already claimed by the Review
// control plane and node functionality sub-job, so this job largely
// duplicates it. Either write an HA architecture concept module or fold
// this job into that sub-job.

include::modules/evaluate-high-availability-capabilities-con.adoc[leveloffset=+0,chunk="to-content"]

// Decomposed from architecture/control-plane.adoc
// TODO: Describes control plane and compute roles; HA distribution is
// implicit.
include::modules/architecture-machine-roles.adoc[leveloffset=+1,toc="no"]

// TODO: etcd quorum only.
include::modules/etcd-overview.adoc[leveloffset=+1,toc="no"]

// cpmso-control-plane-recovery carries an ifeval block for
// "{context}" == "control-plane", the context of its source assembly
// architecture/control-plane.adoc. That block only sets :rosa-classic: and
// :osd:, which the module body does not reference -- it gates on the
// openshift-dedicated and openshift-rosa distro attributes instead. The block
// is a ROSA/OSD specialization and is inert for OCP, so the context is not
// borrowed here.
// TODO: Recovery, not HA architecture.
include::modules/cpmso-control-plane-recovery.adoc[leveloffset=+1,toc="no"]
