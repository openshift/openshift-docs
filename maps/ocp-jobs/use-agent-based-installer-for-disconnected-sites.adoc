:_mod-docs-content-type: MAP
:context: use-agent-based-installer-for-disconnected-sites

// TODO: Partially mapped job. No module documents the Agent-based Installer
// as the provisioning path for a {ztp}-managed far-edge site: the ABI
// toolchain and the hub-driven {ztp} pipeline are documented as separate
// installation methods. modules/installing-ocp-agent-ZTP.adoc is the only
// bridge, and it covers only generating {ztp} cluster manifests locally with
// `openshift-install agent create cluster-manifests`. Missing: guidance on
// when to choose the Agent-based Installer over hub-driven {ztp}
// provisioning for a disconnected site, and how an ABI-installed edge
// cluster is subsequently imported into and managed by the {ztp} pipeline.

// Decomposed from installing/installing_with_agent_based_installer/preparing-to-install-with-agent-based-installer.adoc
include::modules/understanding-agent-install.adoc[leveloffset=+0]

// Decomposed from installing/installing_with_agent_based_installer/understanding-disconnected-installation-mirroring.adoc
include::modules/agent-install-about-mirroring-for-disconnected-registry.adoc[leveloffset=+1]

include::modules/agent-install-configuring-for-disconnected-registry.adoc[leveloffset=+2]

// Decomposed from installing/installing_with_agent_based_installer/installing-with-agent-based-installer.adoc
include::modules/installing-ocp-agent-prereqs.adoc[leveloffset=+1]

include::modules/installing-ocp-agent-download.adoc[leveloffset=+1]

include::modules/installing-ocp-agent-inputs.adoc[leveloffset=+1]

// TODO: The only bridge between the Agent-based Installer and {ztp}, but it
// stops at generating {ztp} cluster manifests locally; it does not connect
// the site to a {ztp} hub pipeline.
include::modules/installing-ocp-agent-ZTP.adoc[leveloffset=+2]

include::modules/sample-ztp-custom-resources.adoc[leveloffset=+1]

include::modules/installing-ocp-agent-boot.adoc[leveloffset=+1]

include::modules/installing-ocp-agent-verify.adoc[leveloffset=+1]

include::modules/installing-ocp-agent-gather-log.adoc[leveloffset=+1]

// Decomposed from installing/installing_with_agent_based_installer/preparing-an-agent-based-installed-cluster-for-mce.adoc
// TODO: Prepares an ABI-installed disconnected cluster for multicluster
// engine; the closest content to making an ABI site manageable from a hub,
// but written for MCE rather than the {ztp} pipeline.
include::modules/preparing-an-initial-cluster-deployment-for-mce-disconnected.adoc[leveloffset=+1]
