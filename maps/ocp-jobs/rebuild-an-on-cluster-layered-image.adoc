:_mod-docs-content-type: MAP
:context: rebuild-an-on-cluster-layered-image

// Job name in the coverage map: "Force a rebuild of the object used with the on-cluster mode of the Image Mode for OpenShift feature"

include::modules/rebuild-an-on-cluster-layered-image-con.adoc[leveloffset=+0]

// Decomposed from machine_configuration/mco-coreos-layering.adoc
include::modules/coreos-layering-configuring-on-rebuild.adoc[leveloffset=+1]
