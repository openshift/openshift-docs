:_mod-docs-content-type: MAP
:context: compare-architectural-deployment-models

// TODO: This job is partially mapped. No module compares the standard, compact
// three-node, single-node, and hosted-control-plane topologies against
// operational scale and hardware limits -- only the hosted model is described.
// Two further modules that the coverage map originally listed,
// installing-preparing-selecting-cluster-type-reference and
// installing-preparing-supported-installation-methods-reference, were dropped
// because they are navigation-only modules marked "must only ever be included
// from one location" and carry 219 directory-relative xrefs that break from
// maps/ocp-jobs/.
// After that drop this job is identical in content to the top-level
// compare-openshift-deployment-models job. Fold the two together and write a
// single comparison module.

include::modules/compare-architectural-deployment-models-con.adoc[leveloffset=+0,chunk="to-content"]

// TODO: Covers the hosted control planes model only; does not compare it
// against the other topologies.
// Decomposed from hosted_control_planes/index.adoc
include::modules/hosted-control-planes-overview.adoc[leveloffset=+1,toc="no"]
