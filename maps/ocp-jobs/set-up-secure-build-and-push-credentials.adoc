:_mod-docs-content-type: MAP
:context: set-up-secure-build-and-push-credentials

// TODO: This job is partially mapped. No module documents the registry push
// credentials a KMM build needs (the secret referenced by the Module build
// spec). Writer must add one, or confirm the generic pull-secret module is
// sufficient and cross-reference it. Parent 'Manage kernel modules across
// cluster nodes' is not a job in the Secure category; orphan sub-job.

include::modules/set-up-secure-build-and-push-credentials-con.adoc[leveloffset=+0,chunk="to-content"]

// Decomposed from hardware_enablement/kmm-kernel-module-management.adoc
// TODO: Creates the signing key/certificate secrets, not the registry push
// credentials
include::modules/kmm-adding-the-keys-for-secureboot.adoc[leveloffset=+1,toc="no"]

// TODO: Verifies the signing secrets only
include::modules/kmm-checking-the-keys.adoc[leveloffset=+2,toc="no"]

// Decomposed from openshift_images/managing_images/using-image-pull-secrets.adoc
// TODO: Generic pull-secret creation; not scoped to KMM build/push
include::modules/creating-pull-secrets.adoc[leveloffset=+2,toc="no"]
