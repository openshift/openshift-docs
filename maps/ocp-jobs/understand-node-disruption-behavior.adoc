:_mod-docs-content-type: MAP
:context: understand-node-disruption-behavior

// Merged from two coverage-map jobs that resolved to the same content:
// "Understand node drain behavior when updating the node configuration" and
// "Understand which configuration changes cause a node reboot and how to
// reduce the disruptions". The second was a strict superset of the first by
// one module, and the first job's statement already covered both.

// Decomposed from machine_configuration/index.adoc
include::modules/machine-config-node-drain.adoc[leveloffset=+0]

// TODO: observing the rollout while it disrupts nodes
include::modules/checking-mco-node-status-configuring.adoc[leveloffset=+1]

// Decomposed from machine_configuration/machine-config-node-disruption.adoc
include::modules/machine-config-node-disruption-example.adoc[leveloffset=+1]

include::modules/machine-config-node-disruption-config.adoc[leveloffset=+1]
