:_mod-docs-content-type: MAP
:context: manage-kernel-modules-across-cluster-nodes-deploy-device-plugins-alongside-kernel-modules

// TODO: This job is partially mapped. The two halves of the job exist but are
// never joined: the nodes/pods modules explain generic device plugins, and the
// KMM modules explain kernel module loading. No module walks an administrator
// from a KMM-loaded kernel module to a device plugin that advertises the
// hardware that module exposes. Add a bridging PROCEDURE, or add explicit
// sequencing to kmm-deploying-modules.adoc.

// Decomposed from nodes/pods/nodes-pods-plugins.adoc
include::modules/nodes-pods-plugins-about.adoc[leveloffset=+0,chunk="to-content"]

include::modules/nodes-pods-plugins-device-mgr.adoc[leveloffset=+1,toc="no"]

include::modules/nodes-pods-plugins-install.adoc[leveloffset=+1,toc="no"]

// Decomposed from hardware_enablement/kmm-kernel-module-management.adoc
// TODO: Deploys kernel modules with the Module CR and names the devicePlugin
// container, but does not walk the device plugin deployment itself.
include::modules/kmm-deploying-modules.adoc[leveloffset=+1,toc="no"]

// TODO: Reference-level coverage of the Module CR fields, including the device
// plugin spec.
include::modules/kmm-creating-module-cr.adoc[leveloffset=+2,toc="no"]
