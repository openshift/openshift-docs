:_mod-docs-content-type: MAP
:context: follow-plugin-development-guidelines

// TODO: This job is partially mapped. Of the three guideline areas named in
// the job statement -- PatternFly, localization, and CSS -- only localization
// has a module behind it (dynamic-plugin-localization). The "General
// guidelines" section (CSS class-name prefixing, avoiding selectors that leak
// outside the plugin, Node.js, Webpack 5, MIME types) and the "PatternFly
// guidelines" section (the PatternFly version matrix per OCP release,
// accessibility fundamentals, not using Bootstrap or Tailwind) are written
// inline in the body of
// web_console/dynamic-plugin/overview-dynamic-plugin.adoc and have no
// modules. Extract both sections into CONCEPT modules and add them here.

include::modules/follow-plugin-development-guidelines-con.adoc[leveloffset=+0,chunk="to-content"]

// Decomposed from web_console/dynamic-plugin/overview-dynamic-plugin.adoc
// Promoted from +2 to +1: in the source assembly this module sits under the
// inline "PatternFly guidelines" section, which this job map drops as
// assembly chrome.
include::modules/dynamic-plugin-localization.adoc[leveloffset=+1,toc="no"]

// Decomposed from web_console/dynamic-plugin/content-security-policy.adoc
// TODO: csp-overview covers the Content Security Policy constraints a plugin
// must satisfy, which is adjacent to the CSS guidelines this job asks for, but
// is not those guidelines.
include::modules/csp-overview.adoc[leveloffset=+1,toc="no"]

// Decomposed from web_console/dynamic-plugin/dynamic-plugins-get-started.adoc
// TODO: prerequisite environment setup rather than guideline content.
include::modules/dynamic-plug-in-development.adoc[leveloffset=+1,toc="no"]

// Decomposed from web_console/dynamic-plugin/dynamic-plugins-reference.adoc
// TODO: reference material that plugin authors must conform to, not the
// guidelines themselves.
include::modules/dynamic-plugin-sdk-extensions.adoc[leveloffset=+1,toc="no"]

include::modules/dynamic-plugin-api.adoc[leveloffset=+1,toc="no"]
