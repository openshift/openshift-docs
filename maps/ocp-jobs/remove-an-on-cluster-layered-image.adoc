:_mod-docs-content-type: MAP
:context: remove-an-on-cluster-layered-image

// Job name in the coverage map: "Remove any versions of the object needed to use the on-cluster mode of the Image Mode for OpenShift feature"

// Decomposed from machine_configuration/mco-coreos-layering.adoc
include::modules/coreos-layering-configuring-on-remove.adoc[leveloffset=+0]
