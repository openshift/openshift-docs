:_mod-docs-content-type: MAP
:context: access-the-web-console

// NOTE: the coverage map lists "Access the web console" and "Access the web
// console URL" as separate jobs, but one module covers both. Merged here.

// Decomposed from web_console/web-console.adoc
include::modules/web-console-overview.adoc[leveloffset=+0]

// Decomposed from web_console/web-console-overview.adoc. The snippet is kept
// from that assembly's intro because it is support-critical: the Developer
// perspective is no longer enabled by default as of 4.19, which is what the
// enabling procedure below exists for.
include::snippets/snip-unified-perspective-web-console.adoc[]

// TODO: describes the Administrator perspective, not console access itself.
include::modules/about-administrator_web-console.adoc[leveloffset=+1]

// TODO: describes the Developer perspective, not console access itself.
include::modules/about-developer_web-console.adoc[leveloffset=+1]

// TODO: a follow-on task once the console is accessible, not access itself.
include::modules/enabling-developer-perspective_web-console.adoc[leveloffset=+1]
