:_mod-docs-content-type: MAP
:context: detect-sudden-increases-in-bandwidth-utilization

// TODO: This job is partially mapped. CSV malformation: the source sheet
// lists the parent as a bare section number ("5.1"/"5.2"/"5.3"/"5.4")
// instead of a job name. Reassigned to "Traffic Analysis", the top-level
// job whose statement matches the 5.x series. Missing: no procedure for
// detecting a bandwidth spike; see 5.7 for the related alerting gap.

// Decomposed from observability/network_observability/observing-network-traffic.adoc
// TODO: This module only partially covers its job step: histogram shows
// volume over time but is not framed as spike detection
include::modules/network-observability-histogram-trafficflow.adoc[leveloffset=+0]
