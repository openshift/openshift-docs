:_mod-docs-content-type: MAP
:context: calculate-prometheus-storage-requirements

// TODO: This job is partially mapped. No module provides a calculation
// that turns cluster scale -- node count, series count, scrape interval --
// into a Prometheus storage figure. The existing content documents the
// knobs, not how to size them. Add a sizing REFERENCE module with a worked
// example.

include::modules/calculate-prometheus-storage-requirements-con.adoc[leveloffset=+0,chunk="to-content"]

// Decomposed from observability/monitoring/configuring-core-platform-monitoring/storing-and-recording-data.adoc
include::modules/monitoring-modifying-retention-time-and-size-for-prometheus-metrics-data.adoc[leveloffset=+1,toc="no"]

// modules/monitoring-configuring-persistent-storage.adoc is listed for
// this job in the coverage map but is omitted here: it is already included
// by the parent job, "Size monitoring infrastructure".
