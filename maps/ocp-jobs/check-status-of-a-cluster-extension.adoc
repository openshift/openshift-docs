:_mod-docs-content-type: MAP
:context: check-status-of-a-cluster-extension

// TODO: This job is partially mapped. No module documents the first step the
// job statement asks for: reading the ClusterExtension status conditions
// (Installed, Progressing, Deprecated) and deciding what to do next. Every
// module below covers one failure class after the fact. Add a PROCEDURE for
// inspecting `oc get clusterextension -o yaml` output, plus a reference table
// mapping each status condition to its next troubleshooting step. Note that
// the ClusterObjectSets path is Technology Preview and cannot carry this job
// on its own.

include::modules/check-status-of-a-cluster-extension-con.adoc[leveloffset=+0,chunk="to-content"]

// Decomposed from extensions/ce/managing-ce.adoc
// TODO: Covers the RBAC failure class only.
include::modules/olmv1-troubleshooting-rbac-errors-with-preflight-check.adoc[leveloffset=+1,toc="no"]

// Decomposed from extensions/catalogs/catalog-content-resolution.adoc
// TODO: Covers catalog resolution failures only.
include::modules/olmv1-troubleshooting-catalog-selection-errors.adoc[leveloffset=+1,toc="no"]
