:_mod-docs-content-type: MAP
:context: review-kmm-releases-and-updates

// TODO: Partially mapped job. The release notes themselves are complete and
// well modularized -- seven REFERENCE modules, one per KMM version. Two things
// the job statement asks for are missing. First, the three content types are
// uneven across versions: 2.2 and 2.3 carry new features only, with no bug
// fixes and no known issues sections; 2.4.1 and 2.5.1 carry known issues but
// no bug fixes. Only 2.4, 2.5 and 2.6 come close to the full set. Second, the
// "so I can plan upgrades" half of the job has no supporting content: there is
// no KMM Operator to {product-title} version compatibility matrix module, and
// no procedure for upgrading the KMM Operator itself.

// Decomposed from hardware_enablement/kmm-kernel-module-management.adoc
include::modules/kmm-about-kmm.adoc[leveloffset=+0]

// TODO: Installation overview; supplies the version and channel context the
// release notes assume, rather than release content itself.
include::modules/kmm-installation.adoc[leveloffset=+1]

// Decomposed from hardware_enablement/kmm-release-notes.adoc
include::modules/kmm-rn-2-6.adoc[leveloffset=+1]

include::modules/kmm-rn-2-5-1.adoc[leveloffset=+1]

include::modules/kmm-rn-2-5.adoc[leveloffset=+1]

include::modules/kmm-rn-2-4-1.adoc[leveloffset=+1]

include::modules/kmm-rn-2-4.adoc[leveloffset=+1]

// TODO: New features only -- no bug fixes and no known issues sections, so
// this version does not answer the full job statement.
include::modules/kmm-rn-2-3.adoc[leveloffset=+1]

// TODO: New features only -- no bug fixes and no known issues sections, so
// this version does not answer the full job statement.
include::modules/kmm-rn-2-2.adoc[leveloffset=+1]

// Decomposed from hardware_enablement/kmm-kernel-module-management.adoc
// TODO: The only version-selection guidance in the docs. It supports the
// "plan upgrades" half of the job, but is written for earlier
// {product-title} versions rather than for moving between KMM versions.
include::modules/kmm-installing-older-versions.adoc[leveloffset=+1]

// TODO: Covers upgrading the kernel modules KMM manages during a cluster
// upgrade -- adjacent to the job, but not an Operator upgrade procedure.
include::modules/kmm-customizing-upgrades-for-kernel-modules.adoc[leveloffset=+1]
