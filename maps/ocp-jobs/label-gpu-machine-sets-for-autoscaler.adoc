:_mod-docs-content-type: MAP
:context: label-gpu-machine-sets-for-autoscaler

// TODO: This job is partially mapped. Partial. The Machine API module is
// rendered from the autoscaling assembly and reused by nearly every
// creating-machineset-* provider assembly (AWS, Azure, Azure Stack Hub,
// bare metal, GCP, IBM Cloud, Nutanix, OpenStack, vSphere); it is not
// included by creating-machineset-ibm-power-vs. What is missing is the
// Cluster API equivalent: cluster-api-config-options-aws.adoc and
// cluster-api-config-options-bare-metal.adoc both include
// modules/machine-feature-agnostic-options-label-gpu-autoscaler.adoc, but
// that module file does not exist on this branch, so the CAPI GPU
// autoscaler labelling content is a broken include and renders nothing.

// Decomposed from machine_management/applying-autoscaling.adoc
include::modules/machineset-label-gpu-autoscaler.adoc[leveloffset=+0]

// modules/machineset-label-gpu-autoscaler.adoc omitted here: already included above in this job.
