:_mod-docs-content-type: MAP
:context: update-cluster-without-registry

// TODO: This job is a documented gap. The coverage map analysis found no existing
// modules that cover updating a cluster in a disconnected environment without
// having to set up a mirror registry and mirror images to the registry.
//
// This workflow would allow users to quickly update a cluster without internet
// access using self-contained update media (similar to the OVE installation method).
//
// Required content:
// - Concept module explaining how updates work without an external registry
// - Procedure for downloading update media/ISO
// - Procedure for applying updates using the self-contained media
// - Considerations for Operator updates in this scenario
//
// Related content to investigate:
// - installing/installing_with_agent_based_installer/installing-ove.adoc (installation without registry)
// - Check if OVE clusters have a special update mechanism
// - disconnected/updating/ assemblies for standard disconnected update workflows

