:_mod-docs-content-type: MAP
:context: apply-configuration-changes-to-compute-machines-with-automation

// TODO: This job is partially mapped. No module describes automated rollout
// of a compute machine set configuration change to existing machines; the
// control plane equivalent (cpmso-feat-auto-update) has no compute-side
// counterpart, so the reader is left to delete and recreate machines
// manually.

include::modules/apply-configuration-changes-to-compute-machines-with-automation-con.adoc[leveloffset=+0]

// Decomposed from machine_management/modifying-machineset.adoc
// TODO: shows the edit but not how the change is rolled out to existing
// machines
include::modules/machineset-modifying.adoc[leveloffset=+1]
