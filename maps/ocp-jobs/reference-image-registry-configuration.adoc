:_mod-docs-content-type: MAP
:context: reference-image-registry-configuration

// TODO: Partially mapped job. The reference material exists but is scattered
// exactly as the job statement describes: Image Registry Operator parameters
// live in registry/configuring-registry-operator.adoc and there is no
// consolidated reference assembly today. Platform-specific storage parameters
// are deliberately not repeated here -- every module that would have formed a
// platform storage sub-job already renders under
// maps/ocp-jobs/configure-registry-storage-by-platform.adoc in the Configure
// category, and navigation.adoc builds all categories into one book. The
// platform coverage gap recorded in the coverage map still stands: parameter
// reference tables exist only for AWS S3, {gcp-short} GCS and
// {rh-openstack} Swift, not for Azure Blob storage, vSphere, bare metal,
// Nutanix or {rh-storage}.

// New intro concept module written for this job.
include::modules/reference-image-registry-configuration-con.adoc[leveloffset=+0]

// Decomposed from registry/index.adoc
include::modules/registry-integrated-openshift-registry.adoc[leveloffset=+1]

// TODO: Registry terminology; orientation for the reference job rather than
// configuration parameters.
include::modules/registry-common-terms.adoc[leveloffset=+1]

// Nested sub-jobs. These come last because each sub-job map sets its
// own :context:, which would leak into any include that followed.
include::image-registry-operator-configuration-parameters.adoc[leveloffset=+1]
