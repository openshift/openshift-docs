:_mod-docs-content-type: MAP
:context: prepare-to-install-openshift

include::modules/prepare-to-install-openshift-con.adoc[leveloffset=+0,chunk="to-content"]

// Phase 2 of the installation journey: set up the installation host. These are
// the only modules in the journey that carry no platform conditionals, so they
// render correctly under this job's own context and belong here rather than in
// each platform supplement.
// Decomposed from installing/installing_aws/ipi/ipi-aws-preparing-to-install.adoc
include::modules/cli-installing-cli-linux.adoc[leveloffset=+1,toc="no"]

include::modules/cli-installing-cli-macos.adoc[leveloffset=+1,toc="no"]

include::modules/cli-installing-cli-windows.adoc[leveloffset=+1,toc="no"]

// TODO: ssh-agent-using and installation-obtaining-installer also belong to this
// phase, but they are 42% and 92% platform-conditional respectively. They need
// either a borrowed context per platform or a neutral default branch before they
// can live in a shared job.
