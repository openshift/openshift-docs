[id="image-pull-policy"]
= Image pull policy
include::modules/common-attributes.adoc[]
:context: image-pull-policy

toc::[]

Each container in a Pod has a container image. Once you have created an image
and pushed it to a registry, you can then refer to it in the Pod.

include::modules/images-image-pull-policy-overview.adoc[leveloffset=+1]
