:_mod-docs-content-type: ASSEMBLY
[id="configuring-openshift-builds"]
include::_attributes/common-attributes.adoc[]
= Configuring {builds-shortname}
:context: configuring-openshift-builds

toc::[]

The OpenShift Pipelines injects proxy environment variables in the network restricted environments. As a cluster administrator, you can verify cluster-wide proxy settings and environment variables to ensure that the proxy settings are correctly configured for builds on an OpenShift Container Platform cluster.

include::modules/ob-verifying-cluster-wide-proxy.adoc[leveloffset=+1]



// [role="_additional-resources"]
// == Additional resources
