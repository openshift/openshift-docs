//NOTE TO CONTRIBUTORS:
//
//If you update any of the content in this assembly file, be sure to also make the same changes in the assemblies in the following file: etcd/etcd-backup-restore/replace-healthy-etcd-member.adoc.

:_mod-docs-content-type: ASSEMBLY
[id="replace-healthy-etcd-member"]
include::_attributes/common-attributes.adoc[]
= Replacing a healthy etcd member
:context: replace-healthy-etcd-member

toc::[]

You might need to replace a healthy etcd member for planned hardware maintenance, hardware upgrades, or migration to new infrastructure. The process to replace a single healthy etcd member depends on whether the cluster has access to the Machine API and a `ControlPlaneMachineSet` (CPMS) or not. With the Machine API and CPMS, deleting the machine for the node to be replaced results in the automatic creation of a replacement machine. Without the Machine API or CPMS, the machine for the node to be replaced must be deleted and replaced manually.

include::modules/determining-cluster-access-machine-api.adoc[leveloffset=+2]

include::modules/replacing-etcd-member-machine-api.adoc[leveloffset=+2]

include::modules/replacing-etcd-member-manually.adoc[leveloffset=+2]
