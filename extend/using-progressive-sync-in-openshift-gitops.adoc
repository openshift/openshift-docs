:_mod-docs-content-type: ASSEMBLY
[id="using-progressive-sync-in-openshift-gitops"]
= Use Progressive Sync
include::_attributes/common-attributes.adoc[]
:context: using-progressive-sync-in-openshift-gitops

toc::[]

--
:FeatureName: Progressive Sync
// When including this file, ensure that {FeatureName} is set immediately before
// the include. Otherwise it will result in an incorrect replacement.

[IMPORTANT]
====
[subs="attributes+"]
{FeatureName} is a Technology Preview feature only. Technology Preview features
are not supported with Red Hat production service level agreements (SLAs) and
might not be functionally complete. Red Hat does not recommend using them
in production. These features provide early access to upcoming product
features, enabling customers to test functionality and provide feedback during
the development process.

For more information about the support scope of Red Hat Technology Preview features, see link:https://access.redhat.com/support/offerings/techpreview/[Technology Preview Features Support Scope].
====
// Undefine {FeatureName} attribute, so that any mistakes are easily spotted
:!FeatureName:
--

[role="_abstract"]
Progressive Sync supports application rollout strategies by managing synchronization across multiple clusters. Configure `ApplicationSet` strategies to control how applications are created, updated, and deleted.

You can configure creation strategies (`AllAtOnce` or `RollingSync`) to control application deployment patterns and deletion strategies to manage application removal order. When you enable Progressive Sync and configure a `RollingSync` strategy, the {gitops-title} console displays real-time visibility into sync waves, application health, and deployment progress.

[NOTE]
====
To monitor Progressive Sync status in the {gitops-shortname} Web UI, ensure that the feature is enabled in your Argo CD controller and your `ApplicationSet` uses a `RollingSync` strategy.
====

Enable Progressive Sync, configure `ApplicationSet` strategies, and monitor rollout behavior.

[id="prerequisites_{context}"]
== Prerequisites

* You have logged in to the {OCP} cluster as an administrator.
* You have installed the {gitops-shortname} Operator on your {OCP} cluster.
* You can access the default Argo CD instance in the `openshift-gitops` namespace.
* You have created an Argo CD `ApplicationSet` resource in your defined namespace, for example, `openshift-gitops`.

// Overview of enabling Progressive Sync
include::modules/gitops-overview-enabling-progressive-sync.adoc[leveloffset=+1]

// Enabling Progressive Sync
include::modules/gitops-enabling-progressive-sync.adoc[leveloffset=+1]

// Adding the extraCommandArgs argument to the Argo CD CR
include::modules/gitops-adding-extraCommandArgs-to-argo-cd.adoc[leveloffset=+2]

// Setting the env environment variable in the Argo CD CR
include::modules/gitops-setting-env-to-argo-cd.adoc[leveloffset=+2]

// Understanding ApplicationSet strategies
include::modules/gitops-applicationset-strategies.adoc[leveloffset=+1]

// Understanding creation strategies
include::modules/gitops-applicationset-creation-strategies.adoc[leveloffset=+1]

// Configuring AllAtOnce creation strategy
include::modules/gitops-configuring-allatonce-creation-strategy.adoc[leveloffset=+2]

// Example: AllAtOnce creation strategy for simultaneous deployment
include::modules/gitops-example-allatonce-creation-strategy.adoc[leveloffset=+2]

// Configuring RollingSync strategy
include::modules/gitops-configuring-rollingsync-strategy-enhanced.adoc[leveloffset=+2]

// Example: Progressive sync with environment labels
include::modules/gitops-example-rollingsync-progressive-sync.adoc[leveloffset=+2]

// Understanding deletion strategies
include::modules/gitops-applicationset-deletion-strategies.adoc[leveloffset=+1]

// Configuring AllAtOnce deletion strategy
include::modules/gitops-configuring-allatonce-deletion-strategy.adoc[leveloffset=+2]

// Example: AllAtOnce deletion strategy for multi-cluster deployments
include::modules/gitops-example-allatonce-deletion-strategy.adoc[leveloffset=+2]

// Configuring ApplicationSet deletion order
include::modules/gitops-configuring-applicationset-reverse-deletion-strategy.adoc[leveloffset=+2]

// Example: Reverse deletion order with dependent services
include::modules/gitops-example-reverse-deletion-order.adoc[leveloffset=+2]

// Troubleshooting Progressive Sync
include::modules/gitops-troubleshooting-progressive-sync.adoc[leveloffset=+1]

// Troubleshooting Progressive Sync section not appearing
include::modules/gitops-troubleshooting-progressive-sync-section-not-appearing.adoc[leveloffset=+2]

// Troubleshooting Progressive Sync unknown state
include::modules/gitops-troubleshooting-progressive-sync-unknown-state.adoc[leveloffset=+2]

[role="_additional-resources"]
== Additional resources

* xref:../install/installing-openshift-gitops.adoc#installing-openshift-gitops[Install the GitOps Operator]
* link:https://argo-cd.readthedocs.io/en/stable/operator-manual/applicationset/#the-applicationset-resource[The `ApplicationSet` resource]
* xref:../configure/argo-cd-cr-component-properties.adoc#argo-cd-cr-component-properties[Configure Argo CD custom resource properties]
* link:https://argo-cd.readthedocs.io/en/stable/operator-manual/applicationset/Progressive-Syncs/[Progressive Sync, Argo CD upstream documentation]