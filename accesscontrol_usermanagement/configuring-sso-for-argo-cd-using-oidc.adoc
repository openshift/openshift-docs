:_mod-docs-content-type: ASSEMBLY
include::_attributes/common-attributes.adoc[]
[id="configuring-sso-for-argo-cd-using-oidc"]
= Configuring SSO for Argo CD using external OIDC providers
:context: configuring-sso-for-argo-cd-using-oidc

toc::[]

[role="_abstract"]
After the {gitops-title} Operator is installed, Argo CD automatically creates a user with `admin` permissions. To manage multiple users, cluster administrators can use Argo CD to configure Single Sign-On (SSO) with external OpenID Connect (OIDC) providers.

// Understanding OIDC integration approaches
include::modules/gitops_oidc-integration-approaches.adoc[leveloffset=+1]

[id="prerequisites_{context}"]
== Prerequisites

* The {gitops-title} Operator is installed on your {OCP} cluster.
* You have access to the cluster with `cluster-admin` privileges.
* You have configured an OIDC-compliant identity provider according to the provider's official documentation.
* You have obtained the following information from your OIDC provider:
** Issuer URL
** Client ID
** Client secret

// Configuring direct OIDC integration for Argo CD
include::modules/gitops_configuring-direct-oidc-integration.adoc[leveloffset=+1]

// Verifying OIDC login
include::modules/gitops_verifying-oidc-login.adoc[leveloffset=+1]

[role="_additional-resources"]
.Additional resources
* link:https://argo-cd.readthedocs.io/en/stable/operator-manual/user-management/#sso[Argo CD upstream documentation, SSO Configuration section]
* link:https://argo-cd.readthedocs.io/en/stable/operator-manual/user-management/[Argo CD upstream documentation, User Management section]
