:_mod-docs-content-type: ASSEMBLY
[id="about-sriov"]
= About Single Root I/O Virtualization (SR-IOV) hardware networks
include::_attributes/common-attributes.adoc[]
:context: about-sriov

toc::[]

[role="_abstract"]
Single Root I/O Virtualization (SR-IOV) enables high-performance networking by allowing a single physical network device to appear as multiple virtual devices that can be assigned directly to pods.

[IMPORTANT]
====
*New to SR-IOV?* Start with xref:../../networking/hardware_networks/sriov-getting-started.adoc#sriov-getting-started[Get started with SR-IOV secondary networks] for a complete walkthrough from installation to your first working pod.
====

[id="when-to-use-sriov_{context}"]
== When to use SR-IOV

Use SR-IOV when your applications require:

* High bandwidth networking
* Low latency communication
* Direct hardware access for performance-critical workloads
* Data Plane Development Kit (DPDK) acceleration
* Applications that benefit from bypassing the kernel network stack

[NOTE]
====
As of {product-title} 4.21, the SR-IOV Operator can support ARM hardware.
====

[id="how-sriov-works_{context}"]
== How SR-IOV works

SR-IOV segments a compliant network device, recognized on the host node as a physical function (PF), into multiple virtual functions (VFs). The VF is used like any other network device. The SR-IOV network device driver for the device determines how the VF is exposed in the container:

* `netdevice` driver: A regular kernel network device in the `netns` of the container
* `vfio-pci` driver: A character device mounted in the container

You can configure SR-IOV devices in your cluster by using the xref:../../networking/networking_operators/sr-iov-operator/installing-sriov-operator.adoc#installing-sriov-operator[SR-IOV Network Operator].

[id="supported-platforms-devices_{context}"]
== Supported platforms and devices

The SR-IOV Network Operator is supported on the following platforms:

- Bare metal
- {rh-openstack-first}

You can use SR-IOV network devices with additional networks on your {product-title} cluster for applications that require high bandwidth or low latency.

[NOTE]
====
For a list of devices, such as network interface controllers (NICs) that {product-title} supports, see link:https://catalog.redhat.com/en/hardware[Red Hat certified hardware] on the Red Hat Ecosystem Catalog. The following example, finds the Intel X710 network adapter:

. From the Red Hat certified hardware webpage, click *Explore* from the *Components* tile. 
. From the *Provider* drop-down menu, click the *Intel Corporation* checkbox.
. From the *Platform* drop-down menu, click the *Red Hat OpenShift Container Platform* checkbox.
. Find the *Intel® Ethernet Server Adapter X710* network adapter from the list and then click on its tile. A new webpage opens that shows information for the network adapter. 
====

You can configure multi-network policies for SR-IOV networks. The support for this is technology preview and SR-IOV additional networks are only supported with kernel NICs. They are not supported for Data Plane Development Kit (DPDK) applications.

[NOTE]
====
Creating multi-network policies on SR-IOV networks might not deliver the same performance to applications compared to SR-IOV networks without a multi-network policy configured.
====

:FeatureName: Multi-network policies for SR-IOV network
include::snippets/technology-preview.adoc[leveloffset=+2]

You can enable SR-IOV on a node by using the following command:
[source,terminal]
----
$ oc label node <node_name> feature.node.kubernetes.io/network-sriov.capable="true"
----


[role="_additional-resources"]
[id="additional-resources-about-sr-iov_{context}"]
== Additional resources

* xref:../../networking/networking_operators/sr-iov-operator/installing-sriov-operator.adoc#installing-sriov-operator[Installing the SR-IOV Network Operator]

[id="components-sr-iov-network-devices"]
== Components that manage SR-IOV network devices

The SR-IOV Network Operator creates and manages the components of the SR-IOV stack. The Operator performs the following functions:

- Orchestrates discovery and management of SR-IOV network devices
- Generates `NetworkAttachmentDefinition` custom resources for the SR-IOV Container Network Interface (CNI)
- Creates and updates the configuration of the SR-IOV network device plugin
- Creates node specific `SriovNetworkNodeState` custom resources
- Updates the `spec.interfaces` field in each `SriovNetworkNodeState` custom resource

The Operator provisions the following components:

SR-IOV network configuration daemon::
A daemon set that is deployed on worker nodes when the SR-IOV Network Operator starts. The daemon is responsible for discovering and initializing SR-IOV network devices in the cluster.

SR-IOV Network Operator webhook::
A dynamic admission controller webhook that validates the Operator custom resource and sets appropriate default values for unset fields.

SR-IOV Network resources injector::
A dynamic admission controller webhook that provides functionality for patching Kubernetes pod specifications with requests and limits for custom network resources such as SR-IOV VFs. The SR-IOV network resources injector adds the `resource` field to only the first container in a pod automatically.

SR-IOV network device plugin::
A device plugin that discovers, advertises, and allocates SR-IOV network virtual function (VF) resources. Device plugins are used in Kubernetes to enable the use of limited resources, typically in physical devices. Device plugins give the Kubernetes scheduler awareness of resource availability, so that the scheduler can schedule pods on nodes with sufficient resources.

SR-IOV CNI plugin::
A CNI plugin that attaches VF interfaces allocated from the SR-IOV network device plugin directly into a pod.

SR-IOV InfiniBand CNI plugin::
A CNI plugin that attaches InfiniBand (IB) VF interfaces allocated from the SR-IOV network device plugin directly into a pod.

[NOTE]
====
The SR-IOV Network resources injector and SR-IOV Network Operator webhook are enabled by default and can be disabled by editing the `default` `SriovOperatorConfig` CR.
Use caution when disabling the SR-IOV Network Operator Admission Controller webhook. You can disable the webhook under specific circumstances, such as troubleshooting, or if you want to use unsupported devices.
====

[role="_additional-resources"]
[id="configure-multi-networks-additional-resources"]
== Additional resources

* xref:../../networking/multiple_networks/secondary_networks/configuring-multi-network-policy.adoc#configuring-multi-network-policy[Configuring multi-network policy]

[id="about-sriov-next-steps"]
== Next steps

*If you are new to SR-IOV*

* xref:../../networking/hardware_networks/sriov-getting-started.adoc#sriov-getting-started[Get started with SR-IOV secondary networks] - Complete walkthrough for beginners

*Learn key concepts*

* xref:../../networking/hardware_networks/sriov-concept-architecture.adoc#sriov-concept-architecture[Understand SR-IOV architecture and components]
* xref:../../networking/hardware_networks/sriov-concept-ipam.adoc#sriov-concept-ipam[Understand IP address management (DHCP versus Whereabouts versus static IPs)]
* xref:../../networking/hardware_networks/sriov-concept-performance.adoc#sriov-concept-performance[Understand SR-IOV performance optimization]
* xref:../../networking/hardware_networks/sriov-concept-high-availability.adoc#sriov-concept-high-availability[Understand SR-IOV high availability]

*Configure basic SR-IOV networking*

* xref:../../networking/networking_operators/sr-iov-operator/configuring-sriov-operator.adoc#configuring-sriov-operator[Configuring the SR-IOV Network Operator]
* xref:../../networking/hardware_networks/sriov-configure-device.adoc#configuring-sriov-device[Configure SR-IOV network devices]
* xref:../../networking/hardware_networks/sriov-attach-ethernet.adoc#configuring-sriov-net-attach[Attach workloads to SR-IOV Ethernet networks]
* xref:../../networking/hardware_networks/sriov-attach-infiniband.adoc#configuring-sriov-ib-attach[Attach workloads to SR-IOV InfiniBand networks]
* xref:../../networking/hardware_networks/configuring-sriov-ipam.adoc#configuring-sriov-ipam[Configure IP address management]

*Optimize for performance*

* xref:../../networking/hardware_networks/sriov-performance-dpdk-rdma.adoc#using-dpdk-and-rdma[Accelerate packet processing with DPDK and RDMA]
* xref:../../networking/hardware_networks/sriov-performance-hardware-offload.adoc#configuring-hardware-offloading[Offload packet processing to NIC hardware]
* xref:../../networking/hardware_networks/sriov-performance-tune-interface.adoc#configuring-interface-level-sysctl-settings-sriov-device[Tune SR-IOV network interface parameters]

*Ensure high availability*

* xref:../../networking/hardware_networks/sriov-ha-bonding.adoc#using-pod-level-bonding[Combine SR-IOV interfaces with bonding]
* xref:../../networking/hardware_networks/sriov-ha-lacp.adoc#sriov-lacp-sriov[Ensure high availability with LACP monitoring]

*Advanced and specialized configurations*

* xref:../../networking/hardware_networks/sriov-performance-numa-scheduling.adoc#sriov-performance-numa-scheduling[Optimize SR-IOV performance with NUMA-aware scheduling]
* xref:../../networking/hardware_networks/sriov-advanced-parallel-draining.adoc#sriov-advanced-parallel-draining[Speed up SR-IOV configuration changes with parallel node draining]
* xref:../../networking/hardware_networks/sriov-specialized-qinq.adoc#configuring-qinq-support[Stack VLANs with QinQ]
* xref:../../networking/hardware_networks/sriov-specialized-multicast.adoc#using-sriov-multicast[Enable high-performance multicast]
* xref:../../networking/hardware_networks/sriov-specialized-rdma-cni.adoc#configuring-sriov-rdma-cni[Enable RDMA for low-latency communication]
* xref:../../networking/hardware_networks/sriov-specialized-namespaced-resources.adoc#configuring-namespaced-sriov-resources[Delegate SR-IOV management to application teams]

*Hardware-specific*

* xref:../../networking/hardware_networks/sriov-hardware-bluefield2.adoc#switching-bf2-nic-dpu[Switch Bluefield-2 between NIC and DPU modes]

*Virtual machines*

* If you use {VirtProductName}: xref:../../virt/vm_networking/virt-connecting-vm-to-sriov.adoc#virt-connecting-vm-to-sriov[Connect virtual machines to SR-IOV networks]

