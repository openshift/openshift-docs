:_mod-docs-content-type: ASSEMBLY
[id="ingress-controller-dnsmgt"]
= Understanding DNS management policies
include::_attributes/common-attributes.adoc[]
:context: ingress-controller-dnsmgt

toc::[]

When you configure an Ingress Controller, you use the dnsManagementPolicy parameter to specify whether the Operator should manage the DNS records for you automatically or if you will manage them manually.

== Managed DNS management policy

The Managed DNS management policy for Ingress Controllers ensures that the lifecycle of the wildcard DNS record on the cloud provider is automatically managed by the Operator. This is the default behavior.

== Unmanaged DNS management policy

The Unmanaged DNS management policy for Ingress Controllers ensures that the lifecycle of the wildcard DNS record on the cloud provider is not automatically managed; instead, it becomes the responsibility of the cluster administrator.

include::modules/creating-a-custom-ingress-controller.adoc[leveloffset=+1]

include::modules/modifying-an-existing-ingress-controller.adoc[leveloffset=+1]

[role="_additional-resources"]
[id="configuring-ingress-controller-dns-management-additional-resources"]
== Additional resources
* xref:../../../networking/networking_operators/ingress-operator.adoc#nw-ingress-controller-configuration-parameters_configuring-ingress[Ingress Controller configuration parameters]
