:_mod-docs-content-type: ASSEMBLY
[id="routing-tls-requests-to-services"]
= Route TLS requests to services
include::_attributes/common-attributes.adoc[]
:context: routing-tls-requests-to-services

toc::[]

[role="_abstract"]
To route encrypted TLS traffic to a backend that terminates TLS, configure a Gateway API `TLSRoute` custom resource (CR). A `TLSRoute` CR matches the Server Name Indication (SNI) hostname from the TLS handshake and forwards the still-encrypted connection. `TLSRoute` is the only Gateway API route kind that supports TLS passthrough.

Do not attach a `TLSRoute` CR to a listener that uses `tls.mode: Terminate`. That combination is an extended Gateway API feature and is not part of the {product-title} standard-channel implementation. To terminate TLS at the gateway for HTTP applications, use an HTTPS listener with an `HTTPRoute` CR.

To route TLS passthrough traffic, complete the following tasks:

* Configure a TLS passthrough listener on your gateway.
* Create a `TLSRoute` CR that matches SNI hostnames and forwards traffic to a backend service.

include::modules/configuring-tls-passthrough-listener.adoc[leveloffset=+1]

include::modules/gateway-listener-configuration-reference.adoc[leveloffset=+2]

include::modules/creating-tlsroute.adoc[leveloffset=+1]

include::modules/tlsroute-configuration-reference.adoc[leveloffset=+2]

[role="_additional-resources"]
[id="additional-resources_{context}"]
== Additional resources

* xref:controlling-incoming-traffic-gateway-listeners.adoc#controlling-incoming-traffic-gateway-listeners[Control incoming traffic with gateway listeners]
* xref:securing-httproutes.adoc#securing-httproutes[Securing HTTPRoutes]
* xref:routing-http-requests-to-services.adoc#routing-http-requests-to-services[Routing HTTP requests to services]
* xref:../routes/securing-routes.adoc#securing-routes[Securing routes]
