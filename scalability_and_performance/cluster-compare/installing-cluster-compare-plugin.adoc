:_mod-docs-content-type: ASSEMBLY
[id="installing-cluster-compare-plugin"]
= Installing the cluster-compare plugin
include::_attributes/common-attributes.adoc[]
:context: installing-cluster-compare-plugin

toc::[]

You can extract the `cluster-compare` plugin from a container image in the Red Hat container catalog and use it as an `oc` command plugin.

include::modules/installing-cluster-compare.adoc[leveloffset=+1]
