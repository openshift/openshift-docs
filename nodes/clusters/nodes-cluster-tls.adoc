:context: nodes-cluster-tls
[id="nodes-cluster-tls"]
= Enabling cluster-wide TLS profiles
include::modules/common-attributes.adoc[]

toc::[]

The `tlsSecurityProfile` parameter defines the schema for a TLS security profile. This object is used by {product-title} to enable TLS secure communication between components to provide cluster-wide cryptographic policy.

include::modules/cluster-tls-profiles.adoc[leveloffset=+1]
