:_mod-docs-content-type: CONCEPT
[id="ols-large-language-model-requirements-rhelai"]

= {rhelai} requirements

[role="_abstract"]
{ols-long} supports {rhelai} as a self-hosted large language model (LLM) provider.

The {ols-long} Service does not provide the LLM for you, so you must configure the LLM before installing the {ols-long} Operator.

[NOTE]
====
Red{nbsp}Hat does not provide support for any specific models or make suggestions or support statements pertaining to models.
====

{rhelai} is OpenAI API-compatible, and you configure {rhelai} in a similar manner as the OpenAI provider.

You can configure {rhelai} as the LLM provider.

Because the {rhel} is in a different environment than the {ols-long} deployment, the model deployment must allow access by using a secure connection.

{ols-long} version 1.0 and later supports vLLM Server version 0.8.4 and later. When self-hosting the LLM with {rhelai}, you can use vLLM Server as the inference engine.

.Additional resources

* link:https://docs.redhat.com/en/documentation/red_hat_enterprise_linux_ai/1.2/html-single/building_your_rhel_ai_environment/index#creating_secure_endpoint[Optional: Allowing access to a model from a secure endpoint]
