:_mod-docs-content-type: CONCEPT
[id="ols-support-for-trusted-ca-certificates-and-llm-providers-google-vertex-ai"]

= Configure support for trusted-ca certificates with Google Vertex AI

[role="_abstract"]
Configure custom TLS certificates to secure communication between {ols-long} and Google Vertex AI service endpoints.

The {ols-long} Service supports custom TLS certificates for secure communication with Google Vertex AI.

.Procedure

. Create a `ConfigMap` object that contains the certificates.
. Add the name of the object in the `OLSConfig` custom resource (CR) file:
+
[source,yaml]
----
ols:
  additionalCAConfigMapRef:
    name: <config_map_name>
----
+
where
+
`<config_map_name>`:: Enter the name of your `ConfigMap` object.
