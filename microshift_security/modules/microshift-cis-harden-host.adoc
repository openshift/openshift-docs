// Module included in the following assemblies:
//
// * microshift_install_get_ready/microshift-cis-level-2.adoc

:_mod-docs-content-type: PROCEDURE
[id="microshift-cis-harden-host_{context}"]
= Hardening {op-system-base} hosts and verifying CIS Level 2 compliance

[role="_abstract"]
Harden a {op-system-base} host with the Red Hat Official CIS Ansible roles, apply the overrides that {microshift-short} requires for IP forwarding, and verify compliance with OpenSCAP.

.Prerequisites

* You have a {op-system-base} {op-system-version-major} or {op-system-base} {op-system-version-major-10} host on `x86_64` or `aarch64`.
* You have root or `sudo` privileges on the host.
* You can install Ansible and the Red Hat Official CIS role for your {op-system-base} major version.
* You can install the `openscap-scanner` and `scap-security-guide` packages.

.Procedure

. Install Ansible and obtain the CIS hardening role for your {op-system-base} major version.
+
* For {op-system-base} {op-system-version-major}, use the `ansible-role-rhel9-cis` role from the Red Hat Official GitHub organization.
* For {op-system-base} {op-system-version-major-10}, use the `ansible-role-rhel10-cis` role from the Red Hat Official GitHub organization.

. Create a playbook that applies CIS Level 2 hardening and overrides the IP forwarding variables so that the role does not disable forwarding before {microshift-short} starts.
+
.Example playbook for {op-system-base} {op-system-version-major}
[source,yaml]
----
---
- name: Apply CIS Level 2 hardening
  hosts: localhost
  connection: local
  become: true
  roles:
    - role: ansible-role-rhel9-cis
      vars:
        sysctl_net_ipv4_ip_forward: false
        sysctl_net_ipv6_conf_all_forwarding: false
----
+
.Example playbook for {op-system-base} {op-system-version-major-10}
[source,yaml]
----
---
- name: Apply CIS Level 2 hardening
  hosts: localhost
  connection: local
  become: true
  roles:
    - role: ansible-role-rhel10-cis
      vars:
        sysctl_net_ipv4_ip_forward: false
        sysctl_net_ipv4_conf_all_forwarding: false
        sysctl_net_ipv4_conf_default_forwarding: false
        sysctl_net_ipv6_conf_all_forwarding: false
        sysctl_net_ipv6_conf_default_forwarding: false
----
+
[NOTE]
====
Setting each variable to `false` tells the role not to disable forwarding.
The setting does not enable forwarding.
{microshift-short} enables IP forwarding at startup.
====

. Run the playbook against the host.

. Install OpenSCAP scanner packages:
+
[source,terminal]
----
$ sudo dnf install -y openscap-scanner scap-security-guide
----

. Evaluate the host against the CIS profile.
+
.Example command for {op-system-base} {op-system-version-major}
[source,terminal]
----
$ sudo oscap xccdf eval \
  --profile xccdf_org.ssgproject.content_profile_cis \
  --results /tmp/cis-results.xml \
  --report /tmp/cis-report.html \
  /usr/share/xml/scap/ssg/content/ssg-rhel9-ds.xml
----
+
.Example command for {op-system-base} {op-system-version-major-10}
[source,terminal]
----
$ sudo oscap xccdf eval \
  --profile xccdf_org.ssgproject.content_profile_cis \
  --results /tmp/cis-results.xml \
  --report /tmp/cis-report.html \
  /usr/share/xml/scap/ssg/content/ssg-rhel10-ds.xml
----

. Review `/tmp/cis-report.html` and remediate unexpected failures before you install {microshift-short}.

.Verification

* The OpenSCAP report uses the `xccdf_org.ssgproject.content_profile_cis` profile.
* Remaining failures match only rules that you expect to address after {microshift-short} installation, or that are documented as unavoidable for container platforms.
