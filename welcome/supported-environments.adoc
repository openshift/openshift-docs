:_mod-docs-content-type: ASSEMBLY
include::_attributes/common-attributes.adoc[]
[id="supported-environments"]
= Supported environments
:context: supported-environments

toc::[]

Currently {product-title} is supported on the following architectures:

* ARM
* AMD64, or 64-bit x86
* PowerPC
* S390z

{product-title} is supported on the following platforms:

* {platform}
* {hcp-capital} for {platform}

[IMPORTANT]
====
Currently, {product-title} is not supported on {ms}.
====
