:_mod-docs-content-type: ASSEMBLY
[id="configure-nonactive-cluster-removal"]
= Configuring automatic removal of nonactive clusters from RHACS
include::modules/common-attributes.adoc[]
:context: configure-nonactive-cluster-removal

toc::[]

[role="_abstract"]
{product-title} ({product-title-short}) provides the option to configure your system to automatically remove nonactive clusters from {product-title-short} so that you can monitor active clusters only. Note that only clusters that installed and performed a handshake with Central at least one time receive initial monitoring. If you enable this feature, when Central has been unable to reach Sensor in a cluster for the period of time configured in the *Decommissioned cluster age* field, the cluster becomes nonactive in {product-title-short}. Central will then no longer monitor nonactive clusters. You can configure the *Decommissioned cluster age* field in the *Platform Configuration* -> *System Configuration* page. When configuring this feature, you can add a label for the cluster so that {product-title-short} continues to monitor the cluster even if it becomes nonactive.

//Overview of automatic cluster removal
include::modules/configure-inactive-cluster-deletion-overview.adoc[leveloffset=+1]

//Configuring cluster decommissioning
include::modules/configure-cluster-decommissioning.adoc[leveloffset=+1]

//Viewing nonactive clusters
include::modules/view-nonactive-clusters.adoc[leveloffset=+1]