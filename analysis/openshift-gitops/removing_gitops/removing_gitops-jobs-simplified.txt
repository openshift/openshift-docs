## MAIN JOBS

uninstalling-openshift-gitops.adoc
Completely remove GitOps from my cluster
When I need to completely remove GitOps from my cluster, I want to uninstall the Operator and all Argo CD instances, so I can ensure clean removal without orphaned resources

---

## USER STORIES

go-deleting-argocd-instance.adoc
Delete all Argo CD instances
When preparing to uninstall GitOps, I want to delete all Argo CD instances using the CLI, so I can ensure no orphaned instances remain before removing the Operator

go-uninstalling-gitops-operator.adoc
Uninstall the GitOps Operator
When all Argo CD instances are removed, I want to uninstall the GitOps Operator via the web console, so I can complete the removal process
