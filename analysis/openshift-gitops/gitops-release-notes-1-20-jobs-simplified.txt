## MAIN JOBS

gitops-release-notes-1-20.adoc
Stay informed about GitOps releases
When a new version of OpenShift GitOps is released, I want to understand what features, fixes, and changes are included, so I can evaluate whether to upgrade and prepare for any breaking changes

gitops-release-notes-1-20.adoc
Understand GitOps capabilities
When considering GitOps adoption or current usage, I want to understand what tasks GitOps automates, so I can ensure consistent application deployments across multiple clusters and environments

gitops-release-notes-1-20.adoc
Plan version upgrade
When evaluating an upgrade to OpenShift GitOps 1.20.x, I want to review compatibility requirements and platform support, so I can ensure the new version works with my OpenShift cluster version

---

## USER STORIES - VERSION 1.20.1

gitops-release-notes-1-20-1.adoc
Review 1.20.1 errata updates
When OpenShift GitOps 1.20.1 is released, I want to access the enhancement advisory, so I can review the complete list of enhancements and verify container image versions

gitops-release-notes-1-20-1.adoc
Understand AppProject deletion fix
When using autonomous agents with AppProjects, I want to know that AppProjects are preserved during resync operations, so unintended deletions no longer occur when principal restarts trigger resync

---

## USER STORIES - VERSION 1.20.0

gitops-release-notes-1-20-0.adoc
Review 1.20.0 errata updates
When OpenShift GitOps 1.20.0 is released, I want to access the enhancement advisory, so I can review the complete list of enhancements and verify container image versions

gitops-release-notes-1-20-0.adoc
Adapt to Argo CD authentication changes
When external authentication is enabled at cluster level in OCP 4.20+, I want to configure external identity providers using .spec.oidc field in ArgoCD CR, so Argo CD login works correctly without OpenShift OAuth

gitops-release-notes-1-20-0.adoc
Use wildcard patterns for ApplicationSet namespaces
When managing ApplicationSets across dynamic groups of namespaces, I want to use wildcard characters in .spec.applicationSet.sourceNamespaces, so I can enable ApplicationSets for multiple namespaces with a single pattern

gitops-release-notes-1-20-0.adoc
Deploy Argo CD agents through Operator
When implementing agent-based deployments, I want to configure managed and autonomous agents directly through Argo CD CR, so agent installation is simplified without manual configuration

gitops-release-notes-1-20-0.adoc
Map applications using destination field
When running multitenant environments where teams use separate namespaces, I want to route applications to managed agents using .spec.destination.name field, so applications target the correct agent while keeping their original namespace

gitops-release-notes-1-20-0.adoc
Manage image updates with custom resource (Tech Preview)
When using ApplicationSets that generate multiple applications, I want to use ImageUpdater CR that reads existing annotations, so I can manage image updates across multiple applications without migrating configurations

gitops-release-notes-1-20-0.adoc
Automate ECR image updates with CloudEvents (Tech Preview)
When using AWS ECR registries, I want CloudEvents webhook handler to process ECR push events through AWS EventBridge, so image updates are triggered automatically when new images are pushed

gitops-release-notes-1-20-0.adoc
Stream pod logs from workload clusters
When troubleshooting applications in agent architecture, I want to view pod logs from workload clusters in the control plane, so I can troubleshoot centrally without direct access to workload clusters

gitops-release-notes-1-20-0.adoc
Enable distributed tracing with OpenTelemetry
When monitoring Argo CD agent and principal components, I want OpenTelemetry integration for distributed tracing, so I can gain visibility into system behavior and performance

gitops-release-notes-1-20-0.adoc
Secure components with network policies
When deploying Argo CD workload pods, I want Kubernetes Network Policy resources to prevent unnecessary traffic, so Argo CD components follow least-privilege network security posture

gitops-release-notes-1-20-0.adoc
Configure trust anchors through ArgoCD CR
When repo-server plugin sidecars need to trust TLS hosts, I want to configure system CA trust through Argo CD CR using Kubernetes trust anchors, so certificate management is simplified without manual config map edits

gitops-release-notes-1-20-0.adoc
Delete applications in specific order
When removing multiple applications with dependencies, I want progressive sync to support ordered deletion, so I can avoid dependency issues and service disruptions

gitops-release-notes-1-20-0.adoc
Prevent orphaned resources in namespaces
When removing namespaces from sourceNamespaces configuration, I want automatic cleanup of orphaned roles and role bindings, so resource management remains consistent

gitops-release-notes-1-20-0.adoc
Access Argo Rollouts CLI
When implementing progressive delivery workflows, I want to use Argo Rollouts kubectl plugin binaries, so I can directly access Argo Rollouts CLI for multiple platforms

gitops-release-notes-1-20-0.adoc
Understand base image migration to UBI 9
When upgrading to GitOps 1.20.0, I want to know that operator images now use UBI 9 Minimal with rhel9 suffix, so I can update external automation or scripts that reference these images

gitops-release-notes-1-20-0.adoc
Benefit from reduced image sizes
When pulling GitOps component images, I want optimized images with unused packages removed, so image pulls are faster and security posture is improved

gitops-release-notes-1-20-0.adoc
View Applications page without rendering errors
When accessing GitOps console plugin Applications page, I want it to render correctly even with invalid Applications, so JavaScript errors don't prevent page display

gitops-release-notes-1-20-0.adoc
Verify correct ownerReferences after upgrade
When upgrading Argo CD CR from v1alpha1 to v1beta1, I want ownerReferences in config maps to use correct apiVersion, so UI displays Argo CD CR consistently

gitops-release-notes-1-20-0.adoc
View Progressive Sync status correctly
When using ApplicationSets with Progressive Sync, I want accurate status display in UI, so I can monitor sync state properly instead of seeing "Unknown"

gitops-release-notes-1-20-0.adoc
Navigate to applications across Argo CD instances
When cluster-scoped instance deploys namespaced instances and their applications, I want correct application links in cluster-scoped UI, so navigation works across different Argo CD instances

gitops-release-notes-1-20-0.adoc
Enforce namespace boundaries in Image Updater (Tech Preview)
When running multitenant Image Updater setups, I want namespace boundaries enforced and AppProject restrictions respected, so tenant isolation is maintained

gitops-release-notes-1-20-0.adoc
Prevent Git process exhaustion in Image Updater (Tech Preview)
When Image Updater retrieves Git access tokens, I want proper Git process management, so process exhaustion and RPC failures are prevented
