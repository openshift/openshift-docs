:_content-type: ASSEMBLY
[id="cli-manager-install_{context}"]
= Installing the {cli-manager}
include::_attributes/common-attributes.adoc[]
:context: cli-manager-install

toc::[]

//Brief explanation of what the cli manager is.
The {cli-manager} can be used in both connected and disconnected environments, and particularly makes installing and managing CLI plugins in disconnected environments easier. It is makes Krew compatible with the `oc` CLI and cluster administrators add CLI plugin custom resources which can then be accessed in both connected and disconnected environments.

// About the CLI Manager
include::modules/cli-manager-about.adoc[leveloffset=+1]

// Installing the {cli-manager}
include::modules/cli-manager-installing.adoc[leveloffset=+1]

// Adding CLI Manager custom index to krew
include::modules/cli-manager-custom-index-krew.adoc[leveloffset=+1]

// Adding CLI plugins with YAML files
include::cli-manager-adding-plugin-yaml.adoc[leveloffset=+1]