// included in egress gateway documentation
//

:_mod-docs-content-type: SNIPPET

[IMPORTANT]
====
The egress `Gateway` object example use the `networking.istio.io/service-type: ClusterIP` annotation because egress traffic originates inside the cluster. If workloads in other clusters need to reach the egress `Gateway` object, use a `LoadBalancer` annotation instead.
====
