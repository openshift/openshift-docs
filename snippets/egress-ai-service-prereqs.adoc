// included in egress gateway documentation
//

:_mod-docs-content-type: SNIPPET

* You registered an external artificial intelligence (AI) service with the {service-mesh} service registry and created the `ServiceEntry`, `DestinationRule`, and `HTTPRoute` objects that route to it. See "Deploy an egress gateway".
* Your external AI service returns an OpenAI-compatible JSON response body that has a `usage.total_tokens` field.
* You created a test client pod in the `egress-test` namespace.
