## MAIN JOBS

configuring-argo-cd-rbac.adoc
Configure role-based access control policies
When managing access to Argo CD, I want to configure role-based access control policies, so I can ensure users have appropriate permissions for their responsibilities

configuring-sso-on-argo-cd-using-dex.adoc
Configure single sign-on for Argo CD
When managing multiple users, I want to configure single sign-on for Argo CD, so I can centralize authentication and reduce password management overhead

managing-local-users-in-argo-cd.adoc
Manage local user accounts in Argo CD
When I need API tokens for automation or have small teams without SSO, I want to manage local user accounts in Argo CD, so I can provide programmatic access without configuring external identity providers

---

## USER STORIES

configuring-argo-cd-rbac.adoc
Configure user level access
When I need to grant specific permissions to users, I want to edit the RBAC policy in the Argo CD CR, so I can assign appropriate roles without granting cluster-admin access

configuring-sso-on-argo-cd-using-dex.adoc
Enable Dex OpenShift OAuth Connector
When I want to leverage OpenShift users and groups for Argo CD authentication, I want to enable Dex with OpenShift OAuth, so I can avoid maintaining separate user directories

configuring-sso-on-argo-cd-using-dex.adoc
Map users to specific roles
When users have ClusterRoleBinding roles, I want to map them to Argo CD roles through OpenShift groups, so I can grant admin access to authorized users

configuring-sso-for-argo-cd-using-oidc.adoc
Configure SSO using external OIDC providers
When I need to integrate with an external identity provider, I want to configure direct OIDC integration for Argo CD, so I can authenticate users without using Dex as an intermediary

configuring-sso-for-argo-cd-using-oidc.adoc
Verify OIDC login
When I have configured OIDC integration, I want to verify that authentication is working correctly, so I can ensure users can access Argo CD with their identity provider credentials

managing-local-users-in-argo-cd.adoc
Create local users
When I need to provide API access for service accounts or automation scripts, I want to create local users in Argo CD, so I can generate API tokens without configuring SSO

managing-local-users-in-argo-cd.adoc
Configure token lifecycle management
When managing API tokens for local users, I want to configure automatic renewal and rotation policies, so I can balance security with operational continuity

managing-local-users-in-argo-cd.adoc
Manage user lifecycle
When user access needs change, I want to disable or remove local users without losing their configuration, so I can temporarily revoke access or permanently clean up accounts

managing-local-users-in-argo-cd.adoc
Retrieve tokens from Kubernetes secrets
When I need to access Argo CD APIs, I want to retrieve local user tokens from Kubernetes secrets, so I can authenticate service accounts and automation scripts
