## MAIN JOBS

setting-up-argocd-instance.adoc
Set up Argo CD instances
When managing GitOps deployment infrastructure, I want to establish continuous deployment capabilities using GitOps, so I can manage cluster configurations and deploy applications

setting-up-argocd-instance.adoc
Configure Argo CD permissions and security
When operating in multi-tenant cluster environments, I want to reduce security risks and prevent privilege escalation, so I can safely delegate Argo CD instances to development teams

setting-up-argocd-instance.adoc
Scale Argo CD components for high availability
When running production deployments requiring high availability, I want to distribute workloads and ensure service availability, so Argo CD services remain operational

setting-up-argocd-instance.adoc
Configure Argo CD namespace management
When managing resources across multiple namespaces, I want to allow Argo CD to manage resources outside its installation namespace, so I can deploy applications to any namespace

setting-up-argocd-instance.adoc
Configure image pull policies
When managing container image updates and caching, I want to control how and when container images are pulled, so I can optimize image distribution across instances

argo-cd-cr-component-properties.adoc
Understand Argo CD custom resource structure
When configuring Argo CD instances, I want to understand available configuration options, so I can properly configure all Argo CD components

argo-cd-cr-component-properties.adoc
Configure Argo CD repo server
When customizing repo server behavior and capabilities, I want to optimize manifest generation and repository interactions, so I can handle complex repository workflows

argo-cd-cr-component-properties.adoc
Set up notifications for Argo CD events
When tracking application sync status and failures, I want to receive alerts when important events occur, so I can respond to deployment issues quickly

argo-cd-cr-component-properties.adoc
Configure multi-instance Argo CD deployments
When operating multiple Argo CD instances managing resources, I want to prevent conflicts and enable parallel operation of multiple instances, so each instance can safely manage its own resources

---

## USER STORIES

setting-up-argocd-instance.adoc
Install a user-defined Argo CD instance
When I need to deploy applications or manage cluster configurations, I want to manage cluster configurations or deploy applications in isolated namespaces, so I can separate workloads by team or environment

setting-up-argocd-instance.adoc
Disable default Argo CD instance
When I need to prevent automatic default instance creation, I want to control which Argo CD instances run in the cluster, so I can use custom instance configurations in openshift-gitops namespace

setting-up-argocd-instance.adoc
Configure custom cluster roles for namespace-scoped instances
When delegating Argo CD instances to development teams, I want to limit permissions and prevent privilege escalation, so teams cannot gain admin privileges

setting-up-argocd-instance.adoc
Enable replicas for Argo CD server and repo server
When I need better workload distribution, I want to improve availability and distribute load, so Argo CD services can handle increased traffic

setting-up-argocd-instance.adoc
Label namespaces for Argo CD management
When granting Argo CD access to additional namespaces, I want to extend Argo CD management scope beyond its installation namespace, so applications can deploy to multiple namespaces

setting-up-argocd-instance.adoc
Customize Argo CD console link in multitenant clusters
When operating multiple Argo CD instances in cluster, I want to prevent confusion when multiple Argo CD instances exist, so console links point to the correct instance

setting-up-argocd-instance.adoc
Set global image pull policy
When I need consistent image pull behavior across all Argo CD instances, I want to enforce consistent image update behavior, so all instances follow the same policy

argo-cd-cr-component-properties.adoc
Configure ApplicationSet controller
When managing multiple applications with similar patterns, I want to automate multi-application management, so I can generate applications from templates

argo-cd-cr-component-properties.adoc
Enable controller sharding for large-scale deployments
When managing large number of clusters, I want to manage large numbers of clusters efficiently, so memory pressure on the controller is reduced

argo-cd-cr-component-properties.adoc
Configure high availability with Redis
When running production deployments requiring redundancy, I want to ensure service availability and fault tolerance, so Argo CD remains available during failures

argo-cd-cr-component-properties.adoc
Configure RBAC policies
When controlling user access and permissions, I want to control access and enforce authorization policies, so users have appropriate permissions

argo-cd-cr-component-properties.adoc
Configure TLS and certificate management
When securing connections to Git repositories and external services, I want to secure communications and establish trust, so connections are encrypted and verified

argo-cd-cr-component-properties.adoc
Configure TLS trust for repo server
When accessing repositories with custom CA certificates, I want to enable repo server to trust custom certificate authorities, so plugin workflows can access TLS-enabled resources

argo-cd-cr-component-properties.adoc
Configure execution timeout for rendering tools
When using Helm or Kustomize with large manifests, I want to prevent timeouts when rendering complex manifests, so rendering tools have sufficient time to complete

argo-cd-cr-component-properties.adoc
Enable notifications for Argo CD instance
When I need to receive alerts about Argo CD events, I want to receive alerts when events occur in Argo CD, so I can respond to sync failures and other issues

argo-cd-cr-component-properties.adoc
Configure notification templates and triggers
When customizing notification messages and conditions, I want to customize when and how notifications are sent, so notifications contain relevant information

argo-cd-cr-component-properties.adoc
Configure notifications in delegated namespaces
When operating in multi-tenant environment where teams manage their own notifications, I want to enable teams to manage their own notification settings, so teams control their own alerts

argo-cd-cr-component-properties.adoc
Enable Config Management Plugins
When I need to use tools beyond Helm, Jsonnet, and Kustomize, I want to use custom config management tools or enable additional features, so I can use any manifest generation tool

argo-cd-cr-component-properties.adoc
Configure annotation-based tracking in multiple instances
When operating multiple Argo CD instances with potentially identical application names, I want to prevent conflicts and enable safe parallel operation, so instances can coexist without conflicts
