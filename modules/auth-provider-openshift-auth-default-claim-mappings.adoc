// Module included in the following assemblies:
//
// * operating/manage-user-access/understanding-authentication-providers.adoc

:_mod-docs-content-type: REFERENCE
[id="auth-provider-openshift-auth-default-claim-mappings_{context}"]
= OpenShift Auth default claim mappings

[role="_abstract"]
The following list provides the OpenShift Auth default claim mappings.

* `groups` to `groups`
* `uid` to `userid`
* `name` to `name`
