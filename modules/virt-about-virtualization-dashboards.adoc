// Module included in the following assemblies:
//
// * virt/monitoring/virt-prometheus-queries.adoc

:_mod-docs-content-type: CONCEPT
[id="virt-about-virtualization-dashboards_{context}"]
= Virtualization dashboards overview

[role="_abstract"]
{VirtProductName} includes Perses-based dashboards that give you visibility into virtual machine (VM) inventory, health, resource usage, service levels, and capacity across your cluster.

The dashboards are deployed automatically as `PersesDashboard` custom resources by the HyperConverged Cluster Operator (HCO) in the `{CNVNamespace}` namespace.

To access these dashboards, click *Observe* -> *Dashboards* in the web console.

The dashboards require the Cluster Observability Operator with the monitoring stack enabled.

////
Writer note: documenting a new virtualization dashboard (CNV-44340 pattern)

Document dashboards at the JOB altitude, not the panel altitude. Individual
panels and screenshots drift every release; the job a dashboard serves does not.

To add a dashboard:
1. Add ONE row to the table below: what the dashboard shows, plus when to use
   it (the job it serves).
2. Do NOT enumerate individual panels and do NOT add screenshots.
3. Panel formats are described generically in virt-dashboard-panel-types.adoc,
   so you do not need to change that module.

Add a dedicated interpretation CONCEPT (like
virt-nodes-memory-dashboard-concepts.adoc) ONLY when acting on the dashboard
requires a non-obvious mental model, for example overcommit ratios or pressure
stall information (PSI). A ranking or a table of self-evident metrics does not
need one. If you add such a module, wire it into
virt/monitoring/virt-prometheus-queries.adoc.
////

[id="available-dashboards_{context}"]
== Available dashboards

The following table describes the available {VirtProductName} dashboards:

[cols="1,2a",options="header"]
|===
| Dashboard | Description

| *Virtual Machines Inventory*
| Lists all VMs in the cluster with status, instance type, guest operating system, resource allocation, and operational flags such as live-migratable and evictable.

Use this dashboard to manage your VM fleet and find VMs that match specific criteria.

| *Virtual Machines Utilization*
| Shows resource usage for each VM, including CPU, memory, swap, storage, network, and filesystem metrics.

Use this dashboard to monitor resources, right-size VMs, and optimize costs.

| *Virtual Machines Service Level*
| Tracks VM uptime and downtime over 24 hours, showing planned and unplanned breakdowns.

Use this dashboard to report on service-level agreements (SLA) and find VMs with unexpected downtime.

| *Virtual Machines Time in Status*
| Shows how long each VM has been in its current state, with filters for time thresholds.

Use this dashboard to find stuck VMs or long-stopped VMs that can be cleaned up.

| *Top Consumers*
| Ranks VMs by resource consumption across memory, CPU, storage, network, vCPU wait time, and swap.

Use this dashboard to plan capacity and identify resource contention.

| *Nodes Memory*
| Shows memory usage per node, including overcommit ratios, memory pressure, and system reservations.

Use this dashboard to plan memory capacity, analyze overcommit, and prevent out-of-memory events.
|===
