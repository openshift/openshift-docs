// Module included in the following assemblies:
//
// * operating/manage-user-access/understanding-authentication-providers.adoc

:_mod-docs-content-type: CONCEPT
[id="auth-provider-required-attributes_{context}"]
= Required attributes

[role="_abstract"]
Required attributes can restrict issuing of the {product-title-short} token based on whether a user identity has an attribute with a specific value.

For example, you can configure {product-title-short} only to issue a token when the attribute with key `is_internal` has the attribute value `true`.
Users with the attribute `is_internal` set to `false` or not set do not get a token.
