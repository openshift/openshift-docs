:_mod-docs-content-type: CONCEPT
[id="telco-troubleshooting-mco-purpose_{context}"]
= Purpose of the Machine Config Operator

The Machine Config Operator (MCO) manages and applies configuration and updates of {op-system-first} and container runtime, including everything between the kernel and kubelet.
Managing {op-system} is important since most telecommunications companies run on bare-metal hardware and use some sort of hardware accelerator or kernel modification.
Applying machine configuration to {op-system} manually can cause problems because the Machine Config Operator monitors each node and what is applied to them.

You must consider these minor components and how the Machine Config Operator can help you manage your clusters effectively.

[IMPORTANT]
====
You must use the the Machine Config Operator to perform all changes on worker or control plane nodes.
Do not manually make changes to {op-system} or node files.
====

////
Question: As someone not familiar with MCO, the above section made me ask more questions.
If I'm using the MCO, does that mean I'm applying the MCs automatically? So why do I have to worry about problems?
What are "these minor components" that I have to pay attention to? Neither MCO or RHCOS seems to be minor so that's why I'm asking for clarification
////
// These are some great questionst that I'm going to have to ponder and determine how to implement them here. However, you reminded me of a section I wanted to add ----below