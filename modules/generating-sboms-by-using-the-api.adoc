// Module included in the following assemblies:
//
// * operating/manage-vulnerabilities/scanner-generate-sbom.adoc

:_mod-docs-content-type: REFERENCE
[id="generating-sboms-by-using-the-api_{context}"]
= Generating SBOMs by using the API

[role="_abstract"]
You can use the {rh-rhacs-first} API to create a Software Bill of Materials (SBOM) by using the `ROX_API_TOKEN` for authorization.

You can use the {rh-rhacs-first} API to create a Software Bill of Materials (SBOM). You must use the `ROX_API_TOKEN` for authorization to connect to the endpoint and generate the SBOM. The API generates the request payload in JSON format.

For more information, see "GenerateSBOM".