:_mod-docs-content-type: PROCEDURE
[id="update-assessment-ai_{context}"]
= Analyzing update risk and status using AI

[role="_abstract"]
To quickly assess either the risk of an update path or the status of an in-progress update, you can use the web console to generate AI-powered guidance specific to your cluster.

Using this AI assessment feature opens OpenShift Lightspeed Service (OLS) with pre-loaded prompts and requires no manually written prompts. Cluster state data is processed by the large language model (LLM) provider that you have configured OLS to use.

[IMPORTANT]
====
The completeness and accuracy of the assessment depend on the configured model interacting correctly with the OpenShift MCP tools.
If the model does not invoke the required tools, or a tool call fails, the response might not reflect the current cluster state.

AI-generated analysis is advisory.
Verify the analysis against the current cluster state before taking action.
====

.Prerequisites

* You have OLS installed and actively connected to an LLM.
** The configured model can interact with the OpenShift MCP tools.
** OLS is configured with access to the OpenShift MCP server.
* You have access to the cluster with `cluster-admin` privileges.
* You have access to the {product-title} web console.

.Procedure

. On the web console, navigate to *Administration* -> *Cluster Settings*.

. If you want to evaluate an update to the latest version, click *Precheck your cluster with AI* in the *AI Assessment* section. You can click *Update status* to troubleshoot any issues identified by the assessment.

. If you want to assess a specific update path, perform the following steps:

.. Click *Select a version*.

.. Choose a target update version from the *Select a version* dropdown menu.

.. Click *Precheck your cluster with AI*. You can click *Update status* to troubleshoot any issues identified by the assessment.

. If you want to evaluate the status of an in-progress update, click *Update status* in the *AI Assessment* section while an update is in progress.