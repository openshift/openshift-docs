// This module is included in the following assembly:
//
// * work-with-builds/using-builds.adoc

:_mod-docs-content-type: CONCEPT
[id="ob-deleting-resources_{context}"]
= Deleting resources

You can delete the resources created by `Buildah`, `Source-to-image` and `buildpacks` build processes. 
