// Module included in the following assemblies:
//
// * serverless/serverless-logic/serverless-logic-creating-managing-workflows.adoc

:_content-type: PROCEDURE
[id="serverless-logic-running-workflows"]
= Running a workflow

You can use the `run` command with `kn workflow` to build and run your {ServerlessLogicProductName} workflow project in your current directory.


.Prerequisites

* The {ServerlessLogicOperatorName} is installed on the cluster.
* You have installed the Knative (`kn`) CLI.
* A {ServerlessLogicProductName} workflow project is created.

.Procedure

. Execute the following command to build and run your {ServerlessLogicProductName} workflow project:
+
[source,terminal]
----
$ kn workflow run
----

. Once the project is ready, the Development UI will be opened up in a browser automatically on `localhost:8080/q/dev`. 



