// Module included in the following assemblies:
//
// * infrastructure_lifecycle_management/ocloud-preparing-to-deploy-clusters.adoc

:_mod-docs-content-type: PROCEDURE
[id="ocloud-configuring-mtls-for-o2ims-api_{context}"]
= Configure mTLS for API access

[role="_abstract"]
You can configure mutual TLS (mTLS) to require client certificate authentication for all incoming connections to the O-Cloud Manager REST API. This secures API access for any external client, including SMO frameworks, automation platforms, and custom integrations.

.Prerequisites

* The O-Cloud Manager Operator is installed and running on the hub cluster.
* You have a TLS certificate and private key for the O-Cloud Manager ingress. The certificate must include `server auth` and `client auth` extended key usage.
* You have the CA certificate used to validate incoming client certificates.

.Procedure

. Create a TLS `Secret` containing the O-Cloud Manager certificate and private key. The `tls.crt` field must contain the full certificate chain with the device certificate first, intermediate certificates next, and the root certificate last.

.. To create the `Secret` using cert-manager, create a `Certificate` CR such as the following:
+
[source,yaml]
----
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oran-o2ims-tls-certificate
  namespace: oran-o2ims
spec:
  secretName: oran-o2ims-tls-certificate
  subject:
    organizations:
      - ORAN
  commonName: o2ims.apps.<cluster_domain>
  usages:
    - server auth
    - client auth
  dnsNames:
    - o2ims.apps.<cluster_domain>
  issuerRef:
    name: <issuer_name>
    kind: ClusterIssuer
    group: cert-manager.io
----
+
where:
+
`<cluster_domain>`:: Specifies the hub cluster domain name, for example `hub.example.com`.
`<issuer_name>`:: Specifies the name of your cert-manager `ClusterIssuer` resource.

.. Alternatively, to create the `Secret` manually from existing certificate files, run the following command:
+
[source,terminal]
----
$ oc create secret tls -n oran-o2ims oran-o2ims-tls-certificate \
  --cert <path_to_cert> \
  --key <path_to_key>
----
+
where:
+
`<path_to_cert>`:: Specifies the path to the full certificate chain file.
`<path_to_key>`:: Specifies the path to the private key file.

. Create a `ConfigMap` containing the CA certificates used to validate incoming client certificates by running the following command:
+
[source,terminal]
----
$ oc create configmap -n openshift-config ingress-client-ca-certs \
  --from-file=ca-bundle.pem=<path_to_client_ca>
----
+
where:
+
`<path_to_client_ca>`:: Specifies the path to the CA certificate file that validates incoming client certificates.

. Configure the `IngressController` to require mTLS for incoming connections by applying the following YAML:
+
[source,yaml]
----
apiVersion: operator.openshift.io/v1
kind: IngressController
metadata:
  name: default
  namespace: openshift-ingress-operator
spec:
  clientTLS:
    allowedSubjectPatterns:
      - "<subject_pattern>"
    clientCA:
      name: ingress-client-ca-certs
    clientCertificatePolicy: Required
----
+
where:
+
`<subject_pattern>`:: Optional. A regular expression that restricts access to specific certificate subjects, for example `^/O=ORAN/OU=SMO/`. Remove the `allowedSubjectPatterns` field to accept any valid client certificate signed by the configured CA.
+
[WARNING]
====
If the ingress controller is shared with other applications, setting `clientCertificatePolicy: Required` enforces client certificates on all incoming connections. Consider creating a secondary ingress controller for the O-Cloud Manager domain to avoid impacting other applications. Set `clientCertificatePolicy: Optional` if you cannot enforce mTLS globally but have OAuth enabled.
====

. Update the `Inventory` CR to reference the TLS certificate by running the following command:
+
[source,terminal]
----
$ oc patch inventory default -n oran-o2ims --type merge \
  -p '{"spec":{"ingress":{"tls":{"secretName":"oran-o2ims-tls-certificate"}}}}'
----

.Verification

* Verify that the ingress controller is configured for mTLS by running the following command:
+
[source,terminal]
----
$ oc get ingresscontroller default -n openshift-ingress-operator \
  -o jsonpath='{.spec.clientTLS}'
----
+
The output shows the configured `clientCertificatePolicy`, `clientCA`, and any `allowedSubjectPatterns`.
