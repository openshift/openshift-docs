// Module included in the following assemblies:
//
// * maps/jobs/change-core-user-password.adoc

:_mod-docs-content-type: CONCEPT
[id="change-core-user-password-intro_{context}"]
= Change the `core` user password for node access.

[role="_abstract"]
When you want to change the password for the `core` user on nodes in a specific pool, you can create a machine config object so that you can change the password across all the nodes.
