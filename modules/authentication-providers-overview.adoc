// Module included in the following assemblies:
//
// * operating/manage-user-access/understanding-authentication-providers.adoc

:_mod-docs-content-type: CONCEPT
[id="authentication-providers-overview_{context}"]
= Authentication providers overview

[role="_abstract"]
An authentication provider connects to a third-party identity provider, validates user identity, and issues tokens that {product-title-short} uses for authorization.

An authentication provider connects to a third-party source of user identity (for example, an identity provider or IDP), gets the user identity, issues a token based on that identity, and returns the token to {rh-rhacs-first}.
This token allows {product-title-short} to authorize the user.
{product-title-short} uses the token within the user interface and API calls.

After installing {product-title-short}, you must set up your IDP to authorize users.

[NOTE]
====
If you are using OpenID Connect (OIDC) as your IDP, {product-title-short} relies on mapping rules that examine the values of specific claims such as `groups`, `email`, `userid` and `name` from either the user ID token or the `UserInfo` endpoint response to authorize the users.
If these details are absent, the mapping cannot succeed and the user does not get access to the required resources.
Therefore, you need to ensure that the claims required to authorize users from your IDP, for example, `groups`, are included in the authentication response of your IDP to enable successful mapping.
====
