// Module included in the following assemblies:
//
// * observability/network_observability/network-observability-kafka-operator-scaling-network-flow-collection.adoc

:_mod-docs-content-type: REFERENCE
[id="network-observability-kafka-exporter-parameters_{context}"]
= Advanced Kafka exporter parameters

[role="_abstract"]
Configure advanced Kafka exporter parameters to optimize the performance and security of exported network flow data.

.Kafka exporter parameters
[cols="2,4,2,1",options="header"]
|===
|Parameter |Description |Accepted values |Default value

|`compression`
|Sets the compression algorithm for messages sent to Kafka. Compression reduces network bandwidth at the cost of CPU usage on the eBPF agent.
|`gzip`, `snappy`, `lz4`, `zstd`, `none`
|`none`

|`address`
|The address of the Kafka broker or bootstrap server.
|Hostname:port string
|Required

|`topic`
|The Kafka topic to which flow records are sent.
|String
|`network-flows`

|`tls`
|TLS configuration for secure connections to Kafka.
|TLS object
|Not configured
|===

== Compression codec performance characteristics

The following table provides guidance for choosing a compression codec based on the balance between compression ratio and CPU cost:

.Kafka compression codec comparison
[cols="1,^1,^1,^1,3",options="header"]
|===
|Codec |Compression ratio |CPU cost (producer) |Decompression cost |Notes

|`none`
|1x
|—
|—
|No overhead; use when CPU is the bottleneck

|`lz4`
|2-3 times
|Very low
|Very low
|*Recommended default.* Best latency/ratio trade-off

|`snappy`
|2-3 times
|Very low
|Very low
|Similar to lz4, slightly lower ratio

|`zstd`
|3-5 times
|Moderate
|Low
|Higher ratio; good for high-throughput clusters

|`gzip`
|3-5 times
|High
|Moderate
|Maximum ratio but significant CPU cost
|===

[NOTE]
====
The compression ratios and CPU cost estimates are approximate values derived from upstream benchmarks, not from Network Observability-specific measurements. Actual results vary depending on flow record characteristics and batch sizes.
====

== How compression works

* *Producer side (eBPF agent):* Messages are compressed per batch before being sent to Kafka. The compression codec is stored in the message headers.
* *Consumer side (flowlogs-pipeline):* Decompression is handled automatically by the Kafka client based on message headers. No configuration is needed on the consumer.
* *Kafka brokers:* Compressed messages are stored as-is on disk, reducing storage. Brokers do not need any specific configuration for client-side compression.

== Compression efficiency and batching

Compression efficiency improves with larger batches. The `spec.agent.ebpf.kafkaBatchSize` setting (default 1 MiB) controls the maximum batch size in bytes before compression. Larger batches compress better but add latency. The default batch size provides a good balance.
