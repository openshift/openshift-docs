// Module included in the following assemblies:
//
// *install-guide/install-guide.adoc

:_mod-docs-content-type: PROCEDURE
[id="enable-openshift-dynamic-plugin-web-console_{context}"]
= Enabling the {prodname} dynamic plugin for {ocp} web console

[role="_abstract"]
You can use the {prodname} dynamic plugin to view and manage your gateways and policies in the {ocp} web console. You must perform these steps on each {ocp} cluster.

.Prerequisites

* You are using a supported configuration of {ocp} and required components.
* You are logged into {ocp} as a cluster administrator.
* You are logged into the {ocp} web console with administrator access.

.Procedure

. In the navigation menu, select the *Administrator* perspective.

. Click *Home > Overview*.

. In the *Status* panel, click *Dynamic Plugins* > *View all*.

. On the *Console plugins* tab, find the *kuadrant-console-plugin* entry in the table, which should be listed but disabled.

. In the *kuadrant-console-plugin* row, click *Disabled*.

. Select the *Enable* option, and click *Save*.

. Wait for the plugin status to change to *Loaded*.

.Verification

. Refresh the {ocp} web console. A new *{prodname}* menu item is displayed in the navigation sidebar.

.. You can click  *{prodname} > Overview* to explore the available resources and to get started with creating a Gateway and configuring policies in the {ocp} web console.

.Next steps

* Create a gateway.
* Create policies.
