// Module included in the following assemblies:
// * assembly_operating-degraded-two-node-fencing-cluster.adoc

:_mod-docs-content-type: CONCEPT
[id="cluster-operator-stability-during-degraded-operation_{context}"]
= Cluster operator stability during degraded operation

[role="_abstract"]
{product-title} cluster operators maintain control plane stability and API availability when a two-node cluster enters a degraded state.

During degraded operations, cluster operator conditions exhibit the following behaviors:

* Most operators maintain an `Available=True` condition, ensuring core API functionality remains accessible.

* The following operators transition to a `Degraded=True` condition because only one of the two expected control-plane instances is operational:
    - `etcd`
    - `kube-apiserver`
    - `kube-controller-manager`
    - `kube-scheduler`
    - `machine-config`

* Specific operators might exhibit a `Progressing=True` condition during reconciliation routines that require data from both infrastructure nodes. This condition resolves when the second node returns to an online state.

[NOTE]
====
No unexpected or cascading operator failures should occur during degraded operation. If they do, investigate the issue as a potential bug.
====
