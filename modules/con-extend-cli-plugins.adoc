:_mod-docs-content-type: CONCEPT

[id="con-extend-cli-plugins"]
= Extend the CLI with plugins

[role="_abstract"]
When you need CLI functionality beyond the default oc commands, learn how to extend the CLI through custom plugins or the CLI Manager Operator so that you can add specialized tools to your workflow.

Write and install a custom plugin:: Writing CLI plugins, Installing and using CLI plugins
Use the CLI Manager for centralized plugin management:: About the {cli-manager}, {cli-manager} 0.2.0 (Technology Preview), {cli-manager} 0.1.1 (Technology Preview), Installing the {cli-manager}, Adding the {cli-manager} custom index to Krew, Adding a plugin to the {cli-manager}, Installing CLI plugins with the {cli-manager}, Upgrading a plugin with the {cli-manager}, Updating CLI plugins with the {cli-manager}, Uninstalling a CLI plugin with the {cli-manager}
Manage CLI Manager lifecycle:: Uninstalling the {cli-manager}, Removing {cli-manager} resources
