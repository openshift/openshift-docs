// Module included in the following assemblies:
//
// * edge_computing/day_2_core_cnf_clusters/upgrading/telco-upgrade-welcome.adoc

:_mod-docs-content-type: PROCEDURE
[id="telco-upgrade-introduction_{context}"]
= Cluster upgrades for telco core CNF clusters

Upgrading your cluster is a critical task that ensures that bugs and potential security vulnerabilities are patched.
Often, updates to cloud-native network functions (CNF) require additional functionality from the platform that comes when you upgrade the cluster version.
You also need to upgrade the cluster periodically to ensure that the cluster platform version is supported.

You can minimize the effort required to stay current with upgrades by keeping as up to date as possible with EUS releases and upgrading to select important z-stream releases only.

[NOTE]
====
The upgrade path for the cluster can vary depending on the size and topology of the cluster.
The upgrade procedures described here are valid for most clusters from 3-node clusters up to the largest size clusters certified by the telco scale team.
This includes some scenarios for mixed-workload clusters.
====

The following upgrade scenarios are described:

* EUS to EUS upgrades
* Y-stream upgrades
* Z-stream upgrades
