// Module included in the following assemblies:
//
// * scalability_and_performance/low_latency_tuning/cnf-provisioning-low-latency-workloads.adoc

:_mod-docs-content-type: CONCEPT
[id="cnf-disabling-cpu-cfs-quota_{context}"]
= CPU quota annotation for pods

[role="_abstract"]
On {product-title}, you do not need the `cpu-quota.crio.io` annotation to disable the CPU completely fair scheduler (CFS) quota for pods.

Starting in {product-title} 4.20, the kubelet automatically disables the CPU CFS quota (which prevents CPU throttling) for pods that meet all of the following:

* The pod has a `Guaranteed` quality of service (`QoS`) class.
* The pod requests whole CPUs.
* The CPU manager static policy is enabled, for example by a performance profile.

If your workloads already include the `cpu-quota.crio.io: "disable"` annotation, you can leave it in place.
The annotation has no effect because the kubelet provides this functionality automatically.
