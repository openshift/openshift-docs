// Module included in the following assemblies:
//
// * monitoring/monitoring-configuration-scenarios.adoc

:_content-type: PROCEDURE
[id="monitoring-platform-monitoring-basic-configuration-scenario_{context}"]
= Platform monitoring: Basic configuration scenario

This scenario describes setup steps to help administrators get started with monitoring services in a {product-title} cluster using the in-cluster monitoring stack.
These setup steps include configuring ServiceMonitors, metrics, alerts, and persistent storage.

.Prerequisites
* You have access to the cluster as a user with the `cluster-admin` role.
* You have created users.
* You have installed the OpenShift CLI (`oc`).

.Procedure

. Set permissions for users:

.. Allow users to create alerts:
+
[source,terminal]
----
$ oc policy add-role-to-user monitoring-edit <user> -n <namespace>
----
+
.. Allow users to route alerts:
+
[source,terminal]
----
$ oc -n <namespace> adm policy add-role-to-user alert-routing-edit <user>
----
+
. If it does not exist, create the `cluster-monitoring-config` `ConfigMap` object.

.. Verify whether or not the `cluster-monitoring-config` `ConfigMap` object already exists:
+
[source,terminal]
----
$ oc -n openshift-monitoring get configmap cluster-monitoring-config
----
+
.. If the `cluster-monitoring-config` `ConfigMap` object does not exist, create the following YAML manifest. In this example, the file is called `cluster-monitoring-config.yaml`:
+
[source,yaml]
----
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-monitoring-config
  namespace: openshift-monitoring
data:
  config.yaml: |
----
+
.. Apply the configuration to create the `ConfigMap` object:
+
[source,terminal]
----
$ oc apply -f cluster-monitoring-config.yaml
----

. Customize Prometheus.

.. Configure a metrics collection profile to specify the amount of metrics data to scrape.

.. Set the body size limit for metrics collection.

.. Add cluster ID labels for use with remote write storage configuration

. Configure storage.

.. For short-term data retention, configure persistent storage by creating Persistent Volume Claims (PVCs) to provision persistent storage volumes (PVs).
Configure these PVs to be used by Prometheus to store metrics data and by Alertmanager to store alert data. Specify the metrics data retention parameters for Prometheus and Thanos Ruler.
+
[NOTE]
====
By default, a newly installed {product-title} system is configured to fire an alert named `PodStartupStorageOperationsFailing` to remind you that storage is not configured.
You can resolve this alert on the *Alerts* tab in the *Observe* -> *Alerting* section of the {product-title} web console.
====
+
 .. For long-term data retention, configure the remote write feature to enable Prometheus to send ingested metrics to remote systems for storage.

. Customize Alertmanager, which handles alerts generated by Prometheus.

.. Modify or create alerting rules for your cluster by defining rules for different conditions based on metrics and labels.
These rules specify the conditions that trigger alerts, such as high CPU or memory usage, network latency, and so forth.

.. Configure alert receivers to send alerts to a notification system.

. Configure ServiceMonitors, which define the set of services and endpoints that Prometheus will scrape for metrics.

.. Create these ServiceMonitor resources for each service you want to monitor.
These resources specify the labels that identify the target services and the scraping configuration, including the endpoint paths and HTTP ports to scrape.

. Monitor and scale your services and workloads.
With the monitoring stack set up, Prometheus will start scraping metrics from the specified services and storing these metrics.
Use the collected metrics to monitor your services and applications, to identify performance bottlenecks, and to scale resources as needed.
You can use the *Observe* pages in the {product-title} web console to view dashboards to visualize the collected metrics, view and troubleshoot alerts, and monitor other information about your cluster.
