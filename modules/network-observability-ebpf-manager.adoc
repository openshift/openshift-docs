// Module included in the following assemblies:
//
// * network_observability/observing-network-traffic.adoc

:_mod-docs-content-type: PROCEDURE
[id="network-observability-ebpf-manager_{context}"]
= Working with eBPF manager
The eBPF Manager is a separate operator that manages all eBPF programs, thereby reducing the attack surface, and ensuring compliance, security, and preventing conflicts. Network observability can leverage eBPF Manager and let it handle the loading of hooks, ultimately removing the need to provide the eBPF Agent with privileged mode or additional Linux capabilities such as CAP_BPF and CAP_PERFMON. Since this is a Developer Preview feature, privileged mode is still required for now, and it is only supported on the amd64 architecture.

//Delete comments before merging PR
//05/02/2025: Source https://netobserv.io/posts/what-s-new-in-network-observability-1-8/ -> eBPF Manager Support.
//Content is copy/pasted to open PR. Needs further refinement to meet Red Hat style guides.
//According to the official Red Hat Product name spreadsheet, it is "network observability" and not "Network Observability". There does not appear to be attribute for anything related to network observability, which is something to address separately.
//For docs.redhat, Technology Preview banners are handled separately by another team. Inserted admonition from https://redhat-documentation.github.io/supplementary-style-guide/#technology-preview-guidance. CHECK on feature name. Is it "Network observability with eBPF manager" or "eBPF manager" or something else?

[IMPORTANT]
====
Network observability with eBPF manager is a Technology Preview feature only. Technology Preview features are not supported with Red{nbsp}Hat production service level agreements (SLAs) and might not be functionally complete. Red{nbsp}Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.

For more information about the support scope of Red{nbsp}Hat Technology Preview features, see link:https://access.redhat.com/support/offerings/techpreview/[Technology Preview Features Support Scope].
====

.Procedure
. In the web console, navigate to *Operators* -> *Operator Hub*.
. Install *eBPF Manager*.
. Check *Workloads* -> *Pods* in the `bpfman` namespace to make sure they are all up and running.
. Install network observability.
//05/02/2025: Is this the network observability Operator? If users already have network observability installed, do they need to uninstall it first, then install eBPF Manager, install network observability, and configure the FlowCollector resource?
. Configure the `FlowCollector` custom resource, for example:
+
[id="network-observability-flowcollector-configuring-ebpfmanager_{context}"]
.Example `FlowCollector` configuration
[source,yaml]
----
apiVersion: flows.netobserv.io/v1beta2
kind: FlowCollector
metadata:
  name: cluster
spec:
  agent:
    ebpf:
      privileged: true  # required for now
      interfaces: <1>
        - br-ex
      features:
        - eBPFManager
----
<1> You must specify an interface so the eBPF Manager know where to attach the TCx hook.

.Verification
. In the web console, navigate to *Operators* -> *Installed Operators*.
. Click *eBPF Manager Operator* -> *All instances* tab.
There should a BpfApplication named netobserv and a pair of BpfProgram, one for TCx ingress and another for TCx egress, for each node. There might be more if you enable other eBPF Agent features.

//05/02/2025:
//This might need to be broken up into a Concept, and a Procedure
//Will likely need to add Additional resources section with links to networking/networking-operators#bpfman-operator-install