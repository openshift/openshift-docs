// Module included in the following assemblies:
//
// * installing/disconnected_install/installing-mirroring-disconnected-v2.adoc

:_mod-docs-content-type: Procedure
[id="oc-mirror-signature-mirroring-procedure_{context}"]
= Enabling signature mirroring for oc-mirror plugin v2

== Default behavior of signature mirroring

* By default, oc-mirror plugin v2 mirrors cosign tag-based signatures for container images. To disable signature mirroring for all images, use the following command:
+
[source,terminal]
----
$ oc mirror --remove-signatures 
----

== For specific control over signature mirroring 

You can enable or disable signature mirroring by transport protocol, registry, namespace, or image. 

* To do this, create a configuration file in either of the following locations:

* `$HOME/.config/containers/registries.d/`
* `/etc/containers/registries.d/`

.Example: Disable signature mirroring for the `quay.io` registry
+
[source,yaml]
----
docker:
  quay.io:
    use-sigstore-attachments: false
----

.Example: Enable signature mirroring for all registries
+
[source,yaml]
----
default-docker:
  use-sigstore-attachments: true
----

== Custom configuration directory for signature verification

If no configuration is provided, oc-mirror plugin v2 enables signature mirroring for all images by default.

* To specify a custom configuration directory, use the `--registries.d` flag.

For more information, see link:https://github.com/containers/image/blob/main/docs/containers-registries.d.5.md[`containers-registries.d(5)`].
