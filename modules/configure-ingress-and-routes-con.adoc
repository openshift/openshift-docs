// Module included in the following assemblies:
//
// * maps/ocp-jobs/configure-ingress-and-routes.adoc

:_mod-docs-content-type: CONCEPT
[id="configure-ingress-and-routes-con_{context}"]
= Configure ingress and routes

[role="_abstract"]
`Route` and `Ingress` objects expose services inside the cluster to external HTTP and HTTPS clients. Create routes, secure them with TLS, and configure the Ingress Controllers that serve them.
