// Module included in the following assemblies:
//
// * operating/manage-user-access/understanding-authentication-providers.adoc

:_mod-docs-content-type: REFERENCE
[id="auth-provider-saml20-default-claim-mappings_{context}"]
= SAML 2.0 default claim mappings

[role="_abstract"]
The following list applies to SAML 2.0 default claim mappings.

* `Subject.NameID` maps to `userid`
* every SAML `AttributeStatement.Attribute` from the response maps to its name
