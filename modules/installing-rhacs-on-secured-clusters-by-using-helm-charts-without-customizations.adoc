// Module included in the following assemblies:
//
// * installing/installing_other/install-secured-cluster-other.adoc

:_mod-docs-content-type: CONCEPT
[id="installing-sc-helm-default-other_{context}"]
= Install {product-title-short} on secured clusters by using Helm charts without customizations

[role="_abstract"]
This procedure describes how to install {rh-rhacs-first} on secured clusters by using Helm charts with default configuration values and no customizations.