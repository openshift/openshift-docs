// Module included in the following assemblies:
//
// * backup_and_restore/application_backup_and_restore/cli-tools/oadp-cli-plugin.adoc

:_mod-docs-content-type: PROCEDURE
[id="oadp-cli-setup_{context}"]
= Set up the OADP CLI plugin

[role="_abstract"]
After you install the {oadp-short} command-line interface (CLI) plugin, you must run the setup command to configure it for your user permissions.

The setup command automatically detects whether you have cluster-wide administrator permissions and configures the CLI accordingly. The CLI operates in one of the following two modes:

* *Admin mode*: Provides access to cluster-wide Velero backup and restore commands.
* *Non-admin mode*: Provides access to namespace-scoped self-service backup and restore commands.

== Prerequisites

* The {oadp-short} CLI plugin is installed.
* You are logged in to the {product-title} cluster by using the `oc login` command.

== Procedure

. Run the setup command to auto-detect your permissions and configure the CLI:
+
[source,terminal]
----
$ oc oadp setup
----
+
The CLI checks whether you can create `backups.velero.io` resources across all namespaces. If you can, admin mode is enabled. Otherwise, non-admin mode is enabled. The configuration is saved to `~/.config/velero/config.json`.

. Optional: To reconfigure the CLI after a change in permissions, run the setup command with the `--force` flag:
+
[source,terminal]
----
$ oc oadp setup --force
----

== Verification

* Run the following command to confirm that the available commands match your configured mode:
+
[source,terminal]
----
$ oc oadp --help
----

[NOTE]
====
{oadp-short} CLI commands support both noun-verb and verb-noun ordering. For example, `oc oadp backup create` and `oc oadp create backup` are equivalent.
====
