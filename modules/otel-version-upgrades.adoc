// Module included in the following assemblies:
//
// * operator/otel-managing-operator.adoc

:_mod-docs-content-type: CONCEPT
[id="otel-version-upgrades_{context}"]
= Version upgrades

[role="_abstract"]
For version upgrades, OLM queries for upgrades for installed Operators. When OLM upgrades the {OTELOperator} to the new version, the Operator scans for running Collector instances that it manages and upgrades them to the version corresponding to the Operator's new version.

The {OTELOperator} automatically upgrades all `OpenTelemetryCollector` custom resources during its startup. The Operator reconciles all managed instances during its startup. If an error occurs, the Operator retries the upgrade at an exponential back-off rate. If an upgrade fails, the Operator retries the upgrade when it restarts.

OLM runs in the {ocp-product-title} by default.
