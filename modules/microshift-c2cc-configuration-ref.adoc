// Module included in the following assemblies:
//
// * microshift_networking/microshift-cluster-to-cluster-communication.adoc

:_mod-docs-content-type: REFERENCE
[id="microshift-c2cc-configuration-ref_{context}"]
= C2CC configuration reference

[role="_abstract"]
All C2CC settings live under the `clusterToCluster` section of the {microshift-short} configuration file.
Use this reference when configuring remote clusters, DNS caching, routing tables, and health probes.

.C2CC configuration parameters
[cols="1,2"]
|===
|Parameter|Description

|`clusterToCluster`
|The `clusterToCluster` section of the {microshift-short} configuration file defines C2CC settings.
C2CC is disabled when `remoteClusters` is empty.

|`remoteClusters`
|List of remote clusters.
Add one entry per remote cluster on every host in a full-mesh topology.

|`remoteClusters[].nextHop`
|Next-hop IP addresses of the remote node.
At most one IPv4 and one IPv6 address.
Must not equal the local node IP or duplicate another remote.
Required.

|`remoteClusters[].clusterNetwork`
|Remote pod CIDRs.
Must not overlap with local or other remote CIDRs.
Minimum mask `/8` (IPv4) or `/32` (IPv6).
Required.

|`remoteClusters[].serviceNetwork`
|Remote service CIDRs.
Same constraints as `clusterNetwork`; must have the same number of entries with matching IP families.
Required.

|`remoteClusters[].domain`
|Optional DNS suffix for the remote cluster.
Must be a valid DNS subdomain, unique across remotes, and not `cluster.local`.
When set, Services on the remote cluster are resolvable as `<svc>.<ns>.svc.<domain>`.
Default: `""` (no DNS forwarding for this remote).

|`probeInterval`
|Interval between health probes to each remote cluster.
Go duration string between `1s` and `5m`.
Default: `10s`.

|`dns.cacheTTL`
|Maximum TTL in seconds for positive DNS cache entries for remote domains.
`0` disables caching.
Default: `10`.

|`dns.cacheNegativeTTL`
|Maximum TTL in seconds for negative (NXDOMAIN/NODATA) DNS cache entries.
`0` disables caching.
Default: `10`.

|`routing.routeTableID`
|Linux policy routing table for routes to remote CIDRs.
Range `1`–`252`; must differ from `serviceRouteTableID`.
Default: `200`.

|`routing.serviceRouteTableID`
|Linux policy routing table for service routes via the OVN management port.
Range `1`–`252`; must differ from `routeTableID`.
Default: `201`.
|===

The routing table IDs only need to be changed if other software on the host (for example, NetworkManager dispatcher scripts or VPN tooling) already uses tables `200` and `201`.

== IPv6 and dual-stack example

C2CC supports IPv6-only and dual-stack clusters.
The local and remote clusters must have compatible IP families: for every remote CIDR family there must be a matching-family `nextHop` entry.

.Dual-stack remote cluster configuration
[source,yaml]
----
clusterToCluster:
  remoteClusters:
  - nextHop:
    - 192.168.1.20
    - 2001:db8:0:1::20
    clusterNetwork:
    - 10.45.0.0/16
    - fd01:0:0:1::/64
    serviceNetwork:
    - 10.46.0.0/16
    - fd02:0:0:1::/112
    domain: cluster-b.remote
----

`clusterNetwork` and `serviceNetwork` must have the same number of entries with matching IP families at each position (at most one IPv4 and one IPv6 CIDR each).

== Disabling C2CC

Remove the C2CC configuration snippet (or the `remoteClusters` entries) and restart {microshift-short}:

[source,terminal]
----
$ sudo rm /etc/microshift/config.d/50-c2cc.yaml
$ sudo systemctl restart microshift
----

On startup, the controller detects that C2CC is disabled and cleans up all C2CC-owned state: OVN static routes, kernel routes and rules, nftables rules, node annotations, the probe deployment, and the `RemoteCluster` resources.

Remember to remove the remote CIDRs from the firewall trusted zone as well.

If you overrode `routing.routeTableID` or `routing.serviceRouteTableID`, disable in two stages so the cleanup targets the right tables: first remove only the `remoteClusters` entries while keeping the `routing` overrides and restart, then remove the rest of the `clusterToCluster` section and restart again.
