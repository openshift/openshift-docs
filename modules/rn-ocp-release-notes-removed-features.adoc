:_mod-docs-content-type: REFERENCE
[id="rn-ocp-release-notes-removed-features_{context}"]
= Removed features

This section includes removed features for {product-title} {product-version}.

Swap memory support has been removed::
+
The ability to configure swap memory is no longer available in {product-title} because using swap memory can prevent a node from properly safeguarding itself from failures.
