// Module included in the following assemblies:
//
// backup_and_restore/application_backup_and_restore/oadp-self-service/oadp-self-service-non-admin-use-cases.adoc

:_mod-docs-content-type: PROCEDURE
[id="oadp-self-service-use-case-kopia_{context}"]
= Creating a non-admin backup and restore

In this use case, as a non-admin user:

* You create a File System Backup (FSB) of your application running in your authorized namespace. You do this by creating a `NonAdminBackup` (NAB) CR. The cluster administrator has enabled the `nodeAgent` with Kopia as the `uploaderType` in the `DataProtectionApplication` (DPA). 
* Optionally, you can also use the Data Mover or the Container Storage Interface (CSI) to back up the data volumes
* To simulate disaster, you delete the application in your authorized namespace. 
* You then restore the application in your namespace by creating a `NonAdminRestore` (NAR) CR.

.Prerequisites

* You are logged in to the cluster as a non-admin user.
* The cluster administrator has installed the {oadp-short} Operator.
* The cluster administrator has configured the DPA to enable {oadp-short} Self-Service.
* The cluster administrator has created a namespace for you and has authorized you to operate from that namespace.
* You have installed an application in your authorized namespace. 

.Procedure

. To create a `NonAdminBackup` (NAB) CR for filesystem backup of your application, configure a YAML manifest file as shown in the following example:
+
.Example `NonAdminBackup`
[source,yaml]
----
apiVersion: oadp.openshift.io/v1alpha1
kind: NonAdminBackup
metadata:
  name: test-nab
spec:
  backupSpec:
    includedNamespaces:
    - "test-nac" # <1>
    defaultVolumesToFsBackup: true # <2>
    snapshotMoveData: false # <3>
----
1. Specify the name of your authorized namespace in which your application is running. In this example, the namespace is `test-nac`.
2. Set the `defaultVolumesToFsBackup` field to `true` to enable FSB.
3. To back up the volumes by using the Data Mover, set the `snapshotMoveData` field to `true`.

. Create the NAB CR by running the following command:
+
[source,terminal]
----
$ oc apply -f <nab_cr_filename> # <1>
----
1. Specify the YAML file name containing the NAB CR configuration.

. Verify that the phase of the NAB CR is `Created` by running the following command:
+
[source,terminal]
----
$ oc get nab test-nab -o yaml
----
+
.Example output
[source,yaml]
----
apiVersion: oadp.openshift.io/v1alpha1
kind: NonAdminBackup
....
spec:
  ....
status:
  conditions:
  - lastTransitionTime: "2025-02-11T13:37:33Z"
    message: backup accepted
    reason: BackupAccepted
    status: "True"
    type: Accepted
  - lastTransitionTime: "2025-02-11T13:37:33Z"
    message: Created Velero Backup object
    reason: BackupScheduled
    status: "True"
    type: Queued
  phase: Created # <1>
----
1. Note that the `status.phase` is `Created`.

. Wait until the `veleroBackup.status.phase` field of the NAB CR is `Completed` as shown in the following example.
+
.Example output
[source,yaml]
----
apiVersion: oadp.openshift.io/v1alpha1
kind: NonAdminBackup
...
status:
  ....
  veleroBackup:
    nacuuid: test-nac-test-nab-...5-a0a9-16813d2e4d51
    name: test-nac-test-nab-6...5-a0a9-16813d2e4d51
    namespace: openshift-adp
    status:
      completionTimestamp: "2025-02-11T13:37:54Z"
      expiration: "2025-03-13T13:37:33Z"
      formatVersion: 1.1.0
      hookStatus: {}
      phase: Completed # <1>
      progress:
        itemsBackedUp: 50
        totalItems: 50
      startTimestamp: "2025-02-11T13:37:33Z"
      version: 1
----
1. The phase is now `Completed`.

. To simulate disaster, delete the application running in your authorized namespace.

. Verify that no pods are running in your authorized namespace after you delete the application.
+
[source,terminal]
----
$ oc get pod -n test-nac # <1>
----
1. `test-nac` is your authorized namespace.
+
.Example output
[source,terminal]
----
No resources found in test-nac namespace.
----

. To create a `NonAdminRestore` (NAR) CR, configure a YAML manifest file as shown in the following example:
+
.Example `NonAdminRestore`
[source,yaml]
----
apiVersion: oadp.openshift.io/v1alpha1
kind: NonAdminRestore
metadata:
  name: test-nar
spec:
  restoreSpec:
    backupName: test-nab # <1>
----
1. Specify the name of the NAB CR in the `backupName` field. In this example, `test-nab` is the NAB CR.

. Create the NAR CR by running the following command:
+
[source,terminal]
----
$ oc apply -f <nar_cr_filename> # <1>
----
1. Specify the YAML file name containing the NAR CR configuration.

. Verify that the NAR CR is in the `created` phase, and eventually, the `completed` phase by running the following command:
+
[source,terminal]
----
$ oc get nar test-nar -o yaml
----
+
.Example `NonAdminRestore`
[source,yaml]
----
apiVersion: oadp.openshift.io/v1alpha1
kind: NonAdminRestore
...
status:
  ...
  veleroRestore:
    nacuuid: test-nac-test-nar....94e-3ff83737948b
    name: test-nac-test-nar-....7b-b94e-3ff83737948b
    namespace: openshift-adp
    status:
      completionTimestamp: "2025-02-11T13:51:24Z"
      hookStatus: {}
      phase: Completed # <2>
      progress:
        itemsRestored: 29
        totalItems: 29
      startTimestamp: "2025-02-11T13:50:51Z"
      warnings: 6
----
1. The `status.phase` field is `Created`. 
2. After a few minutes, the `veleroRestore.status.phase` field is `Completed`, indicating a successful restore.