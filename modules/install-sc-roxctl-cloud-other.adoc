// Module included in the following assemblies:
//
// * cloud_service/installing_cloud_other/install-secured-cluster-cloud-other.adoc

:_mod-docs-content-type: CONCEPT
[id="installing-sc-roxctl-cloud-other_{context}"]
= Installing {product-title-short} on secured clusters by using the roxctl CLI

[role="_abstract"]
Install {product-title-short} on secured clusters by using the roxctl CLI to deploy Sensor components.

To install {product-title-short} on secured clusters by using the CLI, perform the following steps:

. Install the `roxctl` CLI.
. Install Sensor.
