// Module included in the following assemblies:
//
// * maps/ocp-jobs/rebuild-an-on-cluster-layered-image.adoc

:_mod-docs-content-type: CONCEPT
[id="rebuild-an-on-cluster-layered-image-con_{context}"]
= Force a rebuild of the object used with the on-cluster mode of {image-mode-os-lower}

[role="_abstract"]
Some changes to a `MachineOSConfig` object do not trigger an automatic rebuild of the layered image. When you make such a change, you can start a rebuild manually so that the change reaches your nodes.
