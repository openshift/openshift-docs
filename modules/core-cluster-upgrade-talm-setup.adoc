// Module included in the following assemblies:
//
// * post_installation_configuration/day_2_core_cnf_clusters/updating/update-bare-metal-core-cluster-preparing-policies.adoc

:_mod-docs-content-type: PROCEDURE
[id="core-cluster-upgrade-talm-setup_{context}"]
= Preparing {rh-rhacm} policies and placement rules for cluster updates

[role="_abstract"]
You can prepare {rh-rhacm-first} policies and placement rules on your hub cluster to enable policy-based updates for your bare-metal core clusters.
This preparation includes organizing your Git repository for update policies and configuring cluster labels and placement rules to target clusters for updates.

.Prerequisites

* {rh-rhacm} hub cluster is deployed and managing target bare-metal core clusters. For more information, see link:https://access.redhat.com/documentation/en-us/red_hat_advanced_cluster_management_for_kubernetes/[Red{nbsp}Hat Advanced Cluster Management for Kubernetes documentation].
* {cgu-operator-full} ({cgu-operator}) is installed on the {rh-rhacm} hub cluster. For more information, see "Installing {cgu-operator-full} by using the CLI".
* Git repository for storing update policies with appropriate access controls.
* Cluster-admin privileges on the {rh-rhacm} hub cluster.
* The `oc` CLI tool is installed and configured.

.Procedure

. Prepare your Git repository structure similar to the following example:
+
[source,text]
----
upgrade-policies/
├── policies/
│   ├── zstream/
│   │   └── upgrade-4.20.1-policy.yaml
│   ├── ystream/
│   │   └── upgrade-4.20-policy.yaml
│   └── eus/
│       └── upgrade-4.20-eus-policy.yaml
└── cgu/
    ├── core-zstream-upgrade.yaml
    ├── core-ystream-upgrade.yaml
    └── core-eus-upgrade.yaml
----
+
NOTE: If you are using Kustomize for policy management with ArgoCD or Flux, you can add a `kustomization.yaml` file to manage policy overlays.
This file is optional if you apply policies directly with `oc apply`.

. Create a `Placement` rule so that update policies can be bound to clusters through labels.
+
NOTE: If you use PolicyGenerator with ArgoCD or a ZTP pipeline, `Placement` and `PlacementBinding` resources are generated automatically from your PolicyGenerator configuration. The following manual steps are provided for environments that do not use PolicyGenerator or for reference when troubleshooting placement issues.
+
The following example uses the label `upgrade-20to21=""` to bind a policy to a cluster:
+
[source,yaml]
----
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: openshift-upgrade-placement
  namespace: policies
  annotations:
    policy.open-cluster-management.io/description: |
      Placement rule for OpenShift upgrade policies. Targets clusters with
      the label "upgrade-20to21=true" for 4.20 to 4.21 upgrades.
spec:
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchLabels:
            upgrade-20to21: ""
----

. Create a `PlacementBinding` resource to connect the policies to the placement rule:
+
[source,yaml]
----
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: openshift-upgrade-placement-binding
  namespace: policies
  annotations:
    policy.open-cluster-management.io/description: |
      Binding that connects OpenShift upgrade policies to their placement rule.
      This binding ensures that both the main upgrade policy and pre-upgrade
      health check policy are applied to the same set of target clusters.
placementRef:
  name: openshift-upgrade-placement
  kind: Placement
  apiGroup: cluster.open-cluster-management.io
subjects:
  - name: openshift-upgrade-4.20-to-4.21-policy
    kind: Policy
    apiGroup: policy.open-cluster-management.io
  - name: pre-upgrade-health-check-policy
    kind: Policy
    apiGroup: policy.open-cluster-management.io
----
+
A single policy, placement rule, and placement binding set can be bound to multiple clusters by adding the matching label to each cluster.

. Add an update label to your target clusters by running the following command:
+
[source,terminal]
----
$ oc label mcl <cluster_name> upgrade-version-to-4-21=""
----

. Verify the current labels on a managed cluster by running the following command:
+
[source,terminal]
----
$ oc get mcl <cluster_name> -o jsonpath='{.metadata.labels}'
----
+
.Example output
[source,terminal]
----
{
  "app.kubernetes.io/instance": "clusters",
  "cluster.open-cluster-management.io/clusterset": "default",
  "name": "test1",
  "ocp-version": "4.20",
  "openshiftVersion": "4.20.15",
  "upgrade-version-to-4-21": "",
  "upgrade-version-to-4-22": "",
  "unpause-worker-mcp": ""
}
----

. Optional: Label clusters by site location for topology awareness by running the following command:
+
[source,terminal]
----
$ oc label managedcluster <cluster_name> site=<site_name>
----

. Create a `ManagedClusterSet` resource for update targets:
+
[source,yaml]
----
apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSet
metadata:
  name: core-bm-clusters
spec:
  clusterSelector:
    selectorType: LabelSelector
    labelSelector:
      matchLabels:
        cluster-type: bare-metal-core
----

. Apply the `ManagedClusterSet` resource by running the following command:
+
[source,terminal]
----
$ oc apply -f managedclusterset.yaml
----

.Verification

. Verify {cgu-operator} is running by running the following command:
+
[source,terminal]
----
$ oc get pods -n openshift-operators | grep cluster-group-upgrades
----
+
.Example output
[source,terminal]
----
cluster-group-upgrades-controller-manager-5d7b9c8f7d-abc12   2/2     Running   0          5m
----

. Verify cluster labels are applied by running the following command:
+
[source,terminal]
----
$ oc get managedclusters --show-labels
----

. Verify the `ManagedClusterSet` resource exists by running the following command:
+
[source,terminal]
----
$ oc get managedclusterset core-bm-clusters
----
