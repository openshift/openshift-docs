// Module included in the following assemblies:
//
// * maps/ocp-jobs/achieve-higher-host-availability-by-configuring-multipathing-on-my-nodes.adoc

:_mod-docs-content-type: CONCEPT
[id="achieve-higher-host-availability-by-configuring-multipathing-on-my-nodes-con_{context}"]
= Achieve higher host availability by configuring multipathing on my nodes

[role="_abstract"]
You can enable multipathing on the primary disk of your nodes after installation by creating a machine config that sets the required kernel arguments. Multipathing gives each node redundant paths to its storage, so the host stays available if a single path fails.
