// Module included in the following assemblies:
//
// * installing/overview-of-openshift-logging-installation.adoc

:_mod-docs-content-type: REFERENCE

[id="logging-preinstallation-checklist_{context}"]
= Preinstallation checklist

[role="_abstract"]
Before you install the Operators, verify the following prerequisites. Completing these checks prevents hours of troubleshooting later.

[cols="1,3",options="header"]
|===
|Requirement
|Description

|✅ Default StorageClass configured
|`LokiStack` requires persistent storage for its internal components. Without a default `StorageClass`, pods remain in `Pending` state and no logs are collected. See xref:../installing/verifying-cluster-prerequisites.adoc#verifying-cluster-prerequisites[Verifying cluster prerequisites].

|✅ Object storage created
|`LokiStack` requires an S3-compatible object storage backend (AWS S3, GCS, Azure Blob, MinIO, Ceph RGW, or Swift) to store log data. See xref:../installing/configuring-storage-for-lokistack.adoc#configuring-storage-for-lokistack[Configuring storage for LokiStack].

|✅ TLS CA bundle configured (if needed)
|If your object storage uses self-signed certificates or an internal certificate authority, you must create a CA bundle secret. Without this, logs are silently dropped. See xref:../installing/configuring-storage-for-lokistack.adoc#configuring-tls-ca-bundle-for-object-storage_configuring-storage-for-lokistack[Configuring TLS CA bundle].

|✅ Deployment size calculated
|Determine the correct `LokiStack` size (`1x.demo`, `1x.pico`, `1x.extra-small`, `1x.small`, or `1x.medium`) based on your expected log volume and query patterns. See xref:../installing/loki-deployment-sizing.adoc#loki-deployment-sizing[Loki deployment sizing].

|✅ Cluster capacity verified
|Ensure your cluster has sufficient CPU, memory, and persistent volume capacity for the chosen deployment size. A `1x.medium` deployment with ruler requires 70 vCPUs and 171 GiB memory.

|✅ Network connectivity verified
|Test connectivity between your cluster and the object storage endpoint. For cloud-managed storage, verify IAM roles or service accounts are configured correctly.
|===

[IMPORTANT]
====
The most common deployment failures occur when users install operators before completing these prerequisites. Missing a default `StorageClass` or TLS CA bundle causes silent failures where the `LokiStack` shows `Ready` but logs are not collected or stored.
====
