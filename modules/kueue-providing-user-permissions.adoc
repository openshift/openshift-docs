// Module included in the following assemblies:
//
// * ai_workloads/kueue/monitoring-pending-workloads.adoc

:_mod-docs-content-type: PROCEDURE
[id="providing-user-permissions_{context}"]
= Providing user permissions

You need to provide permissions to the users that require access to the specific APIs.

*NOTE TO REVIEWERS: CAN I HAVE EXAMPLES OF THESE? COMMANDS? MORE?*

* If the user needs access to the pending workloads of ClusterQueue, a ClusterRoleBinding needs to be created referencing the ClusterRole `kueue-batch-admin-role`.

* If the user needs access to the pending workloads of LocalQueue, a RoleBinding needs to be created referencing the ClusterRole `kueue-batch-user-role`.