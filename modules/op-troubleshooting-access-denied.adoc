// This module is included in the following assemblies:
//
// * create/migrate-from-clustertask-to-cluster-resolver.adoc

:_mod-docs-content-type: PROCEDURE
[id="troubleshooting-access-denied_{context}"]
= Resolve namespace access blocked errors

If you receive an error that access to the specified namespace is blocked, the cluster resolver might not have permission to access that namespace. This can occur when the cluster resolver configuration restricts access to certain namespaces, or when RBAC policies prevent the service account from accessing the namespace.

.Procedure

. Inspect the cluster resolver configuration by running the following command:
+
[source,terminal]
----
$ oc get configmap cluster-resolver-config -n openshift-pipelines -o yaml
----

. Check if the namespace is in the `blocked-namespaces` list:
+
[source,yaml]
----
data:
  blocked-namespaces: "kube-system,kube-public"
----
+
`kube-system,kube-public`:: Lists namespaces that cannot be accessed by the cluster resolver.

. If necessary, update the configuration to allow access to the namespace by running the following command:
+
[source,terminal]
----
$ oc edit configmap cluster-resolver-config -n openshift-pipelines
----

. Ensure that the service account running the pipeline has permissions to view tasks in the target namespace:
+
[source,yaml]
----
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pipeline-viewer
  namespace: shared-tasks
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: view
subjects:
- kind: ServiceAccount
  name: pipeline
  namespace: my-project
----
