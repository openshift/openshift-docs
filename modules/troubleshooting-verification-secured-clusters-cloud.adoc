// Module included in the following assemblies:
//
// * cloud_service/installing_cloud_ocp/verify-installation-cloud-ocp.adoc

:_mod-docs-content-type: REFERENCE
[id="troubleshooting-verification-secured-clusters-cloud_{context}"]
= Troubleshooting verification

[role="_abstract"]
If no data is displayed in the ACS Console after installation, you can perform the following troubleshooting steps.

* Ensure that at least one secured cluster connects to your {product-title-managed-short} instance.
* Examine your Sensor pod logs to ensure that the connection to your {product-title-managed-short} instance is successful.
* In the {osp} cluster, go to *Platform Configuration* -> *Clusters* to verify that the components are healthy and view additional operational information.
* Examine the values in the `SecuredCluster` API in the Operator on your local cluster to ensure that you entered the *Central API Endpoint* correctly. This value should be the same value as shown in the *ACS instance* details in the {cloud-console}.
