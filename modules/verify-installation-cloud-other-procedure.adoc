// Module included in the following assemblies:
//
// * cloud_service/installing_cloud_other/verify-installation-cloud-other.adoc

:_mod-docs-content-type: PROCEDURE
[id="verify-installation-cloud-other-procedure_{context}"]
= Verifying installation by accessing the ACS Console

[role="_abstract"]
You can verify that {product-title-managed-short} is successfully installed by accessing the ACS Console and checking the Dashboard for cluster monitoring data.

.Procedure

. Access your ACS Console from the {cloud-console}.

. Review the Dashboard, which displays the number of clusters that {product-title-managed-short} is monitoring, along with information about nodes, deployments, images, and violations.
+
If no data is displayed in the ACS Console, perform the following troubleshooting steps:
+
* Ensure that at least one secured cluster connects to your {product-title-managed-short} instance. For more information, see the instructions for installing by using Helm charts or by using the `roxctl` CLI.

* Examine your Sensor pod logs to ensure that the connection to your {product-title-managed-short} instance is successful.

* Examine the values in the `SecuredCluster` API in the Operator on your local cluster to ensure that you entered the *Central API Endpoint* correctly. This value should be the same value as shown in the *ACS instance* details in the {cloud-console}.
