// Module included in the following assemblies:
//
// * operating/examine-images-for-vulnerabilities.adoc
:_mod-docs-content-type: CONCEPT
[id="vm-scanning-requirements_{context}"]
= Requirements for virtual machine scanning

[role="_abstract"]
To use {product-title} ({product-title-short}) to scan virtual machines (VMs), you must meet specific requirements.

{ocp-virt-first} must meet the following requirements:

* The {ocp-virt} Operator is installed in the cluster you want to scan.
* The HyperConverged resource is patched to enable virtual socket (vsock) support.

VMs must meet the following requirements:

* VM resources include vsock support with the setting `spec.domain.devices.autoattachVSOCK: true`.
* VMs are running {op-system-base-full}. You must register any host where the scan agent is installed with Red Hat Subscription Manager and have an active {op-system-base} subscription.
* VMs have internet access to download repository-to-Common Platform Enumeration (CPE) mappings.
* DNF is installed.

{product-title-short} must meet the following requirements:

* Scanner V4 is deployed and available.

For resource requirements, such as CPU and memory resource allocation, see xref:recommended-requirements-vm-scanning_{context}[Resource requirements for virtual machine scanning].


