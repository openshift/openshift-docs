:_mod-docs-content-type: REFERENCE
[id="configure-build-buildrun-settings_{context}"]
= Configure Build and BuildRun Settings

[role="_abstract"]
Configure `Build` and `BuildRun` custom resources to control build execution, define parameter values, and manage controller behavior in OpenShift Builds by understanding the configurable fields in both resource types and the controller settings that affect build processing.