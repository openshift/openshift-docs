// This module is included in the following assemblies:
// * install_config/customizing-configurations-in-the-tektonconfig-cr.adoc

:_mod-docs-content-type: REFERENCE
[id="default-pruner-configuration_{context}"]
= Configuring the event-based pruner

//add TP snippet

//add intro

[IMPORTANT]
====
The event-based pruner can only be enabled or disabled in the TektonConfig custom resource (CR). Detailed configuration must be handled via ConfigMaps.
====

//add how to enable
.TektonConfig CR with the event-based pruner enabled
[source,yaml]
----
spec:
  tektonpruner:
    disabled: false

----


.Default ConfigMap definition
[source,yaml]
----

----
//add config reference
.Supported parameters for pruner configuration

