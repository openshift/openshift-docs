// Module included in the following assemblies:
// Epic CNF-9657 (TELCODOCS-1540) (4.16)
// * edge_computing/cnf-understanding-siteconfig-operator.adoc

:_mod-docs-content-type: CONCEPT
[id="cnf-understanding-templates_{context}"]
= Understanding templates

The {sco} supports the inclusion of Golang text templates in the object definitions, which allows you to dynamically generate installation manifests for target clusters.
This reduces the need to create separate manifests for many clusters with similar configurations but with different values.

Additionally, the {sco} supports all link:https://masterminds.github.io/sprig/[sprig library functions] and provides two new functions that you can use while creating your custom manifests:

`toYaml`:: The `toYaml` function encodes an item into a YAML string. If the item cannot be converted to YAML the function returns an empty string.

`anyFieldDefined`:: The `anyFieldDefined` function checks if a specific field is defined. If defined, the {sco} renders the field as instructed.

.anyFieldDefined example from InfraEnv
[source,yaml]
----
[...]
    {{ if (anyFieldDefined .Site.ProxySettings) }}
        proxy:
    {{ .Site.ProxySettings | toYaml | indent 4 }}
    {{ end }}
[...]
----

There are two types of templates:

* Cluster-level templates that must reference only cluster-specific fields.
* Node-level templates that can reference both cluster- and node-specific fields.

The {sco} provides the following default, validated, immutable set of templates:

.Default set of templates
[cols=4*, width="80%", options="header"]
|====

|Installation method
|Template type
|Template file name
|Template content

.2+|Assisted Installer
|Cluster-level templates
|`ai-cluster-templates-v1.yaml`
a|`ClusterDeployment`
`InfraEnv`
`KlusterletAddonConfig`
`ManagedCluster`
`AgentClusterInstall`

|Node-level templates
|`ai-node-templates-v1.yaml`
a|`NMStateConfig`
`BareMetalHost`

.2+|Image-based Install Operator
|Cluster-level templates
|`ibi-cluster-templates-v1.yaml`
a|`ClusterDeployment`
`KlusterletAddonConfig`
`ManagedCluster`

|Node-level templates
|`ibi-node-templates-v1.yaml`
a|`ImageClusterInstall`
`NetworkConfigMap`
`BareMetalHost`

|====

[id="cnf-special-template-variable_{context}"]
== Understanding special templates variables

The {sco} provides a set of special template variables that you can use in your templates.

`CurrentNode`:: This variable contains all the content for the current node being handled in templating.
`InstallConfigOverrides`:: This variable contains the merged `networkType`, `cpuPartitioningMode` and `installConfigOverrides` content.
`ControlPlaneAgents`:: This variable is automatically derived from the `SiteConfig` node objects. It consists of the number of control plane agents.
`WorkerAgents`:: This variable is automatically derived from the `SiteConfig` node objects. It consists of the number of worker agents.

You can create a custom templated field by capitalizing the field name in the text template.
For example, if you want to specify the `name` and `namespace` for your current node by using the `CurrentNode` special template variable, use the field names in the following form:

[source,yaml]
----
    name: "{{ .SpecialVars.CurrentNode.HostName }}"
    namespace: "{{ .Site.ClusterName }}"
----