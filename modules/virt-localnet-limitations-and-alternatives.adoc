// Module included in the following assemblies:
//
// * virt/vm_networking/virt-connecting-vm-to-physical-network-ovs-bridge.adoc

:_mod-docs-content-type: REFERENCE
[id="virt-localnet-limitations-and-alternatives_{context}"]
= Localnet network limitations and alternatives

[role="_abstract"]
Before implementing a localnet network for virtual machines (VMs), review the following limitations and consider alternative networking approaches if your use case requires features that localnet does not support.

You must consider the following limitations before implementing a localnet network:

* After you define the localnet network, you can only change the node selector configuration. If you need to change the localnet network definition, create a new `ClusterUserDefinedNetwork` manifest and update your VMs to reference the new network.
* MAC spoof check is always enabled with OVN localnet. You cannot change the MAC address of the running guest interface.
* IP address management (IPAM) is not supported.
* Promiscuous mode is not supported.
* VLAN trunking is not supported.

The following alternative networking approaches are available if your use case requires features that localnet does not support:

SR-IOV:: You can use Single Root I/O Virtualization (SR-IOV) network devices with additional networks on your {product-title} cluster installed on bare metal or Red Hat OpenStack Platform (RHOSP) infrastructure for applications that require high bandwidth or low latency.

Linux bridge CNI:: You can use a Linux bridge CNI to connect a VM to the physical network when VLAN trunking, MAC spoofing, or promiscuous mode is needed.

Primary layer 2 network:: You can use a custom primary layer 2 network for a fully managed overlay network capable of BGP peering and EVPN.

Secondary layer 2 overlay network:: You can configure a secondary layer 2 overlay network so that VMs can exchange traffic over a dedicated, persistent overlay subnet without touching the physical network switches or exposing traffic to the default pod network.
