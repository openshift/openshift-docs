// Module included in the following assemblies:
//
// * deployment/rhcl-egress-gateway.adoc

:_mod-docs-content-type: CONCEPT
[id="con-rhcl-egress-gateway_{context}"]
= Centralize control over outbound traffic

[role="_abstract"]
You can centralize control over outbound traffic from your cluster to external services by using an egress `Gateway` object.

include::snippets/egress-gateway-ossm.adoc[]

{prodname} extends {service-mesh} egress `Gateway` objects with rate limiting, workload identity, and credential injection policies by using Gateway API resources. With an egress `Gateway` object, you can:

* Apply rate limits to outbound requests using `RateLimitPolicy` custom resources (CRs).
* Identify internal workloads making outbound requests using Kubernetes `TokenReview` authentication with an `AuthPolicy` CR.
* Fetch external API credentials from a secret store and inject the credentials into outbound requests.
* Monitor egress traffic health with metrics and access logs.

The following describes the typical flow of an egress request:

. An internal workload sends an `HTTP` request to the egress `Gateway` object.
. The gateway applies {prodname} policies, such as rate limiting and authentication.
. The gateway originates TLS to the external service using a `DestinationRule` CR.
. The external service receives the request over `HTTPS` and responds.

[IMPORTANT]
====
Applications must send HTTP. For {prodname} policies to inspect request headers and apply rate limiting, the application must send plain HTTP to the egress Gateway `object`. The `DestinationRule` resource handles TLS origination to the external service. If the application sends HTTPS directly, the Gateway `object` cannot inspect the encrypted traffic and policies cannot be enforced.
====

To configure specific egress routing strategies or credential management::

To route outbound requests by using internal domain names or CoreDNS, see "Route pod traffic through an egress Gateway".
+
To automatically inject secrets or API tokens into outbound requests, see "Inject credentials into egress requests".

//TODO remove from here and include what is replaceable in each procedure
//The examples included use the following placeholder values:

//Egress gateway namespace
//`_<gateway_system>_`

//Egress gateway name
//`kuadrant-egressgateway`

//Egress gateway internal service
//`kuadrant-egressgateway-istio.gateway-system.svc.cluster.local`

//External target service
//`httpbin.org`
