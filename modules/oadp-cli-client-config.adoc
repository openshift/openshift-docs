// Module included in the following assemblies:
//
// * backup_and_restore/application_backup_and_restore/cli-tools/oadp-cli-plugin.adoc

:_mod-docs-content-type: REFERENCE
[id="oadp-cli-client-config_{context}"]
= OADP CLI client configuration commands

[role="_abstract"]
You can use the {oadp-short} command-line interface (CLI) to view and modify client configuration settings. Configuration is stored in `~/.config/velero/config.json`.

== Prerequisites

* The {oadp-short} CLI plugin is installed.

== Current configuration viewing command

To view the current client configuration, use the following command:

[source,terminal]
----
$ oc oadp client config get
----

== Configuration value setting command

To set a configuration value, use the following command:

[source,terminal,subs="+quotes"]
----
$ oc oadp client config set __<key>__=__<value>__
----

.Example of setting the default namespace
[source,terminal]
----
$ oc oadp client config set namespace=openshift-adp
----
