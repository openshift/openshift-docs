// Module included in the following assemblies:
//
// * argocd_applications/working-with-gitops-console-plugin.adoc

:_mod-docs-content-type: REFERENCE
[id="ref-gitops-console-troubleshooting_{context}"]
= Troubleshoot the GitOps Console plugin

.Prerequisites

* You have access to the {OCP} web console.
* The {gitops-shortname} Console plugin is enabled.


*GitOps is missing from the navigation*

The plugin is disabled, or the plugin pods are not running. Refresh the browser after you enable the plugin.

*ImageUpdaters or Rollouts are missing*

Those list pages appear only when the matching CRDs are installed.

*Empty lists or disabled actions*

Confirm the *Project* (namespace) selector and that resources exist. Empty lists or disabled edit and delete actions often mean the user lacks {OCP} RBAC (`get`, `list`, `create`, `update`, `patch`, or `delete`) on the {gitops-shortname} custom resources. An AppProject can still block an Argo CD sync after a YAML save succeeds.

*Health is Unknown or missing*

The plugin reads health from the Application CR. Set `controller.resource.health.persist: "true"` in the `argocd-cmd-params-cm` ConfigMap, for example in the `openshift-gitops` namespace.

*Application stays OutOfSync*

Check *Sync Status*, *History*, conditions, and *Events*. The Application kebab and History tab do not provide *Sync* or *Rollback*. Use the Argo CD UI, YAML, or the `argocd` CLI.

*View in Argo CD is missing or disabled*

The action requires a Route to the Argo CD server for that Application's instance. Confirm the Argo CD Route exists in the Application namespace or the instance namespace your Operator uses.

*The Application graph looks incomplete*

The *Graph view* on the Application *Resources* tab shows immediate managed resources only, not the full Argo CD resource tree. Use the Argo CD link on the tab for the complete hierarchy.

*List pages are slow*

Search, filtering, and pagination run in the browser on the loaded list. They do not use Kubernetes API `limit` or `continue` tokens. Select a narrower namespace or tighten filters.

*Pagination shows unexpected totals*

Totals reflect the filtered and searched result set, not the full cluster inventory. Clear filters or search, then confirm the namespace selector.

*ImageUpdater shows no recent updates*

Nothing was recorded in `status.recentUpdates` for the last reconciliation cycle. Check *Ready*, *Conditions* when present, and the ImageUpdater YAML `status`. Some ImageUpdater builds do not write conditions even when updates exist.

*Rollout Rollback is disabled*

Rollback is disabled for the current revision, which is the first revision in the tree, when the ReplicaSet is missing, or when you cannot `patch` the Rollout. Use a non-current revision and confirm your permissions.

