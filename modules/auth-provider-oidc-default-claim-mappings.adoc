// Module included in the following assemblies:
//
// * operating/manage-user-access/understanding-authentication-providers.adoc

:_mod-docs-content-type: REFERENCE
[id="auth-provider-oidc-default-claim-mappings_{context}"]
= OIDC default claim mappings

[role="_abstract"]
The following list provides the default OIDC claim mappings.

* `sub` to `userid`
* `name` to `name`
* `email` to `email`
* `groups` to `groups`
