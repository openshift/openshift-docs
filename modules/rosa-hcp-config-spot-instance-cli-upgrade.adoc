// Module included in the following assemblies:
//
// * rosa_hcp/rosa-hcp-config-aws-spot-instances-cli.adoc
:_mod-docs-content-type: PROCEDURE
[id="rosa-hcp-config-spot-instance-cli-upgrade_{context}"]
= Upgrading your {product-title} cluster with Spot instances from simple to enhance mode

[role="_abstract"]
You can upgrade an existing simple-mode cluster to enhanced mode by adding the AWS resources and associate the SQS queue URL with the cluster.

.Prerequisites
* You have SQS queue configured.
* You have your EventBridge rules configured.
* You have the associated resource policy configured

.Procedure

. Associate the queue URL with your existing cluster:
+
[source,terminal]
----
$ rosa edit cluster --cluster=<cluster-name> \
  --spot-termination-queue-url "${QUEUE_URL}"
----

Existing Spot machine pools immediately benefit from graceful termination handling. The AWS Node Termination Handler deployment is created in the control plane namespace and begins polling the queue. No machine pool changes are required.

[NOTE]
====
If you used the automated helper command, `rosa create spot-termination-queue`, the queue URL is associated to the cluster automatically. You do not need to run `rosa edit cluster` separately.
====
