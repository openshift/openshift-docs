// Module included in the following assemblies:
//
// * operating/manage-user-access/understanding-multi-tenancy.adoc

:_mod-docs-content-type: PROCEDURE
[id="multi-tenancy-per-namespace-config-example-procedure_{context}"]
= Configuring multi-tenancy per namespace

[role="_abstract"]
You can configure multi-tenancy by creating a custom permission set, access scope, and role that restricts user access to a specific namespace.

.Procedure

. In the {product-title-short} portal, select *Platform Configuration* -> *Access Control*.
. Select *Permission Sets*.
. Click *Create permission set*.
. Enter a *Name* and *Description* for the permission set.
. Select the following resources and access level and click *Save*:
** `READ` Alert
** `READ` Deployment
** `READ` DeploymentExtension
** `READ` Image
** `READ` K8sRole
** `READ` K8sRoleBinding
** `READ` K8sSubject
** `READ` NetworkGraph
** `READ` NetworkPolicy
** `READ` Secret
** `READ` ServiceAccount
. Select *Access Scopes*.
. Click *Create access scope*.
. Enter a *Name* and *Description* for the access scope.
. In the *Allowed resources* section, select the namespace you want to use for scoping and click *Save*.
. Select *Roles*.
. Click *Create role*.
. Enter a *Name* and *Description* for the role.
. Select the *Permission Set* and *Access scope* that you created before for the role and click *Save*.
. Assign the role to your required user or group.
+
[NOTE]
====
The {product-title-short} dashboard options for users with the sample role are minimal compared to options available to an administrator.
Only relevant pages are visible for the user.
====
