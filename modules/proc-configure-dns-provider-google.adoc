// Module included in the following assemblies:
//
// *install-guide/install-guide.adoc

:_mod-docs-content-type: PROCEDURE
[id="proc-configure-dns-provider-google_{context}"]
= Configure Google DNS provider credentials

[role="_abstract"]
If you want to configure DNS policies in {prodname} using Google Cloud, you must configure the DNS credentials. You must perform the steps on each {ocp} cluster that you want to use {prodname} on.

You must configure a DNS hosted zone. The credentials for your DNS provider must have permissions to update DNS records within this zone.

.Prerequisites

* You installed {prodname} on an {ocp} cluster.
* You have access to the namespace of your gateway, for example, `api-gateway`.
+
[NOTE]
====
If you already know your environment variable values, you can create the required YAML files as required for your use case.
====

.Procedure

. Optional: Specify your `GOOGLE` environment variable by running the following command:
+
[source,terminal]
----
$ export GOOGLE=xxxxxxx
----
+
`GOOGLE`: specifies the JSON credentials generated by the `gcloud` CLI or by the service account. For example, `$HOME/.config/gcloud/application_default_credentials.json`, which has the following credentials:
+
[source,json]
----
{"client_id": "***","client_secret": "***","refresh_token": "***","type": "authorized_user"}
----

. Optional: Specify your `PROJECT_ID` environment variable by running the following command:
+
[source,terminal]
----
$ export PROJECT_ID=xxxxxxx
----
+
`PROJECT_ID`: specifies the Google project ID.

. Create a `Secret` resource for your credentials by running the following command:
+
[source,terminal]
----
$ oc create secret generic test-gcp-credentials \
  --namespace=api-gateway \
  --type=kuadrant.io/gcp \
  --from-literal=PROJECT_ID=$PROJECT_ID \
  --from-file=GOOGLE=$GOOGLE
----
+
[IMPORTANT]
====
You must configure the secret in the same namespace as your gateway.
====
