// Module included in the following assemblies:
//
// * maps/ocp-jobs/build-modules-against-the-linux-kernel.adoc

:_mod-docs-content-type: CONCEPT
[id="build-modules-against-the-linux-kernel-con_{context}"]
= Build modules against the Linux kernel

[role="_abstract"]
Building kernel modules on a node requires the kernel development headers. You can create a machine config that adds the {op-system-base} `kernel-devel` extension to every node in a machine config pool.
