// Module included in the following assemblies:
//
// * infrastructure_lifecycle_management/ocloud-understanding-the-ocloud-manager.adoc

:_mod-docs-content-type: CONCEPT
[id="ocloud-architecture_{context}"]
= O-Cloud Manager architecture

[role="_abstract"]
The O-Cloud Manager runs on a hub cluster and integrates with several {product-title} and {rh-rhacm-first} components to manage the lifecycle of spoke clusters. 

The O-Cloud Manager exposes REST API servers for programmatic orchestration of the cluster lifecycle.

You can create and manage `ProvisioningRequest` CRs by using the following methods: 

* Use the Provisioning REST API to create or update the `ProvisioningRequest` CR through the provisioning server.
* Use the GitOps approach to create or update the `ProvisioningRequest` CR in your Git repository. ArgoCD synchronizes the CR to the hub cluster. 

In both cases, the controller manager watches the `ProvisioningRequest` CR and orchestrates the cluster lifecycle Operators to provision bare-metal servers and spoke clusters.

[NOTE]
====
The Alarms server leverages the {rh-rhacm} Observability stack for alarm management and subscriptions.
====


image::o_cloud_arch.png[O-Cloud Manager architecture]
