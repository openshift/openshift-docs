// Module included in the following assemblies:
//
// * rosa_cluster_admin/autonode/rosa-autonode-setup.adoc

:_mod-docs-content-type: PROCEDURE
[id="rosa-nodes-autonode-managing-set-env-vars_{context}"]
= Set environment variables for the {autonode} setup

[role="_abstract"]
Set the environment variables that are used throughout the {autonode} setup procedures.

.Procedure

* Export the following environment variables:
+
[source,terminal,subs="+quotes"]
----
$ export CLUSTER_NAME=<cluster_name>
$ export AWS_REGION=<aws_region>
$ export CLUSTER_ID=$(rosa describe cluster -c "$CLUSTER_NAME" -o json | jq -r '.id')
$ echo $CLUSTER_NAME $AWS_REGION $CLUSTER_ID
----
+
[IMPORTANT]
====
Ensure that your AWS client is using the region where your cluster is deployed.
====
