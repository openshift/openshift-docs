// Module included in the following assemblies:
//
// * maps/ocp-jobs/openstack-installer-provisioned-infrastructure-cinder-pvc.adoc

:_mod-docs-content-type: CONCEPT
[id="openstack-installer-provisioned-infrastructure-cinder-pvc-con_{context}"]
= Configure image registry storage on {rh-openstack-first} installer-provisioned infrastructure

[role="_abstract"]
You configure the image registry to use custom storage on clusters that run on {rh-openstack}. Configure custom storage when the registry must use a Cinder volume in a specific availability zone.
