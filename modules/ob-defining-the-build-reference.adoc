// This module is included in the following assembly:
//
// * configuring/configuring-openshift-builds.adoc

:_mod-docs-content-type: REFERENCE
[id="ob-defining-the-build-reference_{context}"]
= Define build references

[role="_abstract"]
Configure the `spec.build.name` field in your `BuildRun` resource to reference a `Build` resource that indicates an image to build. This reference connects a build run execution to its build definition, which contains the source code location, build strategy, and output image configuration.

[source,yaml]
----
apiVersion: shipwright.io/v1beta1
kind: BuildRun
metadata:
  name: buildah-buildrun
spec:
  build:
    name: buildah-build
----
