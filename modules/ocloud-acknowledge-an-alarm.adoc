// Module included in the following assemblies:
//
// * infrastructure_lifecycle_management/ocloud-monitoring-alarms-with-the-alarms-api.adoc

:_mod-docs-content-type: PROCEDURE
[id="ocloud-acknowledge-an-alarm_{context}"]
= Acknowledge an alarm

[role="_abstract"]
You can acknowledge an alarm through the O-Cloud Manager Alarms API to indicate that an operator is aware of the issue. Acknowledging an alarm does not resolve it, but marks it as reviewed.

.Prerequisites

* You authenticated to the O-Cloud Manager REST API and set the `API_BASE_URL` and `MY_TOKEN` environment variables.
* You identified the alarm to acknowledge by querying alarms.

.Procedure

* Acknowledge an alarm by running the following command:
+
[source,terminal]
----
$ curl -ks \
  -H "Authorization: Bearer ${MY_TOKEN}" \
  -H "Content-Type: application/merge-patch+json" \
  -X PATCH \
  -d '{"alarmAcknowledged": true}' \
  "https://${API_BASE_URL}/o2ims-infrastructureMonitoring/v1/alarms/<alarm_id>" | jq
----
+
Replace `<alarm_id>` with the `alarmEventRecordId` of the alarm to acknowledge.
+
[NOTE]
====
The PATCH endpoint also supports modifying `perceivedSeverity` to manually override the severity level of an alarm record.
====

.Verification

* Verify the alarm is acknowledged by running the following command:
+
[source,terminal]
----
$ curl -ks -H "Authorization: Bearer ${MY_TOKEN}" \
  "https://${API_BASE_URL}/o2ims-infrastructureMonitoring/v1/alarms/<alarm_id>" | jq '.alarmAcknowledged'
----
+
The output should return `true`.
