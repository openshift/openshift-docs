// Module included in the following assemblies:
//
// * backup_and_restore/application_backup_and_restore/cli-tools/oadp-cli-plugin.adoc

:_mod-docs-content-type: REFERENCE
[id="oadp-cli-admin-vsl_{context}"]
= Volume snapshot location management commands

[role="_abstract"]
You can use the {oadp-short} command-line interface (CLI) to create, view, set, and delete volume snapshot locations (VSLs). Volume snapshot locations define where persistent volume (PV) snapshots are stored.

[id="oadp-cli-admin-vsl-prereqs_{context}"]
== Prerequisites

* The {oadp-short} CLI plugin is installed and configured in admin mode.
* You are logged in to the {product-title} cluster as a user with `cluster-admin` privileges.

[id="oadp-cli-admin-vsl-create_{context}"]
== Volume snapshot location creation command

To create a volume snapshot location, use the following command:

[source,terminal]
----
$ oc oadp snapshot-location create <vsl_name> [flags]
----

.Flags for the `oc oadp snapshot-location create` command
[cols="1,3",options="header"]
|===
|Flag |Description

|`--provider`
|The name of the cloud provider, for example, `aws`, `gcp`, or `azure`.

|`--config`
|The provider-specific configuration as `key=value` pairs.

|`--request-timeout`
|The timeout for the request to the Kubernetes API server.
|===

.Example of the volume snapshot location creation command
[source,terminal]
----
$ oc oadp snapshot-location create my-vsl \
    --provider aws \
    --config region=us-east-1
----

[id="oadp-cli-admin-vsl-list_{context}"]
== Volume snapshot location listing command

To list all volume snapshot locations, use the following command:

[source,terminal]
----
$ oc oadp snapshot-location get [<vsl_name>] [flags]
----

.Flags for the `oc oadp snapshot-location get` command
[cols="1,3",options="header"]
|===
|Flag |Description

|`-o`
|The output format. Supported values are `json` and `yaml`.
|===

[id="oadp-cli-admin-vsl-set-default_{context}"]
== Default volume snapshot location command

To set the default volume snapshot location, use the following command:

[source,terminal]
----
$ oc oadp snapshot-location set <vsl_name>
----

[id="oadp-cli-admin-vsl-delete_{context}"]
== Volume snapshot location deletion command

To delete a volume snapshot location, use the following command:

[source,terminal]
----
$ oc oadp snapshot-location delete <vsl_name> [flags]
----

.Flags for the `oc oadp snapshot-location delete` command
[cols="1,3",options="header"]
|===
|Flag |Description

|`--confirm`
|Specifies whether to confirm the deletion without prompting.
|===
