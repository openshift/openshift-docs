// Module included in the following assemblies:
// * osd-whats-new.adoc

:_mod-docs-content-type: REFERENCE
[id="osd-q3-2026_{context}"]
= Q3 2026

[role="_abstract"]
The following items were added during the third quarter of 2026.

Bring your own firewall rules for {product-title} on {gcp-short} with Workload Identity Federation for shared VPC deployments::
+
Starting with {product-title} version 4.21, you can pre-create required cluster firewall rules in your {gcp-short} shared VPC host project before installing the {product-title} cluster into a shared VPC. This removes the need to grant `compute.firewalls.create` and `compute.firewalls.delete` permissions to Workload Identity Federation (WIF) service accounts.
This approach gives your network security team control over firewall rule management in the shared VPC host project and reduces the permissions granted to cluster-managed service accounts, aligning with the principle of least privilege.
+
[IMPORTANT]
====
At this time, only manual mode is supported. Additionally, this permission reduction applies only to shared VPC deployments. For service project deployments, the `compute.firewalls` permissions cannot yet be removed.
====
+
For more information, see link:https://docs.redhat.com/en/documentation/openshift_dedicated/4/html/planning_your_environment/gcp-ccs#osd-gcp-firewall-rules_gcp-ccs[Create firewall rules for reduced Workload Identity Federation permissions on {gcp-short}].

Reduced permissions for Shared VPC installations on {gcp-short}::
+
For Shared VPC deployments, you can now replace the broad `roles/compute.networkAdmin` and `roles/compute.securityAdmin` administrative roles on the host project with a custom role that grants only the minimal permissions required. Combined with pre-created firewall rules, this significantly reduces the privileges required across multi-project Shared VPC architectures.
+
For more information, see link:https://docs.redhat.com/en/documentation/openshift_dedicated/4/html/planning_your_environment/gcp-ccs#osd-gcp-shared-vpc-firewall-rules_gcp-ccs[Configure reduced permissions for a Shared VPC host project on {gcp-short}].

