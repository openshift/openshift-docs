// Module included in the following assemblies:
//
// * applications/application-health.adoc

:_mod-docs-content-type: PROCEDURE
[id="application-health-configuring_{context}"]
= Configure health checks using the CLI

[role="_abstract"]
To configure readiness, liveness, and startup probes, add one or more probes to the specification for the pod that contains the container which you want to perform the health checks.

[NOTE]
====
If you want to add or edit health checks in an existing pod, you must edit the pod `DeploymentConfig` object or use the web console. You cannot use the CLI to add or edit health checks for an existing pod.
====

.Procedure

. Create a `Pod` object to add one or more probes:
+
[source,yaml]
----
apiVersion: v1
kind: Pod
metadata:
  labels:
    test: health-check
  name: my-application
spec:
  containers:
  - name: my-container
    args:
    image: registry.k8s.io/goproxy:0.1
    livenessProbe:
      tcpSocket:
        port: 8080
      initialDelaySeconds: 15
      periodSeconds: 20
      timeoutSeconds: 10
    readinessProbe:
      httpGet:
        host: my-host
        scheme: HTTPS
        path: /healthz
        port: 8080
    startupProbe:
      exec:
        command:
        - cat
        - /tmp/healthy
      failureThreshold: 30
      periodSeconds: 20
      timeoutSeconds: 10
----

`name`:: Specify the container name.
`image`:: Specify the container image to deploy.
`livenessProbe`:: Optional: Create a Liveness probe.
`tcpSocket`:: Specify a test to perform, here a TCP Socket test.
`port`:: Specify the port on which the container is listening.
`initialDelaySeconds`:: Specify the time, in seconds, after the container starts before the probe can be scheduled.
`periodSeconds`:: Specify the number of seconds to perform the probe. The default is `10`. This value must be greater than `timeoutSeconds`.
`timeoutSeconds`:: Specify the number of seconds of inactivity after which the probe is assumed to have failed. The default is `1`. This value must be lower than `periodSeconds`.
`readinessProbe`:: Optional: Create a Readiness probe.
`httpGet`:: Specify the type of test to perform, here an HTTP test.
`host`:: Specify a host IP address. When `host` is not defined, the `PodIP` is used.
`scheme`:: Specify `HTTP` or `HTTPS`. When `scheme` is not defined, the `HTTP` scheme is used.
`port`:: Specify the port on which the container is listening.
`startupProbe`:: Optional: Create a Startup probe.
`exec`:: Specify the type of test to perform, here a Container Execution probe.
`command`:: Specify the commands to run on the container.
`failureThreshold`:: Specify the number of times to try the probe after a failure.
`periodSeconds`:: Specify the number of seconds to perform the probe. The default is `10`. This value must be greater than `timeoutSeconds`.
`timeoutSeconds`:: Specify the number of seconds of inactivity after which the probe is assumed to have failed. The default is `1`. This value must be lower than `periodSeconds`.
+
[NOTE]
====
If the `initialDelaySeconds` value is lower than the `periodSeconds` value, the first Readiness probe occurs at some point between the two periods due to an issue with timers.

The `timeoutSeconds` value must be lower than the `periodSeconds` value.
====

. Create the `Pod` object:
+
[source,terminal]
----
$ oc create -f <file-name>.yaml
----

. Verify the state of the health check pod:
+
[source,terminal]
----
$ oc describe pod my-application
----
+
.Example output
[source,terminal]
----
Events:
  Type    Reason     Age   From                                  Message
  ----    ------     ----  ----                                  -------
  Normal  Scheduled  9s    default-scheduler                     Successfully assigned openshift-logging/liveness-exec to node1.example.com
  Normal  Pulling    2s    kubelet, node1.example.com  pulling image "registry.k8s.io/liveness"
  Normal  Pulled     1s    kubelet, node1.example.com  Successfully pulled image "registry.k8s.io/liveness"
  Normal  Created    1s    kubelet, node1.example.com  Created container
  Normal  Started    1s    kubelet, node1.example.com  Started container
----
+
The following is the output of a failed probe that restarted a container:
+
.Sample Liveness check output with unhealthy container
[source,terminal]
----
$ oc describe pod pod1
----
+
.Example output
[source,terminal]
----
....

Events:
  Type     Reason          Age                From                                               Message
  ----     ------          ----               ----                                               -------
  Normal   Scheduled       <unknown>                                                             Successfully assigned aaa/liveness-http to worker-0.example.com
  Normal   AddedInterface  47s                multus                                             Add eth0 [10.129.2.11/23]
  Normal   Pulled          46s                kubelet, worker-0.example.com  Successfully pulled image "registry.k8s.io/liveness" in 773.406244ms
  Normal   Pulled          28s                kubelet, worker-0.example.com  Successfully pulled image "registry.k8s.io/liveness" in 233.328564ms
  Normal   Created         10s (x3 over 46s)  kubelet, worker-0.example.com  Created container liveness
  Normal   Started         10s (x3 over 46s)  kubelet, worker-0.example.com  Started container liveness
  Warning  Unhealthy       10s (x6 over 34s)  kubelet, worker-0.example.com  Liveness probe failed: HTTP probe failed with statuscode: 500
  Normal   Killing         10s (x2 over 28s)  kubelet, worker-0.example.com  Container liveness failed liveness probe, will be restarted
  Normal   Pulling         10s (x3 over 47s)  kubelet, worker-0.example.com  Pulling image "registry.k8s.io/liveness"
  Normal   Pulled          10s                kubelet, worker-0.example.com  Successfully pulled image "registry.k8s.io/liveness" in 244.116568ms
----
