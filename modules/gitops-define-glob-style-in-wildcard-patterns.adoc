// Module included in the following assemblies:
//
// * argocd_application_sets/managing-app-sets-in-non-control-plane-namespaces.adoc

:_mod-docs-content-type: PROCEDURE
[id="gitops-define-glob-style-in-wildcard-patterns_{context}"]
= Define glob-style in wildcard patterns

[role="_abstract"]
To grant permissions across multiple namespaces that share a common naming convention, use glob-style wildcard patterns.

.Procedure

. Use glob-style wildcard patterns in the `spec.applicationSet.sourceNamespaces` field to grant permissions across multiple namespaces:
+
[source,yaml]
----
apiVersion: argoproj.io/v1beta1
kind: ArgoCD
metadata:
  name: example
spec:
  sourceNamespaces:
    - team-*
  applicationSet:
    sourceNamespaces:
      - team-*
----
+
This configuration allows the Argo CD instance to manage `ApplicationSet` resources in namespaces, such as `team-1` and `team-2`.
