// Module included in the following assemblies:
//
// * installing/installing_azure/upi/installing-azure-user-infra.adoc
// * installing/installing_azure/upi/installing-restricted-networks-azure-user-provisioned.adoc

:_mod-docs-content-type: CONCEPT
[id="installation-requirements-user-infra_{context}"]
= Requirements for a cluster with user-provisioned infrastructure

[role="_abstract"]
For a cluster that uses user-provisioned infrastructure, you must deploy all required machines. These machines keep the cluster available during a node failure and include control plane and compute hosts that meet platform requirements.
