// Module included in the following assemblies:
//
// * security/rosa-configuring-the-log-forwarder.adoc
:_mod-docs-content-type: PROCEDURE
[id="rosa-create-an-iam-role_{context}"]
= Creating an _IAM_ role

Create an `IAM` role to enable the log forwarder by completing the following steps: 

.Procedure

. To enable the log forwarder delivery capability, create an `assume-role-polocy.json` by applying the following JSON sample: 
+
[source,json]
----
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Principal": {
                "AWS": "arn:aws:iam::859037107838:role/ROSA-CentralLogDistributionRole-241c1a86"
            },
            "Action": "sts:AssumeRole"
        }
    ]
}
----
+
. To enable the log forwarder distribution capability, create an `IAM` role by applying the following JSON sample: 
+
[source,json]
----
aws iam create-role \
    --role-name CustomerLogDistribution-RH \
    --assume-role-policy-document file://assume-role-policy.json
----
+
