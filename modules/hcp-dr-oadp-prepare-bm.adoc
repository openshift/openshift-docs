// Module included in the following assemblies:
//
// * hosted_control_planes/hcp-disaster-recovery-oadp.adoc

:_mod-docs-content-type: REFERENCE
[id="hcp-dr-oadp-prepare-bm_{context}"]
= Preparing bare metal to use {oadp-short}

To perform disaster recovery for a hosted cluster, you can use {oadp-first} on bare metal.

. Install the {oadp-short} Operator. See "Installing the {oadp-short} Operator" in the Additional resources of this section.

. Install the Data Protection Application by creating an instance of the `DataProtectionApplication` API. For more information, see "Configuring the OpenShift API for Data Protection with Multicloud Object Gateway" in the Additional resources of this section.

. After creating the `DataProtectionApplication` object, new `velero` deployment and `node-agent` pods are created in the `openshift-adp` namespace.
