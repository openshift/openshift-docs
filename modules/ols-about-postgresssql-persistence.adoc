// Module included in the following assemblies:
// * lightspeed-docs-main/configure/ols-configuring-openshift-lightspeed.adoc

:_mod-docs-content-type: CONCEPT
[id="about-postgresssql-persistence_{context}"]
= About {ols-short} and PostgressSQL Persistence 

Using PostgressSQL Persistence you can save {ols-long} conversation history to a PostgresSQL database so that the history is retained in persistent volume.The conversation history persists if the PostgreSQL pod restarts or is rescheduled. PostgresSQL persistence is disabled by default. To enable the functionality, define the `size` and `class` parameters in the `ols.storage` specification of the `OLSConfig` CR.