// Module included in the following assemblies:
//
// 

:_mod-docs-content-type: PROCEDURE
[id="oadp-self-service-creating-nab_{context}"]
= Creating a `NonAdminBackup` CR

As a non-admin user, you can create a `NonAdminBackup` (NAB) CR to back up your application from your authorized namespace. A NAB CR is an {product-title} custom resource that securely facilitates the creation of a `Velero` backup object. The `Velero` backup object reports the status back to the NAB CR that ultimately updates the `status.phase` field.

When you create a NAB CR:

* The initial phase for the CR is `New`. 
* The CR creation request goes to the `NonAdminController` (NAC) for reconciliation and validation. 
* Upon successful validation and creation of the `Velero` backup object, the `status.phase` field of the NAB CR is updated to the next phase, namely, `Created`.

Following are some important points to take note of, when you create a NAB CR:

* The `NonAdminBackup` CR creates the `Velero` backup object in a secure way so that other non-admin users cannot access the CR.
* As a non-admin user, you cannot specify the namespace to back up. The namespace from which the NAB CR is created, is the namespace to be backed up.
* Apart from creating the `Velero` backup object, the main purpose of the `NonAdminBackup` CR is to track the status of the `Velero` backup in a secure way.

.Prerequisites

* You are logged in to the cluster as a non-admin user.
* The cluster administrator has installed the {oadp-short} Operator.
* The cluster administrator has configured the `DataProtectionApplication` to enable {oadp-short} Self-Service.
* The cluster administrator has created a namespace for you and has authorized you to operate from that namespace.
* You have installed an application in your authorized namespace. 

.Procedure

. To create a `NonAdminBackup` CR, create a YAML manifest file with the following configuration:
+
.Example `NonAdminBackup`
[source,yaml]
----
apiVersion: oadp.openshift.io/v1alpha1
kind: NonAdminBackup
metadata:
  name: test-nab # <1>
spec:
  backupSpec:
    includedNamespaces:
    - "test-nac-ns" # <2>
    defaultVolumesToFsBackup: true
----
1. Specify a name for the NAB CR. In this example, `test-nab` is the name.
2. Specify the namespace that is authorized for you to create a NAB CR. In this example, `test-nac-ns` is the namespace.

. To create the NAB CR, run the following command:
+
[source,terminal]
----
$ oc apply -f <nab_cr_filename> # <1>
----
1. Specify the file name containing the NAB CR configuration.

.Verification

. To verify that the NAB CR is successfully created, run the following command:
+
[source,terminal]
----
oc get nab test-nab -o yaml
----
+
.Example output

The following annotated fields contain important information related to the NAB CR and the corresponding `Velero` backup object.
+
[source,yaml]
----
apiVersion: oadp.openshift.io/v1alpha1
kind: NonAdminBackup
metadata:
  creationTimestamp: "2025-02-11T13:37:33Z"
  finalizers:
  - nonadminbackup.oadp.openshift.io/finalizer
  generation: 2
  name: test-nab
  namespace: test-nac-ns # <1>
  resourceVersion: "1...182"
  uid: 5ce...7dc-7..29-4d..6-8..d1-b0...a60e
spec:
  backupSpec:
    csiSnapshotTimeout: 0s
    defaultVolumesToFsBackup: true
    hooks: {}
    includedNamespaces:
    - test-nac-ns
    itemOperationTimeout: 0s
    metadata: {}
    ttl: 0s
status:
  conditions:
  - lastTransitionTime: "2025-02-11T13:37:33Z"
    message: backup accepted # <2>
    reason: BackupAccepted
    status: "True"
    type: Accepted
  - lastTransitionTime: "2025-02-11T13:37:33Z"
    message: Created Velero Backup object
    reason: BackupScheduled
    status: "True"
    type: Queued
  phase: Created # <3>
  queueInfo:
    estimatedQueuePosition: 0 # <4>
  veleroBackup:
    nacuuid: test-nac-test-nab-6....fc60-1..1e-47c5-a0a9-16...4d51 # <5>
    name: test-nac-test-nab-63...c60-1..e-47c5-a0a9-168...2e4d51 # <6>
    namespace: openshift-adp
    status: # <7>
      completionTimestamp: "2025-02-11T13:37:54Z"
      expiration: "2025-03-13T13:37:33Z"
      formatVersion: 1.1.0
      hookStatus: {}
      phase: Completed # <8>
      progress:
        itemsBackedUp: 50
        totalItems: 50
      startTimestamp: "2025-02-11T13:37:33Z"
      version: 1
----
1. This is the namespace that the `NonAdminController` (NAC) sets on the `Velero` backup object to back up.
2. The NAC has reconciled and validated the NAB CR and has created the `Velero` backup object.
3. The NAB CR is in the `Created` phase.
4. This field indicates the queue position of the NAB CR. There can be multiple NAB CRs in process and each NAB CR is assigned a queue position. When the backup is complete, the queue position is set to 0.
5. The NAC creates the `Velero` backup object and sets the `nacuuid`.
6. This is the name of the associated `Velero` backup object.
7. This is the status of the `Velero` backup object.
8. The `Velero` backup object is in the `Completed` phase and the backup is successful.