// This module is included in the following assemblies:
//
// * installing/installing_bare_metal/bare-metal-using-bare-metal-as-a-service.adoc

:_mod-docs-content-type: PROCEDURE
[id="bmaas-enabling-bmo-to-watch-all-namespaces_{context}"]
= Enabling the Bare Metal Operator to watch all namespaces

To allow the Bare Metal Operator (BMO) to manage `BareMetalHost` resources across all namespaces in your {product-title} cluster, you must configure it to watch all namespaces. This configuration is important to avoid mixing non-{product-title} workloads with OpenShift components in the same namespace.

.Procedure

* Patch the provisioning configuration to enable watching all namespaces by running the following command:
+
[source,terminal]
----
$ oc patch provisioning/provisioning-configuration \
  --type merge -p '{"spec": {"watchAllNamespaces": true}}'
----
+
The BMO applies this change automatically, and no further action is required.

[NOTE]
====
For installer-provisioned infrastructure (IPI) installations, the installation program creates the Provisioning custom resource (CR) automatically. If you are using user-provisioned infrastructure (UPI) and the Provisioning CR does not exist, you must create it manually. For instructions, see https://docs.openshift.com/container-platform/{product-version}/installing/installing_bare_metal/scaling-a-user-provisioned-cluster-with-the-bare-metal-operator.html#configuring-a-provisioning-resource-to-scale-user-provisioned-clusters_scaling-a-user-provisioned-cluster-with-the-bare-metal-operator[Configuring a provisioning resource to scale user-provisioned clusters].
====