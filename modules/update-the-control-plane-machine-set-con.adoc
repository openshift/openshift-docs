// Module included in the following assemblies:
//
// * maps/ocp-jobs/update-the-control-plane-machine-set.adoc

:_mod-docs-content-type: CONCEPT
[id="update-the-control-plane-machine-set-con_{context}"]
= Change the configuration of control plane machines by updating the control plane machine set

[role="_abstract"]
You can change the configuration of your control plane machines by editing the control plane machine set. The Control Plane Machine Set Operator then replaces the machines so that they match the new configuration.
