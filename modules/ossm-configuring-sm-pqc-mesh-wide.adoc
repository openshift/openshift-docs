// Module included in the following assemblies:
//
// install/ossm-pqc-install.adoc

:_mod-docs-content-type: PROCEDURE
[id="ossm-configuring-sm-pqc-mesh-wide_{context}"]
= Configuring service mesh with mesh-wide {pqc-short}

[role="_abstract"]

Configure the {istio} control plane to enforce a {pqc-short} compliance policy, enabling quantum-resistant security for service mesh communications.

.Prerequisites

* You have logged in to the {ocp-product-title} web console 4.19+ as a user with the `cluster-admin` role.

* You have installed the {SMProductName} Operator 3.2.1+.

* You have deployed the `{istio}` and `IstioCNI` resources.

* You have installed the following CLI tools locally:
** `oc`
** `podman`
** `curl`

.Procedure

* Update the `{istio}` control plane to enable PQC by running the following command:
+
[source,terminal,subs="attributes,verbatim"]
----
$ oc apply -f - <<EOF
apiVersion: sailoperator.io/v1
kind: Istio
metadata:
  name: default
spec:
  version: v{istio-latest}
  namespace: istio-system
  updateStrategy:
    type: InPlace
  values:
    pilot:
      env:
        COMPLIANCE_POLICY: "pqc"
EOF
----
+
** `spec.values.pilot.env.COMPLIANCE_POLICY` specifies the compliance policy that the {istio} control plane enforces. Set the field to `pqc` to enable PQC.