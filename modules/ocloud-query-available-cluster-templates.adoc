// Module included in the following assemblies:
//
// * infrastructure_lifecycle_management/ocloud-discovering-resources-with-ocloud-manager-apis.adoc

:_mod-docs-content-type: PROCEDURE
[id="ocloud-query-available-cluster-templates_{context}"]
= Query available cluster templates

[role="_abstract"]
You can query the O-Cloud Manager Artifacts API to discover which cluster templates are available for provisioning.

.Prerequisites

* You authenticated to the O-Cloud Manager REST API and set the `API_BASE_URL` and `MY_TOKEN` environment variables.
* At least one validated `ClusterTemplate` custom resource (CR) exists on the hub cluster.

.Procedure

. List all available cluster templates by running the following command:
+
[source,terminal]
----
$ curl -ks -H "Authorization: Bearer ${MY_TOKEN}" \
  "https://${API_BASE_URL}/o2ims-infrastructureArtifacts/v1/managedInfrastructureTemplates" | jq
----

. Filter templates by name and version by running the following command:
+
[source,terminal]
----
$ curl -ks -H "Authorization: Bearer ${MY_TOKEN}" \
  "https://${API_BASE_URL}/o2ims-infrastructureArtifacts/v1/managedInfrastructureTemplates?filter=(eq,name,<template_name>);(eq,version,<template_version>)" | jq
----
+
* Replace `<template_name>` with the base name of the `ClusterTemplate` CR, for example `sno-ran-du`.
* Replace `<template_version>` with the template version, for example `v4-18-5-v1`.

. Get a specific template by its identifier by running the following command:
+
[source,terminal]
----
$ curl -ks -H "Authorization: Bearer ${MY_TOKEN}" \
  "https://${API_BASE_URL}/o2ims-infrastructureArtifacts/v1/managedInfrastructureTemplates/<template_id>" | jq
----
+
* Replace `<template_id>` with the `managedInfrastructureTemplateId` value, which follows the format `<name>.<version>`, for example `sno-ran-du.v4-18-5-v1`.

. View the template defaults to understand the preconfigured values by running the following command:
+
[source,terminal]
----
$ curl -ks -H "Authorization: Bearer ${MY_TOKEN}" \
  "https://${API_BASE_URL}/o2ims-infrastructureArtifacts/v1/managedInfrastructureTemplates/<template_id>/defaults" | jq
----
+
* Replace `<template_id>` with the `managedInfrastructureTemplateId` value, which follows the format `<name>.<version>`, for example `sno-ran-du.v4-18-5-v1`.
* The defaults response includes the merged content of the `clusterinstance-defaults` and `policytemplates-defaults` ConfigMap resources referenced by the `ClusterTemplate` CR.
