//Module included in the following assemblies:
// hosted_control_planes/hcp-manage/hcp-manage-aws.adoc

:_mod-docs-content-type: PROCEDURE
[id="hcp-aws-tags_{context}"]
= Adding or updating {aws-short} tags for a hosted cluster

As a hosted cluster administrator, you can add or update {aws-first} tags without needing to re-create your hosted cluster. _Tags_ are key-value pairs that are attached to {aws-short} resources for management and automation.

You might want to use tags for the following purposes:

* Managing access controls.
* Tracking chargeback or showback.
* Managing cloud IAM conditional permissions.
* Aggregating resources based on tags. For example, you can query tags to calculate resource usage and billing costs.

[NOTE]
====
On network load balancers, tags cannot be updated. The {aws-short} load balancer reconciles whatever tags are in the `HostedCluster` resource. If you try to add new tag, the load balancer overwrites everything.
====

.Prerequisites

* You must have cluster administrator permissions for your hosted cluster on {aws-short}.

.Procedure

* To enable EFS access point tags to be updated, complete the following steps:

. In the `aws-efs-csi-driver-operator` service account, add two annotations, as shown in the following example. These annotations are required so that the {aws-short} EKS pod identity webhook that runs on the cluster can correctly assign {aws-short} roles to the pods that the Operator uses.
+
[source,yaml]
----
eks.amazonaws.com/role-arn:<role_arn>
eks.amazonaws.com/audience:sts.amazonaws.com
----

. Delete the operator pod or roll out a restart of the `aws-efs-csi-driver-operator` deployment.

. To update the tags in a hosted cluster, in the `HostedCluster` resource, add the following information:
+
[source,yaml]
----

----

//add steps here to enable tags for other operators