// Module included in the following assemblies:
//
// * ai_applications/mcp_server/mcp-server-overview.adoc

:_mod-docs-content-type: PROCEDURE
[id="ai-app-mcp-server-prometheus-metrics_{context}"]
= Monitoring the MCP server for Red{nbsp}Hat OpenShift with Prometheus

[role="_abstract"]
The MCP server Red{nbsp}Hat OpenShift exposes metrics that you can collect with Prometheus to monitor server health, request rates, and toolset activity.

.Prerequisites
* MCP server Red{nbsp}Hat OpenShift is installed and running.
* // SME CONFIRM: user workload monitoring enabled? cluster monitoring only?

.Procedure
. // SME CONFIRM: does the LCO create a ServiceMonitor automatically, or must the user create one?
. Example ServiceMonitor:
+
[source,yaml]
----
apiVersion: monitoring.coreos.com/v1
  kind: ServiceMonitor
  metadata:
    name: mcp-server
    namespace: <mcp_server_namespace>
  spec:
    selector:
      matchLabels:
        app: <mcp_server_label> // SME CONFIRM: actual service label
    endpoints:
    - port: metrics // SME CONFIRM: metrics port name
----

.Available metrics
[cols="1,1,2",options="header"]
|===
|Metric |Type |Description
|===

// SME CONFIRM: metric names + types from engineering

Note: netedge already has netedge_query_prometheus (a tool that queries Prometheus) — keep that distinct from this (Prometheus scraping the server's own metrics). Worth a one-line clarifier so readers don't conflate them.
