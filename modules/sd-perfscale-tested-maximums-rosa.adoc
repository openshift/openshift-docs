// Module included in the following assemblies:
//
// * rosa_planning/rosa-hcp-limits-scalability.adoc
// * rosa_planning/rosa-limits-scalability.adoc

:_mod-docs-content-type: REFERENCE
[id="sd-perfscale-tested-maximums-rosa_{context}"]
= Tested cluster maximums ({rosa-classic-short} and {hcp-title})

[role="_abstract"]
The following table lists tested resource maximums for {product-title} 4.21 for {rosa-classic-short} and {hcp-title} clusters, measured without changing default cluster configuration beyond what the test describes.

.Tested cluster maximums (ROSA)
[options="header",cols="2,1,1"]
|===
| Resource | {rosa-classic-short} | {hcp-title}

| Number of compute (worker) nodes
| 249
| 501

| Number of pods ^[1]^
| 60,000
| 105,205

| Number of deployments
| 11,200
| 22,600

| Number of pods per node ^[2]^
| 250
| 250

| Number of namespaces ^[3]^
| 2,250
| 4,500

| Number of services ^[4]^
| 22,000
| 38,000

| Number of routes ^[5]^
| 4,500
| 9,000

| Number of secrets
| 29,000
| 59,000

| Number of config maps
| 34,000
| 68,000

| Number of pods per namespace
| 2,000
| 2,000

| Number of services per namespace ^[6]^
| 1,000
| 1,000

| Number of deployments per namespace
| 2,000
| 2,000
|===
[.small]
--
1. Simple pause pods staying idle on the cluster, including default control plane pods on {rosa-classic-short}; used `node-density` workload. Actual pod counts depend on workload requirements.
2. Workload shape can lower pods per node; for example, many PostgreSQL pods on one node with high disk IOPS, or limits on persistent volume claims (PVCs) per node.
3. Namespaces included a few idle pods plus default config maps and secrets each, including default operator namespaces.
4. ClusterIP services only; other service types can have different limits. Used `cni-workload`.
5. Test cluster used two routers with default configuration on infrastructure nodes ({rosa-classic-short}); used `cluster-density` workload.
6. ClusterIP services used a single endpoint each.
--
