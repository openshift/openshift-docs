// This module is included in the following assembly:
//
// builds/installing-sample-build-strategies.adoc

:_content-type: CONCEPT
[id="about-buildah_{context}"]
= Buildah

You can use the `buildah` cluster build strategy to build and push a container image by specifying the container file in your `Build` CR. You can install the `buildah` strategy at cluster level.
