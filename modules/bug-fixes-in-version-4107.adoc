// Module included in the following assemblies:
//
// * release_notes/410-release-notes.adoc

:_mod-docs-content-type: REFERENCE
[id="bug-fixes-in-version-4107_{context}"]
= Bug fixes in version 4.10.7

[role="_abstract"]
This release provides the following bug fixes:

//ROX-35859
* Before this update, the `NODE_EVENT` event source was missing from the security policy Custom Resource Definition (CRD), preventing you from configuring node-level security policies by using Kubernetes custom resources (CRs). With this release, `NODE_EVENT` is now available as an event source in the policy CRD, and you can configure node-level security policies by using CRs.

//ROX-35968
* Before this update, pending view-based reports failed to reschedule automatically when Central restarted. With this release, pending reports now reschedule correctly after a Central restart.

//ROX-36112
* Before this update, searches on V1 images that used the `Dockerfile instruction` field returned 0 results. With this release, queries that use the `Dockerfile instruction` field now return the expected results.

//ROX-35910
* Before this update, compliance scans that used CEL-scanner profiles, such as the {ocp-virt-first} profiles, ran successfully but showed no results in the {product-title-short} *Coverage* page. The scan results existed in the database but were not displayed because {product-title-short} could not connect the results to the profile. With this release, CEL-scanner profile results now appear correctly in the *Coverage* view.

This release also addresses the following security vulnerabilities:

* Go and golang.org:
//ROX-35672
** Go os.Root: Symlink following vulnerability allows directory traversal (link:https://access.redhat.com/security/cve/CVE-2026-39822[CVE-2026-39822])
//ROX-35436, ROX-36051
** Golang MIME: Denial of service via maliciously-crafted MIME header (link:https://access.redhat.com/security/cve/CVE-2026-42504[CVE-2026-42504])
//ROX-36096
* brace-expansion: Denial of service via unbounded intermediate arrays (link:https://access.redhat.com/security/cve/CVE-2026-69152[CVE-2026-69152])
