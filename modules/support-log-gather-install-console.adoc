// Module included in the following assemblies:
//
// * support/support-log-gather-operator-install.adoc

:_mod-docs-content-type: PROCEDURE
[id="support-log-gather-install-console_{context}"]
= Installing the {support-log-gather} by using the web console

You can use the web console to install the {support-log-gather}.

.Prerequisites

* You have access to the cluster with `cluster-admin` privileges.
* You have access to the {product-title} web console.

.Procedure

. Log in to the {product-title} web console.

. Navigate to *Operators* -> *OperatorHub*.

. Enter *{support-log-gather}* into the filter box.

. Select the *{support-log-gather}*

. Select the {support-log-gather} version from *Version* drop-down list, and click *Install*.

. On the *Install Operator* page:

.. Choose the *Installed Namespace* for the Operator. The default Operator namespace is `must-gather-operator`.
+
If the `must-gather-operator` namespace does not exist, it is created for you.

.. Select an *Update approval* strategy.
+
* The *Automatic* strategy allows Operator Lifecycle Manager (OLM) to automatically update the Operator when a new version is available.
+
* The *Manual* strategy requires a user with appropriate credentials to approve the Operator update.

.. Click *Install*.

.Verification

. Navigate to *Operators* -> *Installed Operators*.

. Verify that *{support-log-gather}* is listed with a *Status* of *Succeeded* in the `support-log-gather` namespace.

. Verify that {support-log-gather} pods are up and running by entering the following command:
+
[source,terminal]
----
$ oc get pods -n support-log-gather
----
+
.Example output
[source,terminal]
----

----
+
You can use the {support-log-gather} only after the pods are up and running.
