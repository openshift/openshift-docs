// Module included in the following assemblies:
//
// * infrastructure_lifecycle_management/ocloud-rest-api-reference.adoc

:_mod-docs-content-type: REFERENCE
[id="ocloud-artifacts-api-operations_{context}"]
= Artifacts API operations

[role="_abstract"]
The Artifacts API exposes available cluster templates, their parameter schemas, and preconfigured defaults. Use this API to discover what parameters a template accepts before creating or updating a provisioning request.

All endpoints use the base path `/o2ims-infrastructureArtifacts/v1`.

The `managedInfrastructureTemplateId` value has the format `<name>.<version>`, where `<name>` and `<version>` are the base name and version of the associated `ClusterTemplate` custom resource (CR), for example `sno-ran-du.v4-18-5-v1`.

.Artifacts API endpoints
[cols="1,3,3",options="header"]
|===
|Method |Path |Description

|`GET`
|`/managedInfrastructureTemplates`
|List available cluster templates

|`GET`
|`/managedInfrastructureTemplates/{managedInfrastructureTemplateId}`
|Get a specific template including its parameter schema

|`GET`
|`/managedInfrastructureTemplates/{managedInfrastructureTemplateId}/defaults`
|Get the preconfigured defaults for a template
|===
