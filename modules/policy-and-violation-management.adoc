// Module included in the following assemblies:
//
// * operating/manage_security_policies/about-security-policies.adoc

:_mod-docs-content-type: CONCEPT
[id="policy-and-violation-management_{context}"]
= Policy and violation management

[role="_abstract"] 

_Policy management_ encompasses the set of activities that security teams undertake in order to establish the appropriate guardrails. All of these activities happen *before* the policy is evaluated. _Violation management_ is the set of activities that security and developer teams take to assist in addressing security incidents in order to remediate policy violations.
All of these activities happen after a policy has been violated. 


Rule Setting:
With Policy As Code: RHACS supports both internal and external sources for policies. When using external policies, RHACS defines a Kubernetes native Custom Resource to streamline policy-as-code using tools like OpenShift Pipelines (ArgoCD) 
Define policy rules: Whether teams manage their policies as code or using the RHACS internal database, authoring policies can be technically challenging. RHACS offers an intuitive UI that simplifies this task by focusing on the controls, while hiding the specification complexity. Policies may be authored using the UI and then exported to YAML to be stored as code, or be stored locally  
Policy definition lifecycle: 
For internal policies, RHACS offers the complete Create, Read, Update, Delete (CRUD) actions.  For external policies, these actions are handled by users on their own, and the results are presented to RHACS using the custom resource.
For internal policies, RHACS also offers role based access control,  and track changes to policies in its audit log. 
Behavior configuration:
Setting the scope : RHACS is unique in providing users with multi-cluster governance in a centralized manner. Policies may select the scope for inclusion or exclusion for individual clusters, or they establish governance to apply to all clusters. 
Choose the action (inform/enforce), Enable/Disable the policy 
Configure notifiers 
Policy Testing
Dry run UI/API,  Policy CR validation
Reporting (UI/API)
Policy coverage
 


