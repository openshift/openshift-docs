// Module included in the following assemblies:
//
// * maps/ocp-jobs/change-management-state-of-the-image-registry.adoc

:_mod-docs-content-type: CONCEPT
[id="change-management-state-of-the-image-registry-con_{context}"]
= Change the management state of the image registry

[role="_abstract"]
Bare-metal installations do not provision registry storage automatically, so the image registry starts in the `Removed` state. Change the management state to `Managed` and configure persistent storage or {rh-storage-first} before the registry can store container images.
