// This module is included in the following assembly:
//
// * work-with-builds/using-builds.adoc

:_mod-docs-content-type: CONCEPT
[id="ob-deleting-custom-resources_{context}"]
= Deleting custom resources

You can delete the custom resources created by `Buildah`, `Source-to-image` and `buildpacks` build processes. 
