// Module included in the following assemblies:
//
// * maps/ocp-jobs/expose-the-default-image-registry-manually.adoc

:_mod-docs-content-type: CONCEPT
[id="expose-the-default-image-registry-manually-con_{context}"]
= Expose the default image registry manually

[role="_abstract"]
The {product-registry} is not exposed outside the cluster when the cluster is installed. Exposing it manually creates the route that external clients need in order to reach the registry.
