// Module included in the following assemblies:
//
// * maps/ocp-jobs/organize-image-versions-with-tags.adoc

:_mod-docs-content-type: CONCEPT
[id="organize-image-versions-with-tags-con_{context}"]
= Organize image versions with tags

[role="_abstract"]
Image stream tags are mutable pointers to image digests, so you can use readable names such as `latest` or `v2` instead of a digest. Use tags to request a specific version and to promote images between environments safely.
