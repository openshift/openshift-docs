// Module included in the following assemblies:
//
// * /cli/checking-policy-compliance.adoc
// * /cli/debugging-issues.adoc
// * /cli/managing-secured-clusters.adoc
// * /cli/using-the-roxctl-cli.adoc

:_mod-docs-content-type: PROCEDURE
[id="setting-up-environment-variables_{context}"]
= Prerequisites

[role="_abstract"]
Before using the `roxctl` CLI to manage secured clusters, you must configure the required environment variables.

.Procedure

* To configure the `ROX_ENDPOINT` environment variable, run the following command:
+
[source,terminal,subs="+quotes"]
----
$ export ROX_ENDPOINT=_<host:port>_
----
+
where:
+
--
`<host:port>`:: Specifies the host and port information that you want to store in the  `ROX_ENDPOINT` environment variable.
--