:_mod-docs-content-type: ASSEMBLY
include::_attributes/common-attributes.adoc[]
:context: core-cluster-upgrades
[id="assembly-core-cluster-upgrades"]
= Upgrade bare metal Core multi-node clusters

[role="_abstract"]
You can upgrade multi-node bare metal Core clusters by using z-stream, y-stream, or Extended Update Support (EUS) to EUS approaches with GitOps and {cgu-operator-full} ({cgu-operator}).
Multi-node bare metal clusters require special considerations for control plane sequencing, worker node batching, and network connectivity during upgrades.
Automation for control plane sequencing and worker node batching for multi-node clusters is available through {cgu-operator}.

Multi-node bare metal Core clusters typically include three or more control plane nodes and multiple worker nodes running network functions.
Understanding multi-node upgrade patterns ensures safe upgrades with minimal workload disruption.

== Multi-node cluster upgrade overview

Multi-node cluster upgrades differ from single-node cluster upgrades in several important ways:

* *Control plane rolling updates*: Control plane nodes upgrade one at a time in a rolling fashion.
The etcd cluster maintains quorum throughout the upgrade because you have three or more control plane nodes.
* *API server availability*: The API server might be briefly unavailable during some control plane operator restarts.
Most requests succeed, but some API calls might fail temporarily during control plane updates.
* *Worker node batching*: Worker nodes upgrade in batches controlled by the `maxUnavailable` setting in the MachineConfigPool.
The ideal value for `maxUnavailable` depends on your application configuration and how your workloads tolerate disruption.
* *Network requirements*: Cluster network connectivity must remain available between nodes during rolling upgrades.
The CNI plugin must support version skew while control plane and worker nodes run different versions.

== Control plane upgrade sequencing

During multi-node cluster upgrades, the control plane updates according to this sequence:

. The Cluster Version Operator identifies the target version.
. Control plane nodes update one at a time in a rolling fashion.
. While one control plane node updates, the other control plane nodes continue serving API requests.
. The etcd cluster maintains quorum with the remaining healthy control plane nodes.
. After each control plane node completes its update, the next control plane node begins updating.

This rolling update pattern ensures the control plane remains available throughout the upgrade, although brief API disruptions might occur during some control plane operator restarts.

== Worker node batching and pod disruption budgets

Worker nodes upgrade in batches determined by the `maxUnavailable` setting:

* *Default `maxUnavailable`*: 1 worker node or 33% of worker nodes, whichever is smaller
* *`maxUnavailable` tuning*: Ideally, MCPs run at 100% `maxUnavailable` for faster upgrades. However, the appropriate value depends on your application configuration and how your workloads tolerate disruption. Consult your application teams to determine the right setting.
* *Pod Disruption Budget interaction*: PDBs must allow at least one disruption, or node upgrades will be blocked

Configure Pod Disruption Budgets for all critical network functions.
The following example uses `maxUnavailable` to specify the maximum number of pods that can be unavailable during a disruption:

[source,yaml]
----
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cnf-workload-pdb
  namespace: cnf-workload
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: cnf-workload
----

Setting `maxUnavailable: 1` allows one pod to be unavailable during node upgrades.
Ensure your PDBs allow at least one disruption, or node draining will be blocked and upgrades will stall.

== Monitoring multi-node upgrades

Monitor these resources during multi-node cluster upgrades:

* *Cluster version*: `oc get clusterversion -w`
* *Cluster operators*: `oc get co`
* *Node status*: `oc get nodes -w`
* *Machine config pools*: `oc get mcp -w`
* *etcd quorum*: `oc get etcd`
* *{cgu-operator} CGU progress*: `oc get cgu <cgu_name> -w`
* *Update status*: `oc adm upgrade status`

For a combined view of the cluster upgrade state, use the following command:

[source,terminal]
----
$ watch "oc get clusterversion; echo; oc get co | head -1; oc get co | grep -v 'True.*False.*False'; echo; oc get nodes; echo; oc get mcp"
----

For multi-cluster upgrades, monitor the ClusterGroupUpgrade status to track progress across all target clusters.

include::modules/core-cluster-upgrade-scenarios.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-gitops-talm-overview.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-talm-setup.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-cgu-configuration.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-health-check.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-z-stream.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-y-stream.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-y-stream-sequential.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-eus-control-plane-only.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-worker-node-management.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-operator-coordination.adoc[leveloffset=+1]

include::modules/core-cluster-upgrade-troubleshooting.adoc[leveloffset=+1]

[role="_additional-resources"]
== Additional resources

* xref:../../../post_installation_configuration/day_2_core_cnf_clusters/updating/update-welcome.adoc#update-welcome[Upgrading an {product-title} cluster]
* xref:../../../post_installation_configuration/day_2_core_cnf_clusters/updating/update-api.adoc#update-api[Verifying cluster API versions between update versions]
* xref:../../../post_installation_configuration/day_2_core_cnf_clusters/updating/update-ocp-update-prep.adoc#update-ocp-update-prep[Preparing a bare-metal cluster for platform update]
* xref:../../../post_installation_configuration/day_2_core_cnf_clusters/updating/update-cnf-update-prep.adoc#update-cnf-update-prep[Configuring application pods before updating your {product-title} cluster]
* xref:../../../backup_and_restore/control_plane_backup_and_restore/backing-up-etcd.adoc#backup-etcd[Backing up etcd]
* xref:../../../post_installation_configuration/day_2_core_cnf_clusters/updating/update-before-the-update.adoc#update-checking-cluster-health_update-before-the-update[Checking the cluster health]
* xref:../../../edge_computing/cnf-talm-for-cluster-upgrades.adoc#cnf-talm-for-cluster-upgrades[Using the {cgu-operator-full} for cluster updates]
* xref:../../../scalability_and_performance/telco-core-rds.adoc#telco-core-ref-design-specs[Bare metal Core reference design specifications]
* link:https://kubernetes.io/docs/tasks/run-application/configure-pdb/[Kubernetes PodDisruptionBudget documentation]
* link:https://www.redhat.com/en/blog/how-to-use-the-topology-aware-lifecycle-manager[How to use the {cgu-operator-full}]
* link:https://www.redhat.com/en/blog/the-ultimate-guide-to-openshift-release-and-upgrade-process-for-cluster-administrators[The ultimate guide to OpenShift release and upgrade process for cluster administrators]
